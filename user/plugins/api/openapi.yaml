openapi: 3.0.3

info:
  title: Grav CMS API
  description: |
    RESTful API for managing Grav CMS content, media, configuration, users, and
    system operations.

    ## Authentication

    The API supports three authentication methods. All three provide the same
    level of access — the authenticated user's permissions apply regardless of
    method. Every request (except `/auth/*` endpoints) must include one of:

    | Method | Header / mechanism | Best for |
    |---|---|---|
    | **API key** | `X-API-Key: grav_...` | CLI tools, MCP servers, server-to-server, automation |
    | **JWT token** (recommended) | `X-API-Token: <token>` | SPAs, mobile apps, admin-next plugin bundles |
    | **JWT Bearer token** (alt) | `Authorization: Bearer <token>` | Standard HTTP clients on hosts that don't strip the header |
    | **Session cookie** | `grav-site` cookie | Admin panel extensions |

    > Prefer `X-API-Token` over `Authorization: Bearer` when portability matters:
    > PHP under FastCGI / CGI / PHP-FPM (notably MAMP's `mod_fastcgi`) silently
    > strips `Authorization` before it reaches the application. A custom `X-*`
    > header bypasses this entirely. The server accepts either transport for the
    > same JWT.

    **API keys** are long-lived credentials that don't expire by default. Generate
    them via `bin/plugin api keys:generate` or the admin panel. Best for any
    server-side or automated integration.

    **JWT tokens** are short-lived (1 hour default). Obtain them by posting
    credentials to `/auth/token`, then refresh before expiry via `/auth/refresh`.
    The refresh flow uses token rotation — each refresh revokes the previous
    refresh token. Best for client-side apps where long-lived secrets shouldn't
    be stored.

    ## Response Envelope

    All successful responses use a standard JSON envelope:

    ```json
    {
      "data": { ... },
      "meta": { ... },
      "links": { ... }
    }
    ```

    `meta` and `links` are present only on paginated responses.

    ## Error Format (RFC 7807)

    Error responses use `application/problem+json`:

    ```json
    {
      "status": 422,
      "title": "Unprocessable Entity",
      "detail": "Missing required fields: title",
      "errors": [
        { "field": "title", "message": "The 'title' field is required." }
      ]
    }
    ```

    ## Environments

    Send the optional `X-Grav-Environment` header to target a specific Grav
    environment. If omitted, defaults to the auto-detected environment (from
    hostname). Use `GET /system/environments` to discover available environments.

    ## Rate Limiting

    When rate limiting is enabled, every response includes:

    - `X-RateLimit-Limit` -- maximum requests per window
    - `X-RateLimit-Remaining` -- requests remaining
    - `X-RateLimit-Reset` -- UTC epoch when the window resets

    ## Optimistic Concurrency (ETags)

    Single-resource GET responses include an `ETag` header. For PATCH requests,
    send the same value in `If-Match` to detect mid-air collisions. A `409
    Conflict` response is returned when the resource has changed since the last
    GET.
  version: 1.0.0-beta.1
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT
  contact:
    name: Team Grav
    url: https://getgrav.org
    email: devs@getgrav.org

servers:
  - url: "{baseUrl}/api/v1"
    description: Default Grav CMS API server
    variables:
      baseUrl:
        default: ""
        description: >
          The base URL of the Grav installation. Leave empty when using
          relative paths, or set to a full origin such as
          https://example.com.

tags:
  - name: Auth
    description: JWT token management -- obtain, refresh, and revoke tokens.
  - name: Pages
    description: Create, read, update, delete, move, and copy Grav pages.
  - name: Media
    description: Manage media files attached to pages or the site-level images folder.
  - name: Config
    description: Read and update Grav configuration (system, site, plugins, themes).
  - name: Users
    description: User account and API key management.
  - name: System
    description: System information, cache management, logs, and backups.
  - name: Languages
    description: Site language configuration and page translations.
  - name: GPM
    description: Package management -- install, remove, update plugins, themes, and Grav core.
  - name: Scheduler
    description: Job scheduling, status, and execution history.
  - name: Sidebar
    description: Admin sidebar navigation items contributed by plugins.
  - name: MCP
    description: Tool manifests contributed by plugins, for an MCP server to offer to a model.
  - name: Dashboard
    description: Notifications, news feed, and statistics.
  - name: Webhooks
    description: Outbound webhook configuration and delivery management.
  - name: Blueprints
    description: Form schema definitions for pages, plugins, themes, and system configuration. Blueprints define the fields, types, validation, and layout used to render editing interfaces.
  - name: Data
    description: Resolve blueprint data-options@ directives and other dynamic data lookups.
  - name: Thumbnails
    description: Serve cached image thumbnails.
  - name: Menubar
    description: Admin toolbar buttons contributed by plugins, and the actions they run.
  - name: Groups
    description: User groups and the access rules they grant, stored in `user/config/groups.yaml`.
  - name: Demo
    description: Demo-mode engine control -- reset status, baseline capture, and on-demand resets.
  - name: Reports
    description: Diagnostic reports, plus the Twig in Content report's per-page status, content scan, sandbox policy and allowlist actions.
  - name: Audit
    description: Read-only access to the admin audit trail (super admins only, off by default).
  - name: Translations Editor
    description: Browse this site's translation strings across every source and edit its per-language overrides in `user/languages`. Authenticated, unlike the public `/translations/{lang}` dictionary.
  - name: Admin Next
    description: |
      UI preferences and site branding for Admin Next (admin2). These endpoints exist to support the Admin Next single-page admin: site-wide defaults, per-user overrides, and the logo/favicon/sign-in copy stored in `user/config/admin-next.yaml`. Other clients can use them, but the payloads follow what Admin Next needs.
  - name: UI Extensions
    description: |
      Registries that let plugins and themes extend the Admin Next (admin2) interface: custom blueprint field types, editor toolbar buttons, floating widgets, page-editor context panels, and Settings page panels. Each list is collected at request time from an `onApi*` event, so its contents depend on which plugins are enabled. The matching JavaScript web components are served by the `/gpm/.../*-script` and `/gpm/.../field(s)` endpoints (tag GPM).

security:
  - apiToken: []
  - apiKey: []
  - bearerAuth: []
  - sessionAuth: []

paths:
  # ---------------------------------------------------------------------------
  # Auth
  # ---------------------------------------------------------------------------
  /auth/token:
    post:
      operationId: createToken
      tags: [Auth]
      summary: Obtain a JWT token pair
      description: |
        Authenticate with username and password to receive an access token and a
        refresh token. The access token is short-lived; use the refresh token to
        obtain a new pair without re-authenticating.

        If the site requires a login captcha, `captcha_token` is mandatory and
        the request fails with 422 without it. Call `GET /auth/captcha` first to
        find out.

        Every token pair carries the `user` profile (resolved permissions and
        demo-mode state, the same data `GET /me` returns). When the account has
        2FA enabled, the response is an `AuthTwoFactorChallenge` (`requires_2fa:
        true` plus a `challenge_token`) instead of tokens; finish the login with
        `POST /auth/2fa/verify`.

        The account must be a super admin or hold `admin.login` or `api.access`,
        and must not be disabled; otherwise the request fails with 403.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [username, password]
              properties:
                username:
                  type: string
                  example: admin
                password:
                  type: string
                  format: password
                  example: Pa$$w0rd!
                captcha_token:
                  type: string
                  description: >-
                    A solved captcha, required only when the site has a login
                    captcha configured. For the built-in `cap` provider this is
                    the token returned by `POST /auth/captcha/redeem`; for
                    Turnstile or reCAPTCHA it is the widget's response token.
                    Single use.
            example:
              username: admin
              password: Pa$$w0rd!
      responses:
        "200":
          description: Token pair with the user profile, or a 2FA challenge when the account has 2FA enabled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    oneOf:
                      - $ref: "#/components/schemas/AuthTokenPairWithUser"
                      - $ref: "#/components/schemas/AuthTwoFactorChallenge"
              examples:
                tokens:
                  summary: Token pair
                  value:
                    data:
                      access_token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
                      refresh_token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
                      token_type: Bearer
                      expires_in: 3600
                      user:
                        username: admin
                        fullname: Site Administrator
                        email: admin@example.com
                        avatar_url: null
                        super_admin: true
                        access:
                          admin:
                            super: true
                        content_editor: ""
                        demo_mode:
                          enabled: false
                          writable: []
                          reset_interval: 30
                          seconds_until_reset: null
                challenge:
                  summary: 2FA challenge
                  value:
                    data:
                      requires_2fa: true
                      challenge_token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
                      expires_in: 300
                      token_type: Challenge
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /auth/captcha:
    get:
      operationId: getCaptchaConfig
      tags: [Auth]
      summary: Discover the login captcha
      description: |
        Report which captcha (if any) guards the unauthenticated auth endpoints,
        so a client can render the right challenge before submitting. Public,
        and never returns a secret.
      security: []
      responses:
        "200":
          description: Captcha configuration.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      enabled:
                        type: boolean
                      provider:
                        type: string
                        nullable: true
                        enum: [cap, turnstile, recaptcha, null]
                      mode:
                        type: string
                        enum: [invisible, checkbox]
                      token_field:
                        type: string
                        description: Body field to send the solved token in.
                      flows:
                        type: object
                        description: Which unauthenticated flows are gated.
                        properties:
                          login:
                            type: boolean
                          forgot_password:
                            type: boolean
                          setup:
                            type: boolean
                      endpoint:
                        type: string
                        description: Base URL of the cap challenge/redeem pair. Only for the `cap` provider.
                      site_key:
                        type: string
                        description: Public widget key. Only for the `turnstile` and `recaptcha` providers.
                      version:
                        type: string
                        description: reCAPTCHA widget version from the Form plugin's config (default `2-checkbox`). Only for the `recaptcha` provider.
                        example: 2-checkbox
              example:
                data:
                  enabled: true
                  provider: cap
                  mode: invisible
                  token_field: captcha_token
                  flows:
                    login: true
                    forgot_password: true
                    setup: false
                  endpoint: https://example.com/api/v1/auth/captcha/

  /auth/captcha/challenge:
    post:
      operationId: createCaptchaChallenge
      tags: [Auth]
      summary: Mint a proof-of-work challenge
      description: |
        Issue a cap.js challenge for the client to solve. Only available while
        `cap` is the active provider; 404 otherwise. The response is the cap.js
        wire format, not this API's `data` envelope.
      security: []
      responses:
        "200":
          description: Challenge issued.
          content:
            application/json:
              schema:
                type: object
                properties:
                  challenge:
                    type: object
                    properties:
                      c:
                        type: integer
                        description: Number of sub-challenges.
                      s:
                        type: integer
                        description: Salt length.
                      d:
                        type: integer
                        description: Required leading zero digits.
                  token:
                    type: string
                  expires:
                    type: integer
                    format: int64
              example:
                challenge: { c: 30, s: 32, d: 4 }
                token: 72d323130ee3541275ca2dc1aa9607b4ca61095a08
                expires: 1787332679923
        "404":
          $ref: "#/components/responses/NotFound"

  /auth/captcha/redeem:
    post:
      operationId: redeemCaptchaChallenge
      tags: [Auth]
      summary: Exchange solutions for a captcha token
      description: |
        Trade the solved sub-challenges for a single-use token to send with the
        login, forgotten-password or setup request. Only available while `cap`
        is the active provider; 404 otherwise. The response is the cap.js wire
        format, not this API's `data` envelope.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [token, solutions]
              properties:
                token:
                  type: string
                  description: The challenge token from `/auth/captcha/challenge`.
                solutions:
                  type: array
                  description: One nonce per sub-challenge, in order.
                  items:
                    type: integer
      responses:
        "200":
          description: Result of the redemption. `success` is false when the solutions don't verify.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  token:
                    type: string
                  expires:
                    type: integer
                    format: int64
                  message:
                    type: string
              example:
                success: true
                token: e734e7c49c34bb0d:d43a6be90ca08cf9f91171a5320e0a
                expires: 1787333298530
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"

  /auth/refresh:
    post:
      operationId: refreshToken
      tags: [Auth]
      summary: Refresh a JWT token pair
      description: |
        Exchange a valid refresh token for a new access/refresh pair. The
        provided refresh token is automatically revoked (token rotation).
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [refresh_token]
              properties:
                refresh_token:
                  type: string
                  description: The refresh token obtained from `/auth/token` or a previous refresh.
            example:
              refresh_token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
      responses:
        "200":
          description: New token pair, with the user profile, returned successfully.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/AuthTokenPairWithUser"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /auth/revoke:
    post:
      operationId: revokeToken
      tags: [Auth]
      summary: Revoke a refresh token
      description: |
        Explicitly revoke a refresh token so it can no longer be used. This is
        the recommended way to implement logout on the client side.

        If the request also presents an access token (`X-API-Token` or
        `Authorization: Bearer`), that access token is revoked too, so the
        current session ends immediately. Always returns 204, even for an
        unknown or already-revoked refresh token.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [refresh_token]
              properties:
                refresh_token:
                  type: string
            example:
              refresh_token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
      responses:
        "204":
          description: Token revoked successfully. No content returned.
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /auth/2fa/verify:
    post:
      operationId: verifyTwoFactorLogin
      tags: [Auth]
      summary: Complete a 2FA login
      description: |
        Second step of a login for an account with two-factor authentication enabled. `POST /auth/token` (or an SSO exchange) returns a `requires_2fa` challenge instead of tokens; send its `challenge_token` here with the current TOTP code to receive the token pair. The challenge token is burned on success so it cannot be replayed.
        Public. Shares the login rate limiter with `POST /auth/token`, keyed by the challenged username.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [challenge_token, code]
              properties:
                challenge_token:
                  type: string
                  description: The `challenge_token` from the `requires_2fa` login response.
                code:
                  type: string
                  description: Current six-digit code from the authenticator app.
                  example: "123456"
            example:
              challenge_token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
              code: "123456"
      responses:
        "200":
          description: Code verified. Token pair returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/AuthTokenPairWithUser"
        "401":
          description: The challenge token is invalid or expired, or the code is wrong.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 401
                title: Unauthorized
                detail: Invalid 2FA code.
        "403":
          description: The account is disabled, or the Login plugin's 2FA support is not installed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /auth/forgot-password:
    post:
      operationId: requestPasswordReset
      tags: [Auth]
      summary: Request a password reset email
      description: |
        Send a password reset link to the account that owns `email`. The response is the same neutral message whether or not the address matches an account, whether the Email and Login plugins are configured, and whether sending failed, so it cannot be used to find accounts.
        The link points at the admin's own `/reset` route. Its base is taken from `admin_base_url`, then the `Referer`, then the `Origin` header, and each candidate is accepted only when its origin is the site's own or is listed in the API's `cors.origins`; anything else falls back to the site's root URL. The reset token is valid for 24 hours.
        Public. When the site has a login captcha that covers the forgotten-password flow, the solved token is required (see `GET /auth/captcha`). Repeated requests for one account are limited by the Login plugin's `pw_resets` bucket.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties:
                email:
                  type: string
                  format: email
                admin_base_url:
                  type: string
                  format: uri
                  description: The admin's base URL (origin plus admin route) to build the reset link from. Ignored unless its origin is trusted.
                  example: https://example.com/admin
                captcha_token:
                  type: string
                  description: A solved captcha. Required only when the site's login captcha covers this flow.
            example:
              email: jane@example.com
              admin_base_url: https://example.com/admin
      responses:
        "200":
          description: Neutral acknowledgement, returned whether or not an email was sent.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      message:
                        type: string
              example:
                data:
                  message: If an account exists for that email, a reset link has been sent.
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /auth/reset-password:
    post:
      operationId: resetPassword
      tags: [Auth]
      summary: Set a new password with a reset token
      description: |
        Complete a password reset using the `user` and `token` values from the reset link. Every failure (unknown user, wrong token, expired token) returns the same 422 message so tokens cannot be probed. Once the link is valid, the new password must meet the site's password policy (`system.pwd_regex`, or at least 8 characters when none is set; see `GET /auth/password-policy`), otherwise a 422 names the `password` field and the link stays usable. On success every access and refresh token issued to the account before the reset stops working.
        Public. Rate limited per username through the Login plugin's login bucket.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [username, token, password]
              properties:
                username:
                  type: string
                token:
                  type: string
                  description: The reset token from the emailed link.
                password:
                  type: string
                  format: password
                  description: The new password.
            example:
              username: jane
              token: 5f4dcc3b5aa765d61d8327deb882cf99
              password: N3w-Pa$$w0rd
      responses:
        "200":
          description: Password changed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      message:
                        type: string
              example:
                data:
                  message: Password reset successfully.
        "422":
          description: A required field is missing, the reset link is invalid or expired, or the new password does not meet the password policy.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 422
                title: Unprocessable Entity
                detail: Invalid or expired reset link.
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /auth/password-policy:
    get:
      operationId: getPasswordPolicy
      tags: [Auth]
      summary: Read the password policy
      description: |
        Return the site's password policy (`system.pwd_regex`, plus the checklist rules from `system.pwd_rules` or parsed from the regex) so setup, reset and user-creation forms can validate as the user types. With no `pwd_regex` set, the API still requires at least 8 characters, so the response reports `min_length: 8` and a single length rule. The same policy is enforced by setup, invite accept, password reset, and user create/update. Public.
      security: []
      responses:
        "200":
          description: Password policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/AuthPasswordPolicy"
              example:
                data:
                  regex: "(?=.*\\d)(?=.*[a-z])(?=.*[A-Z]).{8,}"
                  min_length: 8
                  rules:
                    - { id: length, label: At least 8 characters, pattern: ".{8,}" }
                    - { id: digit, label: At least one number, pattern: "\\d" }
                    - { id: lowercase, label: At least one lowercase letter, pattern: "[a-z]" }
                    - { id: uppercase, label: At least one uppercase letter, pattern: "[A-Z]" }

  /auth/setup:
    get:
      operationId: getSetupStatus
      tags: [Auth]
      summary: Check whether first-run setup is needed
      description: |
        Report whether the site has no user accounts yet, in which case the admin shows its create-the-first-account screen. Also returns the password policy so that screen can validate without a second call. Public.
      security: []
      responses:
        "200":
          description: Setup status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      setup_required:
                        type: boolean
                        description: True while `user/accounts/` holds no accounts.
                      password_policy:
                        $ref: "#/components/schemas/AuthPasswordPolicy"
              example:
                data:
                  setup_required: true
                  password_policy:
                    regex: ""
                    min_length: 8
                    rules:
                      - { id: length, label: At least 8 characters, pattern: ".{8,}" }
    post:
      operationId: completeSetup
      tags: [Auth]
      summary: Create the first super-admin account
      description: |
        Create the site's first account with `api.super` and `site.login` access, then log it in and return a token pair. Only works while no accounts exist; afterwards it returns 409.
        The username must be 3 to 64 characters, may not start with a period or contain `..`, and may not contain `\ / ? * : ; { }` or a line break. The password must match `system.pwd_regex`, or be at least 8 characters when no regex is set.
        Public. Rate limited per IP address, and requires a solved captcha when the site's login captcha covers the setup flow.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [username, password, email]
              properties:
                username:
                  type: string
                  minLength: 3
                  maxLength: 64
                  example: admin
                password:
                  type: string
                  format: password
                email:
                  type: string
                  format: email
                fullname:
                  type: string
                  description: Defaults to the username.
                title:
                  type: string
                  description: Defaults to `Administrator`.
                captcha_token:
                  type: string
                  description: A solved captcha. Required only when the site's login captcha covers the setup flow.
            example:
              username: admin
              password: Pa$$w0rd!
              email: admin@example.com
              fullname: Site Administrator
      responses:
        "200":
          description: Account created and logged in.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/AuthTokenPairWithUser"
        "409":
          description: Setup has already been completed (an account exists).
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 409
                title: Conflict
                detail: Setup has already been completed.
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /auth/invite/{token}:
    parameters:
      - name: token
        in: path
        required: true
        description: The invitation token from the emailed invite link.
        schema:
          type: string
    get:
      operationId: validateInvitation
      tags: [Auth]
      summary: Check an invitation link
      description: |
        Look up an invitation so the accept screen can show who it is for. An expired invitation still returns 200, with `valid: false` and `expired: true`; an unknown token returns 404. Public: the token is the only credential.
      security: []
      responses:
        "200":
          description: Invitation status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      valid:
                        type: boolean
                      expired:
                        type: boolean
                      email:
                        type: string
                        format: email
                        description: The invited address.
                      fullname:
                        type: string
                        description: Name the inviter entered. Omitted when the invitation has expired.
              example:
                data:
                  valid: true
                  expired: false
                  email: jane@example.com
                  fullname: Jane Doe
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      operationId: acceptInvitation
      tags: [Auth]
      summary: Accept an invitation
      description: |
        Create the invited account and log it in. The email address, access and groups come from the invitation and cannot be changed by the request. The username must be 3 to 64 characters, may not start with a period or contain `..`, and may not contain `\ / ? * : ; { }` or a line break; the password must match `system.pwd_regex`, or be at least 8 characters when no regex is set. The invitation is consumed on success, and an expired one is deleted and answered with 410.
        Public: the token is the only credential.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [username, password]
              properties:
                username:
                  type: string
                  minLength: 3
                  maxLength: 64
                password:
                  type: string
                  format: password
                fullname:
                  type: string
                  description: Defaults to the name on the invitation.
                title:
                  type: string
            example:
              username: jane
              password: Pa$$w0rd!
              fullname: Jane Doe
      responses:
        "200":
          description: Account created and logged in. Sends `X-Invalidates` for the users list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/AuthTokenPairWithUser"
        "403":
          description: The account was created, but the invitation's access grants no API or admin login (`api.access`, `admin.login` or super), so no tokens are issued. This is the same check a password login runs.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: An account with that username already exists.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "410":
          description: The invitation has expired.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 410
                title: Gone
                detail: This invitation has expired.
        "422":
          $ref: "#/components/responses/ValidationError"

  /auth/sso/providers:
    get:
      operationId: listSsoProviders
      tags: [Auth]
      summary: List SSO login providers
      description: |
        Return the single sign-on buttons to show on the login screen, collected from every plugin that answers the `onApiLoginProviders` event (for example Login OAuth2). An empty list means password login only. Public.
      security: []
      responses:
        "200":
          description: Providers.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      providers:
                        type: array
                        items:
                          $ref: "#/components/schemas/AuthSsoProvider"
              example:
                data:
                  providers:
                    - id: github
                      label: GitHub
                      icon: fa-github
                      plugin: login-oauth2

  /auth/sso/{provider}/start:
    parameters:
      - name: provider
        in: path
        required: true
        description: Provider id from `GET /auth/sso/providers`.
        schema:
          type: string
        example: github
    get:
      operationId: startSsoLogin
      tags: [Auth]
      summary: Begin an SSO login
      description: |
        Browser navigation, not an XHR call. Stores the provider's CSRF state and the admin's return target in the PHP session, then redirects to the provider's authorization URL built by the plugin that answers `onApiLoginStart`. When no plugin handles the provider, redirects back to the admin's `/login?sso_error=<code>` instead. This route keeps its session cookie so the callback can read the state back. Public.
      security: []
      parameters:
        - name: returnTo
          in: query
          required: false
          description: In-app path to land on after login. Only a root-relative path is kept; absolute URLs, `//host` and control characters are dropped.
          schema:
            type: string
          example: /pages
      responses:
        "302":
          description: Redirect to the provider, or to the admin login screen with an `sso_error` query parameter (default `sso_unavailable`).
          headers:
            Location:
              description: Provider authorization URL, or `<admin>/login?sso_error=<code>`.
              schema:
                type: string
                format: uri

  /auth/sso/{provider}/callback:
    parameters:
      - name: provider
        in: path
        required: true
        description: Provider id.
        schema:
          type: string
        example: github
    get:
      operationId: handleSsoCallback
      tags: [Auth]
      summary: Receive the SSO provider's response
      description: |
        The provider redirects the browser here. The plugin that answers `onApiLoginCallback` validates the state and resolves a Grav user; the same API-access, account-state and 2FA checks as password login then run. The resulting token pair or 2FA challenge is stored under a single-use code (valid for 120 seconds) and the browser is redirected to `<admin>/oauth-callback?code=<code>[&returnTo=<path>]`, where the admin trades it via `POST /auth/sso/exchange`. On failure the browser is sent to `<admin>/login?sso_error=<code>` (`sso_failed`, `sso_forbidden`, or a provider-specific code). Public.
      security: []
      responses:
        "302":
          description: Redirect back into the admin, carrying either a one-time exchange code or an `sso_error`.
          headers:
            Location:
              description: "`<admin>/oauth-callback?code=...` on success, `<admin>/login?sso_error=...` on failure."
              schema:
                type: string
                format: uri

  /auth/sso/exchange:
    post:
      operationId: exchangeSsoCode
      tags: [Auth]
      summary: Trade an SSO code for tokens
      description: |
        Exchange the one-time code from the SSO callback redirect for the stored login result: the same body `POST /auth/token` returns, either a token pair or a `requires_2fa` challenge to finish with `POST /auth/2fa/verify`. The code is deleted on first use. Public.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code]
              properties:
                code:
                  type: string
                  description: Hex code from the `oauth-callback` redirect.
            example:
              code: 9f86d081884c7d659a2feaa0c55ad015a3bf4f1b2b0b822cd15d6c15b0f00a08
      responses:
        "200":
          description: Login result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    oneOf:
                      - $ref: "#/components/schemas/AuthTokenPairWithUser"
                      - $ref: "#/components/schemas/AuthTwoFactorChallenge"
        "401":
          description: The code is unknown, malformed, already used or expired.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 401
                title: Unauthorized
                detail: Invalid or expired exchange code.
        "422":
          $ref: "#/components/responses/ValidationError"

  # ---------------------------------------------------------------------------
  # Current user
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # Ping
  # ---------------------------------------------------------------------------
  /ping:
    get:
      operationId: ping
      tags: [System]
      summary: Health check / keep-alive
      description: |
        Simple health check endpoint. Returns a static response to confirm the
        API is reachable. This is a public route: no credentials are needed
        and no permission is checked.
      security: []
      responses:
        "200":
          description: API is reachable.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      pong:
                        type: boolean
                        example: true
              example:
                data:
                  pong: true
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # Pages
  # ---------------------------------------------------------------------------
  /pages:
    get:
      operationId: listPages
      tags: [Pages]
      summary: List pages
      description: |
        Retrieve a paginated list of all pages. Supports filtering by
        published state, template, routable/visible flags, and parent route.
        Results can be sorted by date, title, slug, modified time, or order.

        The listing uses a lightweight serialization that excludes content,
        media, and children for performance. Every row carries a `permissions`
        object with the caller's effective per-page capabilities; rows are never
        dropped for a page whose own rules deny reading.

        Requires `api.pages.read`.
      parameters:
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/per_page"
        - $ref: "#/components/parameters/lang"
        - name: sort
          in: query
          description: |
            Field to sort by. When omitted the default sort is `date` descending.
            `default` uses native page ordering: combined with `children_of` it
            follows the parent's collection order (`content.order.by` /
            `content.order.dir`, unless `admin.children_display_order` says
            otherwise), or else numbered folders first and unnumbered ones by
            slug; without `children_of` it sorts by `order` ascending. An
            unknown field returns 422.
          schema:
            type: string
            enum: [date, title, slug, modified, order, default]
        - name: order
          in: query
          description: Sort direction. Ignored when `sort` is omitted.
          schema:
            type: string
            enum: [asc, desc]
            default: asc
        - name: published
          in: query
          description: Filter by published state.
          schema:
            type: boolean
        - name: template
          in: query
          description: Filter by page template name (e.g. `blog`, `default`).
          schema:
            type: string
          example: blog
        - name: routable
          in: query
          description: Filter by routable flag.
          schema:
            type: boolean
        - name: visible
          in: query
          description: Filter by visible flag.
          schema:
            type: boolean
        - name: parent
          in: query
          description: |
            Filter pages whose route starts with this parent path (e.g. `/blog`
            returns `/blog/post-1`, `/blog/post-2`, etc.). Returns all descendants.
          schema:
            type: string
        - name: children_of
          in: query
          description: |
            Return only **direct children** of the given route (one level deep).
            Unlike `parent`, this does not return deeper descendants.
            Use `children_of=/` for root-level pages. Ideal for lazy-loading
            tree views and miller column interfaces.
          schema:
            type: string
          example: /blog
        - name: root
          in: query
          description: |
            Filter to top-level (root) pages only: direct children of the
            pages root, resolved from the real hierarchy, so children of the
            home page are not counted as top-level. `root=false` returns
            only pages that are not top-level.
          schema:
            type: boolean
          example: true
        - name: search
          in: query
          description: |
            Free-text search over the page index. Only honored when the Flex
            pages directory is available; ignored on the regular pages
            backend.
          schema:
            type: string
        - name: locate
          in: query
          description: |
            Route of a page to jump to. When the page is in the filtered,
            sorted result set, the server returns the chunk that contains it
            (overriding `page`) and reports its absolute position in
            `meta.pagination.located_at_index`.
          schema:
            type: string
          example: /blog/my-first-post
        - name: translations
          in: query
          description: |
            When `true`, each row also carries `translated_languages`,
            `untranslated_languages`, `has_default_file` and
            `explicit_language_files`.
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: Paginated list of pages.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/PageSummary"
              example:
                data:
                  - route: /blog/my-first-post
                    slug: my-first-post
                    title: My First Post
                    template: blog
                    header:
                      title: My First Post
                      taxonomy:
                        category: [blog]
                        tag: [intro, grav]
                    taxonomy:
                      category: [blog]
                      tag: [intro, grav]
                    published: true
                    visible: true
                    routable: true
                    date: "2026-03-20T12:00:00+00:00"
                    modified: "2026-03-22T09:30:00+00:00"
                    order: 1
                    has_children: true
                meta:
                  pagination:
                    page: 1
                    per_page: 20
                    total: 42
                    total_pages: 3
                links:
                  self: "/api/v1/pages?page=1&per_page=20"
                  next: "/api/v1/pages?page=2&per_page=20"
                  last: "/api/v1/pages?page=3&per_page=20"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

    post:
      operationId: createPage
      tags: [Pages]
      summary: Create a new page
      description: |
        Create a new page at the given route. The parent page must already
        exist (except when creating top-level pages). Supply an optional
        `order` to create an ordered (visible) page, or pass `"auto"` to let
        the server pick the next free prefix based on existing siblings.
        Include `lang` in the body to create the page in a specific language.

        The new frontmatter is built from the template blueprint's
        non-toggleable `default:` values, then `title`, then the supplied
        `header`. A missing parent or an already occupied target returns 422.

        Requires `api.pages.write`, and the parent page's own `create` rule
        (if any) must allow it: a page-level grant can stand in for the
        account permission, a page-level deny refuses it. Setting
        `process.twig: true` additionally needs the Twig-in-content
        permission (403 otherwise).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePageRequest"
            example:
              route: /blog/my-new-post
              title: My New Post
              template: blog
              content: |
                # Hello World

                This is the body of my new blog post.
              header:
                taxonomy:
                  category: [blog]
                  tag: [new, grav]
              order: 3
      responses:
        "201":
          description: Page created successfully.
          headers:
            Location:
              description: URL of the newly created page resource.
              schema:
                type: string
                example: /api/v1/pages/blog/my-new-post
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    description: |
                      The created page. For `kind: folder` the folder may not
                      resolve as a page, in which case only `route` and `kind`
                      are returned.
                    oneOf:
                      - $ref: "#/components/schemas/Page"
                      - type: object
                        required: [route, kind]
                        properties:
                          route:
                            type: string
                            example: /blog/archive
                          kind:
                            type: string
                            enum: [page, folder, module]
                            example: folder
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /pages/{route}:
    get:
      operationId: getPage
      tags: [Pages]
      summary: Get a single page
      description: |
        Retrieve a single page by its route. Returns the full page object
        including raw Markdown content and media. Optionally render the
        content to HTML and/or include child pages.

        The response includes an `ETag` header for use with optimistic
        concurrency on subsequent PATCH requests.

        Requires `api.pages.read`, subject to the page's own `read` rule (a
        page-level grant can stand in for the account permission, a deny
        refuses it). A page that already has `process.twig: true` also needs
        the Twig-in-content permission to be read here (403 otherwise).
      parameters:
        - $ref: "#/components/parameters/pageRoute"
        - $ref: "#/components/parameters/lang"
        - name: summary
          in: query
          description: |
            When `true`, the raw `content` is left out and a plain-text
            `summary` (rendered, tags stripped, truncated) is returned in its
            place.
          schema:
            type: boolean
            default: false
        - name: summary_size
          in: query
          description: Maximum length of `summary` in characters. Only used with `summary=true`.
          schema:
            type: integer
            default: 300
        - name: render
          in: query
          description: When `true`, include the rendered HTML in `content_html`.
          schema:
            type: boolean
            default: false
        - name: children
          in: query
          description: When `true`, include child pages in the response.
          schema:
            type: boolean
            default: false
        - name: children_depth
          in: query
          description: Maximum depth for recursive child inclusion. Only effective when `children=true`.
          schema:
            type: integer
            minimum: 1
            default: 1
        - name: translations
          in: query
          description: |
            When `true`, include `translated_languages`, `untranslated_languages`,
            `has_default_file` and `explicit_language_files` in the response.
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: Page returned successfully.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Page"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

    patch:
      operationId: updatePage
      tags: [Pages]
      summary: Partially update a page
      description: |
        Apply a partial update to an existing page. Only the fields included
        in the request body are modified; all others are left unchanged.

        **Optimistic concurrency:** If the `If-Match` header is provided, it
        is compared against the current ETag. A `409 Conflict` is returned if
        the resource has been modified since the client last fetched it.

        With `?lang=` on a multi-language site the translation must already
        exist; otherwise 422 is returned (create it with
        `POST /pages/{route}/translate` first). Submitted fields are
        validated against the page blueprint (422 on failure).

        Requires `api.pages.write`, subject to the page's own `update` rule.
        Enabling `process.twig`, or editing a page that already has it on,
        also needs the Twig-in-content permission (403 otherwise).
      parameters:
        - $ref: "#/components/parameters/pageRoute"
        - $ref: "#/components/parameters/lang"
        - $ref: "#/components/parameters/ifMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePageRequest"
            example:
              title: Updated Post Title
              content: |
                # Updated Content

                The body has been revised.
              header:
                taxonomy:
                  tag: [updated]
              published: true
      responses:
        "200":
          description: Page updated successfully.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Page"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

    delete:
      operationId: deletePage
      tags: [Pages]
      summary: Delete a page
      description: |
        Delete a page and its directory from disk. By default, child pages
        are deleted as well. Set `children=false` to prevent deletion of a
        page that has children (a 422 error will be returned instead).

        With `?lang=` on a multi-language site only that language's file is
        removed (404 if the page has no such translation). When it is the
        page's only translation the whole page directory is deleted, so
        `children=false` applies just as it does to a plain delete: a page
        with children is refused with a 422.

        Requires `api.pages.write`, subject to the page's own `delete` rule.
      parameters:
        - $ref: "#/components/parameters/pageRoute"
        - $ref: "#/components/parameters/lang"
        - name: children
          in: query
          description: |
            Whether to also delete child pages. Set to `false` to prevent
            accidental deletion of pages with children.
          schema:
            type: boolean
            default: true
      responses:
        "204":
          description: Page deleted successfully. No content returned.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /pages/{route}/move:
    post:
      operationId: movePage
      tags: [Pages]
      summary: Move a page to a new location
      description: |
        Move an existing page to a different parent and/or change its slug or
        ordering prefix. The destination parent must exist and the target
        path must not already be occupied (422 otherwise, as is a move to the
        page's current location). The slug must be a single path segment.

        If the moved page cannot be resolved at its new route afterwards,
        only `route` and `slug` are returned, without an `ETag`.

        Requires `api.pages.write`, subject to the page's own `update` rule
        and the destination parent's `create` rule.
      parameters:
        - $ref: "#/components/parameters/pageRoute"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MovePageRequest"
            example:
              parent: /tutorials
              slug: renamed-post
              order: 5
      responses:
        "200":
          description: |
            Page moved successfully. The `ETag` header is sent only with the
            full page record.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    anyOf:
                      - $ref: "#/components/schemas/Page"
                      - $ref: "#/components/schemas/PageRouteStub"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /pages/{route}/copy:
    post:
      operationId: copyPage
      tags: [Pages]
      summary: Copy a page to a new location
      description: |
        Create a duplicate of an existing page (including all of its files)
        at a new route. The destination parent must exist and the target
        route must not already be occupied (422 otherwise). If the source
        declares a `slug:` in its frontmatter, the copy's is rewritten to the
        new folder name. If the copy cannot be resolved afterwards, only
        `route` and `slug` are returned.

        Requires `api.pages.write`, subject to the source page's `read` rule
        and the destination parent's `create` rule.
      parameters:
        - $ref: "#/components/parameters/pageRoute"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [route]
              properties:
                route:
                  type: string
                  description: The full route for the copied page.
                  example: /blog/my-post-copy
            example:
              route: /blog/my-post-copy
      responses:
        "201":
          description: Page copied successfully.
          headers:
            Location:
              description: URL of the newly created page resource.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    anyOf:
                      - $ref: "#/components/schemas/Page"
                      - $ref: "#/components/schemas/PageRouteStub"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /pages/{route}/languages:
    get:
      operationId: listPageTranslations
      tags: [Pages]
      summary: List translations for a page
      description: |
        Return the list of languages the page has been translated into and
        those still missing. Useful for building translation management UIs.
        On a single-language site both lists are empty.

        Requires `api.pages.read`, subject to the page's own `read` rule.
      parameters:
        - $ref: "#/components/parameters/pageRoute"
      responses:
        "200":
          description: Translation status for the page.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/TranslationInfo"
              example:
                data:
                  route: /blog/my-first-post
                  default_language: en
                  translated:
                    en: /blog/my-first-post
                    fr: /blog/mon-premier-article
                  untranslated:
                    - de
                    - es
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /pages/{route}/translate:
    post:
      operationId: createPageTranslation
      tags: [Pages]
      summary: Create a translation for a page
      description: |
        Create a new language translation for an existing page. The `lang`
        field specifies the target language code; it must be one of the
        site's configured languages, and multi-language must be enabled. If a
        translation already exists for that language, `422` is returned (use
        `PATCH /pages/{route}?lang=` to update it). Fields left out are copied
        from the source page, and the result is validated against the page
        blueprint. The response is the page in the new language.

        Requires `api.pages.write`, subject to the page's own `update` rule.
        Enabling `process.twig` in `header` also needs the Twig-in-content
        permission (403 otherwise).
      parameters:
        - $ref: "#/components/parameters/pageRoute"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [lang]
              properties:
                lang:
                  type: string
                  description: Language code for the translation.
                  example: fr
                title:
                  type: string
                  description: Page title in the target language. Defaults to the source page's title.
                  example: Mon Premier Article
                content:
                  type: string
                  description: Raw Markdown body in the target language. Defaults to the source page's content.
                  example: |
                    # Bonjour le Monde

                    Ceci est le corps de l'article.
                header:
                  type: object
                  additionalProperties: true
                  description: |
                    Frontmatter for the translation. Defaults to the source
                    page's full header; template blueprint defaults fill in
                    anything missing either way.
            example:
              lang: fr
              title: Mon Premier Article
              content: |
                # Bonjour le Monde

                Ceci est le corps de l'article.
      responses:
        "201":
          description: Translation created successfully.
          headers:
            Location:
              description: |
                URL of the page resource. It carries no `lang` query, so add
                `?lang=<code>` to fetch the new translation.
              schema:
                type: string
                example: /api/v1/pages/blog/my-first-post
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Page"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /pages/batch:
    post:
      operationId: batchPages
      tags: [Pages]
      summary: Batch operations on multiple pages
      description: |
        Apply the same operation to multiple pages at once. Supported
        operations are `publish`, `unpublish`, `delete`, and `copy`. Each
        route is processed independently; partial failures are reported in
        the results array.

        The request is rejected with 422 before anything runs when the
        operation is unknown, `routes` is empty, it lists more than
        `batch.max_items` routes (default 50), or any route does not resolve
        to a page. A page whose own rules deny the action (`publish`, falling
        back to `update`, for publish/unpublish; `delete`; `read` for copy)
        is reported as an `error` result rather than failing the batch.

        Requires `api.pages.write`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [operation, routes]
              properties:
                operation:
                  type: string
                  enum: [publish, unpublish, delete, copy]
                  description: The operation to apply to all specified pages.
                  example: publish
                routes:
                  type: array
                  minItems: 1
                  items:
                    type: string
                  description: Array of page routes to operate on.
                  example: [/blog/draft-one, /blog/draft-two]
                options:
                  type: object
                  description: Optional parameters, used by `copy` only.
                  properties:
                    destination:
                      type: string
                      description: |
                        Route of the parent to copy into (`/` for the top
                        level). Defaults to each page's own parent.
                      example: /archive
                    suffix:
                      type: string
                      default: "-copy"
                      description: |
                        Appended to each page's slug to name the copy. Must be
                        a single path segment.
                      example: "-copy"
            example:
              operation: publish
              routes:
                - /blog/draft-one
                - /blog/draft-two
      responses:
        "200":
          description: Batch operation completed. Check individual results for status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    required: [operation, results, total, successful, failed]
                    properties:
                      operation:
                        type: string
                        enum: [publish, unpublish, delete, copy]
                        example: publish
                      results:
                        type: array
                        items:
                          $ref: "#/components/schemas/BatchResult"
                      total:
                        type: integer
                        description: Number of routes processed.
                        example: 2
                      successful:
                        type: integer
                        example: 1
                      failed:
                        type: integer
                        example: 1
              example:
                data:
                  operation: publish
                  results:
                    - route: /blog/draft-one
                      status: success
                    - route: /blog/draft-two
                      status: error
                      message: Page permissions deny 'publish' on this page.
                  total: 2
                  successful: 1
                  failed: 1
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /pages/reorganize:
    post:
      operationId: reorganizePages
      tags: [Pages]
      summary: Reorganize multiple pages (move and reorder atomically)
      description: |
        Move and/or reorder multiple pages in a single atomic operation. Each
        operation specifies a page route and optionally a new parent and/or
        position. All operations are validated before any changes are applied.

        Use this endpoint for drag-and-drop tree reorganization where pages may
        be moved across parents and reordered within the same request.

        Validation failures return 422 and change nothing: more than
        `batch.max_items` operations (default 50), a duplicate route, a route
        or parent that does not resolve, two operations claiming the same
        position under one parent, a move into the page's own subtree, or a
        target parent that is itself being moved in the same request. A
        filesystem failure part-way is rolled back on a best-effort basis and
        also returns 422.

        The response lists every child of each affected parent (old and new),
        not only the pages named in the request.

        Requires `api.pages.write`. A page whose own rules deny `update`, or
        a destination parent that denies `create`, fails the whole request
        with 403.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [operations]
              properties:
                operations:
                  type: array
                  items:
                    type: object
                    required: [route]
                    properties:
                      route:
                        type: string
                        description: Current route of the page to reorganize.
                        example: /blog/post-a
                      parent:
                        type: string
                        description: |
                          New parent route. Use `/` for root. Omit to keep the
                          page under its current parent.
                        example: /tutorials
                      position:
                        type: integer
                        minimum: 1
                        description: |
                          1-based ordering position under the target parent. Omit
                          to keep the current order value.
                        example: 1
                  description: |
                    Array of reorganize operations to apply atomically. All are
                    validated before any filesystem changes are made.
            example:
              operations:
                - route: /blog/post-a
                  parent: /tutorials
                  position: 1
                - route: /blog/post-b
                  position: 2
                - route: /tutorials/old-guide
                  parent: /blog
                  position: 3
      responses:
        "200":
          description: Pages reorganized successfully.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        route:
                          type: string
                          example: /tutorials/post-a
                        slug:
                          type: string
                          example: post-a
                        title:
                          type: string
                          example: Post A
                        order:
                          $ref: "#/components/schemas/PageOrder"
                        parent:
                          type: string
                          description: |
                            Structural route of the parent the row was listed
                            under (`/` for the top level).
                          example: /tutorials
              example:
                data:
                  - route: /tutorials/post-a
                    slug: post-a
                    title: Post A
                    order: 1
                    parent: /tutorials
                  - route: /blog/post-b
                    slug: post-b
                    title: Post B
                    order: 2
                    parent: /blog
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /pages/{route}/reorder:
    post:
      operationId: reorderPages
      tags: [Pages]
      summary: Reorder children of a page
      description: |
        Set the ordering of child pages under the specified parent route.
        The slugs in `order` get numeric folder prefixes 1, 2, 3… in the
        order given (keeping the widest prefix width already used among the
        siblings). Every slug must be an immediate child of the page (422
        otherwise). Children not listed keep their current folder name and
        prefix.

        Requires `api.pages.write`, subject to the parent page's own
        `update` rule.
      parameters:
        - $ref: "#/components/parameters/pageRoute"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [order]
              properties:
                order:
                  type: array
                  items:
                    type: string
                  description: Ordered list of child slugs.
                  example: [third-post, first-post, second-post]
            example:
              order:
                - third-post
                - first-post
                - second-post
      responses:
        "200":
          description: |
            Children reordered successfully. Returns every child of the page
            in its new order.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      required: [route, slug, title, order]
                      properties:
                        route:
                          type: string
                          example: /blog/third-post
                        slug:
                          type: string
                          example: third-post
                        title:
                          type: string
                          example: Third Post
                        order:
                          $ref: "#/components/schemas/PageOrder"
              example:
                data:
                  - route: /blog/third-post
                    slug: third-post
                    title: Third Post
                    order: "01."
                  - route: /blog/first-post
                    slug: first-post
                    title: First Post
                    order: "02."
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # Page Media
  # ---------------------------------------------------------------------------
  /pages/{route}/media:
    get:
      operationId: listPageMedia
      tags: [Media]
      summary: List media for a page
      description: |
        Return all media files attached to the specified page, optionally
        filtered and sorted by their `.meta.yaml` metadata. Only the fields
        configured in `media_metadata.fields` can be filtered or sorted on.

        Requires `api.media.read`; a page whose own rules deny `read` returns
        403.
      parameters:
        - $ref: "#/components/parameters/pageRoute"
        - name: filter
          in: query
          description: |
            Metadata filter clause, `field:value` or `field:operator:value`.
            Repeat as `filter[]=` for several clauses (at most 10), which are
            combined with AND. Operators: `==`, `!=`, `>`, `>=`, `<`, `<=`,
            `in` (comma-separated values), `contains`. The shorthand form uses
            `contains` for a `tags` field and `==` otherwise; a `tags` field
            accepts only `in` and `contains`. A clause naming an unconfigured
            field is ignored; a malformed clause or bad operator returns 422.
          schema:
            type: string
          example: tags:contains:beach
        - name: sort
          in: query
          description: |
            Sort by a configured metadata field, or by `filename`, `size` or
            `modified`. Any other value returns 422.
          schema:
            type: string
          example: title
        - name: order
          in: query
          description: Sort direction for `sort`. `dir` is accepted as an alias.
          schema:
            type: string
            enum: [asc, desc]
            default: asc
      responses:
        "200":
          description: List of media items for the page.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/MediaItem"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

    post:
      operationId: uploadPageMedia
      tags: [Media]
      summary: Upload media to a page
      description: |
        Upload one or more files to the specified page's directory. Files are
        sent as `multipart/form-data`. The maximum upload size per file is
        64 MB. Dangerous file extensions (as configured in Grav's security
        settings) are rejected, checked against every dot-separated part of
        the name, and SVG content is sanitized on arrival.

        Requires `api.media.write`; a page whose own rules deny `update`
        returns 403.
      parameters:
        - $ref: "#/components/parameters/pageRoute"
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: |
                    File to upload. Any field name works, including nested
                    array names, and several file parts may be sent in one
                    request.
                files:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Alternative field name for uploading multiple files.
                random_name:
                  type: boolean
                  description: Replace the file name with a random one (extension kept).
                  default: false
                avoid_overwriting:
                  type: boolean
                  description: Add a prefix instead of replacing an existing file of the same name.
                  default: false
                accept:
                  type: string
                  description: Comma-separated allowlist of extensions (`.pdf`, `*.jpg`) or MIME patterns (`image/*`). May also be sent as an array.
                  example: "image/*,.pdf"
                filesize:
                  type: number
                  description: Per-field maximum size in MB. Can only tighten the 64 MB cap.
                  example: 5
      responses:
        "201":
          description: |
            Files uploaded successfully. The response lists all of the page's
            media after the upload, not only the new files.
          headers:
            Location:
              description: URL of the page media collection.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/MediaItem"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /pages/{route}/media/{filename}:
    delete:
      operationId: deletePageMedia
      tags: [Media]
      summary: Delete a media file from a page
      description: |
        Remove a single media file (and its `.meta.yaml` sidecar, if present)
        from the specified page. Retina variants of the file (`name@2x.jpg`,
        `name@3x.jpg`, …) and their sidecars are removed too, so an image
        stored only as a retina variant can still be deleted by its base
        name.

        Requires `api.media.write`; a page whose own rules deny `update`
        returns 403.
      parameters:
        - $ref: "#/components/parameters/pageRoute"
        - name: filename
          in: path
          required: true
          description: Name of the media file to delete.
          schema:
            type: string
          example: hero-image.jpg
      responses:
        "204":
          description: Media file deleted successfully. No content returned.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /pages/{route}/media/{filename}/meta:
    get:
      operationId: getPageMediaMeta
      tags: [Media]
      summary: Read a page media file's metadata
      description: |
        Return the editable metadata fields (from the `<filename>.meta.yaml`
        sidecar) for a page media file, together with any other keys stored in
        the sidecar (EXIF, dimensions, upload info) as read-only `extra` data.

        Requires `api.media.read`; a page whose own rules deny `read` returns
        403. An invalid filename (leading dot, `..`) returns 422.
      parameters:
        - $ref: "#/components/parameters/pageRoute"
        - $ref: "#/components/parameters/mediaFilename"
      responses:
        "200":
          description: Media metadata.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/MediaMetadata"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationError" }
        "429": { $ref: "#/components/responses/TooManyRequests" }
    patch:
      operationId: savePageMediaMeta
      tags: [Media]
      summary: Save a page media file's metadata
      description: |
        Write the editable metadata fields for a page media file. Only the
        configured fields present in the request are changed (an empty string
        clears a field); every other key already in the sidecar is preserved.
        Values are stripped of HTML tags and control characters on save, since
        they render into `<img>` attributes on the site.

        Requires `api.media.write`; a page whose own rules deny `update`
        returns 403.
      parameters:
        - $ref: "#/components/parameters/pageRoute"
        - $ref: "#/components/parameters/mediaFilename"
      requestBody:
        $ref: "#/components/requestBodies/MediaMetadataWrite"
      responses:
        "200":
          description: Updated media metadata.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/MediaMetadata"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationError" }
        "429": { $ref: "#/components/responses/TooManyRequests" }
    delete:
      operationId: deletePageMediaMeta
      tags: [Media]
      summary: Clear a page media file's metadata
      description: |
        Remove all editable (configured) metadata fields for a page media file.
        Reserved/technical keys are preserved; the sidecar is deleted only if
        nothing else remains in it.

        Requires `api.media.write`; a page whose own rules deny `update`
        returns 403.
      parameters:
        - $ref: "#/components/parameters/pageRoute"
        - $ref: "#/components/parameters/mediaFilename"
      responses:
        "204":
          description: Metadata cleared. No content returned.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationError" }
        "429": { $ref: "#/components/responses/TooManyRequests" }

  /pages/{route}/compare:
    get:
      operationId: comparePageLanguages
      tags: [Pages]
      summary: Compare two language versions of a page
      description: |
        Return the title, raw Markdown, frontmatter and modified date of a page in two languages side by side. A side is `null` when the page does not resolve in that language, and `exists` is false when the page resolves only through the default-language fallback rather than its own file. Needs multi-language enabled (422 otherwise). Requires `api.pages.read`, and neither the source nor the target page may deny the caller read access. A language code that is not a string (for example `source[]=`) is a 422.
      parameters:
        - $ref: "#/components/parameters/pageRoute"
        - name: source
          in: query
          required: true
          description: Source language code.
          schema:
            type: string
          example: en
        - name: target
          in: query
          required: true
          description: Target language code.
          schema:
            type: string
          example: fr
      responses:
        "200":
          description: Both language versions.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      route:
                        type: string
                        example: /blog/my-first-post
                      source:
                        type: object
                        allOf:
                          - $ref: "#/components/schemas/PageLanguageVersion"
                        nullable: true
                      target:
                        type: object
                        allOf:
                          - $ref: "#/components/schemas/PageLanguageVersion"
                        nullable: true
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /pages/{route}/sync:
    post:
      operationId: syncPageTranslation
      tags: [Pages]
      summary: Overwrite a translation from another language
      description: |
        Copy the frontmatter and Markdown of the page in `source_lang` over its existing `target_lang` translation. The target translation file must already exist (create it with `POST /pages/{route}/translate`). The same Twig-content and validation checks as a normal page save apply. Needs multi-language enabled. A language code that is not a string is a 422. Requires `api.pages.write` and update access to the page.
      parameters:
        - $ref: "#/components/parameters/pageRoute"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [source_lang, target_lang]
              properties:
                source_lang:
                  type: string
                  example: en
                target_lang:
                  type: string
                  example: fr
            example:
              source_lang: en
              target_lang: fr
      responses:
        "200":
          description: Target translation overwritten. Returns the updated page.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Page"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /pages/{route}/adopt-language:
    post:
      operationId: adoptPageLanguage
      tags: [Pages]
      summary: Assign an untyped page file to a language
      description: |
        Rename a page's language-less file (for example `default.md`) to the language-specific name (`default.fr.md`) without touching its contents. Meant for sites that started single-language and later enabled multi-language. Fails with 422 when multi-language is off, when there is no language-less file, or when a file for that language already exists. Requires `api.pages.write` and update access to the page.
      parameters:
        - $ref: "#/components/parameters/pageRoute"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [lang]
              properties:
                lang:
                  type: string
                  description: Language code to assign the file to.
                  example: en
            example:
              lang: en
      responses:
        "200":
          description: File renamed. Returns the page as loaded in the adopted language.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Page"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "500":
          description: The file could not be renamed on disk.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /pages/{route}/preview-token:
    post:
      operationId: createPagePreviewToken
      tags: [Pages]
      summary: Get a draft preview token
      description: |
        Mint a short-lived signed token that lets the frontend render this page even when it is unpublished. Append it to the page's frontend URL as `?admin_preview=1&preview_token=<token>`; the token unlocks only this page. For a modular section, `route` is the parent page to load and `anchor` suggests where to scroll. Lifetime is `plugins.api.preview_token_ttl` seconds (default 300, minimum 30). Returns 403 when `plugins.api.allow_draft_preview` is off. Requires `api.pages.read` and read access to the page (and to its parent for a module).
      parameters:
        - $ref: "#/components/parameters/pageRoute"
        - $ref: "#/components/parameters/lang"
      responses:
        "200":
          description: Preview token.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      token:
                        type: string
                      expires_in:
                        type: integer
                        description: Lifetime in seconds.
                      route:
                        type: string
                        description: Frontend route the browser should load.
                      anchor:
                        type: string
                        nullable: true
                        description: Suggested element id to scroll to when previewing a module inside its parent; null otherwise.
              example:
                data:
                  token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
                  expires_in: 300
                  route: /blog/my-first-post
                  anchor: null
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # Users list extensions and 2FA
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # Site-level Media
  # ---------------------------------------------------------------------------
  /media/meta:
    get:
      operationId: getSiteMediaMeta
      tags: [Media]
      summary: Read a site media file's metadata
      description: |
        Return the editable metadata fields for a site-level media file,
        addressed by the `path` query parameter (site paths contain slashes).
        Requires `api.media.read`. A missing or invalid `path` returns 422.
      parameters:
        - $ref: "#/components/parameters/mediaMetaPath"
      responses:
        "200":
          description: Media metadata.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/MediaMetadata"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationError" }
        "429": { $ref: "#/components/responses/TooManyRequests" }
    patch:
      operationId: saveSiteMediaMeta
      tags: [Media]
      summary: Save a site media file's metadata
      description: |
        Write the editable metadata fields for a site-level media file. Only the
        configured fields present in the request are changed; other keys in the
        sidecar are preserved. Values are sanitized on save. Requires
        `api.media.write`.
      parameters:
        - $ref: "#/components/parameters/mediaMetaPath"
      requestBody:
        $ref: "#/components/requestBodies/MediaMetadataWrite"
      responses:
        "200":
          description: Updated media metadata.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/MediaMetadata"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationError" }
        "429": { $ref: "#/components/responses/TooManyRequests" }
    delete:
      operationId: deleteSiteMediaMeta
      tags: [Media]
      summary: Clear a site media file's metadata
      description: |
        Remove all editable metadata fields for a site-level media file.
        Reserved/technical keys are preserved; the sidecar is deleted only if
        nothing else remains. Requires `api.media.write`.
      parameters:
        - $ref: "#/components/parameters/mediaMetaPath"
      responses:
        "204":
          description: Metadata cleared. No content returned.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationError" }
        "429": { $ref: "#/components/responses/TooManyRequests" }

  /media:
    get:
      operationId: listSiteMedia
      tags: [Media]
      summary: List site-level media
      description: |
        Return a paginated list of files in one folder of the site-level media
        directory (`user/media`), plus that folder's subfolders in
        `meta.folders`. Requires `api.media.read`.

        Files follow the folder's saved manual order (`POST /media/order`)
        when one exists, otherwise natural filename order. A folder that does
        not exist returns an empty list. `search` switches to a recursive
        filename search across every folder and cannot be combined with
        `filter` or `sort` (422).
      parameters:
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/per_page"
        - name: path
          in: query
          description: Subfolder path relative to the media root directory.
          schema:
            type: string
          example: blog/2026
        - name: search
          in: query
          description: Case-insensitive search term for recursive filename matching across all subfolders.
          schema:
            type: string
          example: hero
        - name: type
          in: query
          description: Filter by media type category, from the file's detected MIME type. `document` means anything that is not image, video or audio.
          schema:
            type: string
            enum: [image, video, audio, document]
          example: image
        - name: filter
          in: query
          description: |
            Metadata filter clause, `field:value` or `field:operator:value`,
            on a field from the configured `media_metadata.fields`. Repeatable
            as `filter[]=`, up to 10 clauses. Operators: `==`, `!=`, `>`,
            `>=`, `<`, `<=`, `in` (comma-separated values), `contains`. A
            `tags` field accepts only `in` and `contains` (the shorthand uses
            `contains`). Unknown fields are ignored; a malformed clause or
            unsupported operator returns 422.
          schema:
            type: string
          example: tags:contains:hero
        - name: sort
          in: query
          description: |
            Sort by a configured metadata field, or by `filename`, `size` or
            `modified`. Overrides the folder's manual order. An unknown field
            returns 422.
          schema:
            type: string
          example: title
        - name: order
          in: query
          description: Sort direction for `sort`. `dir` is accepted as an alias.
          schema:
            type: string
            enum: [asc, desc]
            default: asc
      responses:
        "200":
          description: Paginated list of site media files.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/MediaItem"
                      meta:
                        type: object
                        properties:
                          path:
                            type: string
                            description: The listed folder, relative to the media root. Empty for the root and in search mode.
                          folders:
                            type: array
                            description: Immediate subfolders of the listed folder. Empty in search mode.
                            items:
                              $ref: "#/components/schemas/FolderInfo"
                          ordered:
                            type: boolean
                            description: Whether the folder has a saved manual order. Absent in search mode and for a missing folder.
                          search:
                            type: string
                            description: The search term. Only present in search mode.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

    post:
      operationId: uploadSiteMedia
      tags: [Media]
      summary: Upload site-level media
      description: |
        Upload one or more files to the site-level media directory
        (`user/media`). Requires `api.media.write`. The maximum upload size per
        file is 64 MB. Files must have an extension, and no dot-separated part
        of the name may be on `security.uploads_dangerous_extensions`; SVG
        content is sanitized on save. Any 422 aborts the request at that file,
        and files already saved before it are kept.

        The optional `random_name`, `avoid_overwriting`, `accept` and
        `filesize` form fields apply a blueprint file field's upload settings
        on top of those checks. Without `avoid_overwriting`, a file with the
        same name is replaced.
      parameters:
        - name: path
          in: query
          description: Subfolder path to upload into. Created automatically if it does not exist.
          schema:
            type: string
          example: blog/2026
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: File to upload. Any field name works (nested names such as `files[]` included), and several file parts may be sent in one request.
                files:
                  type: array
                  items:
                    type: string
                    format: binary
                  description: Alternative field name for uploading multiple files.
                random_name:
                  type: boolean
                  description: Replace the file name with a random one (extension kept).
                  default: false
                avoid_overwriting:
                  type: boolean
                  description: Add a suffix instead of replacing an existing file of the same name.
                  default: false
                accept:
                  type: string
                  description: Comma-separated allowlist of extensions (`.pdf`, `*.jpg`) or MIME patterns (`image/*`). May also be sent as an array.
                  example: "image/*,.pdf"
                filesize:
                  type: number
                  description: Per-field maximum size in MB. Can only tighten the 64 MB cap.
                  example: 5
      responses:
        "201":
          description: Files uploaded successfully.
          headers:
            Location:
              description: URL of the site media collection.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/MediaItem"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /media/{filename}:
    delete:
      operationId: deleteSiteMedia
      tags: [Media]
      summary: Delete a site-level media file
      description: |
        Remove a file (and its `.meta.yaml` sidecar, if present) from the
        site-level media directory, and drop it from the folder's manual
        order. Requires `api.media.write`. A path segment that is empty,
        starts with a period or contains `..` returns 422.
      parameters:
        - name: filename
          in: path
          required: true
          description: Name of the file to delete (may include subdirectory path).
          schema:
            type: string
          example: hero-banner.jpg
      responses:
        "204":
          description: File deleted successfully. No content returned.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /media/folders:
    post:
      operationId: createMediaFolder
      tags: [Media]
      summary: Create a media folder
      description: |
        Create a new folder in the site-level media directory (`user/media`).
        Parent directories are created automatically if they do not exist.
        Requires `api.media.write`. Returns 422 when the folder already
        exists or the path is invalid.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [path]
              properties:
                path:
                  type: string
                  description: Folder path to create, relative to the media root.
                  example: blog/2026/april
      responses:
        "201":
          description: Folder created successfully.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/FolderInfo"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /media/folders/{path}:
    delete:
      operationId: deleteMediaFolder
      tags: [Media]
      summary: Delete an empty media folder
      description: |
        Remove an empty folder from the site-level media directory. The folder
        must be empty (contain no files or subdirectories, hidden files and
        sidecars included); otherwise 422. Requires `api.media.write`.
      parameters:
        - name: path
          in: path
          required: true
          description: Folder path relative to the media root.
          schema:
            type: string
          example: blog/2026
      responses:
        "204":
          description: Folder deleted successfully. No content returned.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /media/rename:
    post:
      operationId: renameSiteMedia
      tags: [Media]
      summary: Rename or move a media file
      description: |
        Rename a file or move it to a different folder within the site-level
        media directory. The destination directory is created automatically
        if it does not exist. Any `.meta.yaml` sidecar is also moved, and the
        folders' manual orders are kept in step. Requires `api.media.write`.

        The new name is cleaned: whitespace becomes a dash, characters other
        than letters, digits, `.`, `_` and `-` are dropped, and the source
        file's extension is always kept (any extension given in `to` is
        replaced). If the cleaned name equals the source, nothing changes and
        the file is returned as is. Returns 422 when a file already exists at
        the destination or the cleaned name is empty.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [from, to]
              properties:
                from:
                  type: string
                  description: Current file path relative to the media root.
                  example: blog/hero.jpg
                to:
                  type: string
                  description: New file path relative to the media root.
                  example: blog/banner.jpg
      responses:
        "200":
          description: File renamed successfully.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/MediaItem"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /media/folders/rename:
    post:
      operationId: renameMediaFolder
      tags: [Media]
      summary: Rename a media folder
      description: |
        Rename a folder within the site-level media directory. All contents
        of the folder are preserved. Requires `api.media.write`. Returns 422
        when something already exists at the destination. The returned
        `children_count` and `file_count` are the renamed folder's real
        counts, matching the folder listing.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [from, to]
              properties:
                from:
                  type: string
                  description: Current folder path relative to the media root.
                  example: blog/old-name
                to:
                  type: string
                  description: New folder path relative to the media root.
                  example: blog/new-name
      responses:
        "200":
          description: Folder renamed successfully.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/FolderInfo"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /media/raw/{path}:
    get:
      operationId: getRawSiteMedia
      tags: [Media]
      summary: Download a site media file
      description: |
        Stream a file from the site media folder. Used for files the web server will not serve directly; everything else keeps its static URL. Only images, video, audio, fonts and PDFs are served; any other type returns 404 so the route cannot be used to read source files. Responses carry a sandboxing `Content-Security-Policy` so an uploaded SVG cannot run script when opened directly. Works as an `<img src>` because the API accepts the site's session cookie. Requires `api.media.read`.
      parameters:
        - name: path
          in: path
          required: true
          description: Path relative to the site media root. May contain `/`.
          schema:
            type: string
          example: photos/beach.jpg
      responses:
        "200":
          description: The file bytes, with its detected MIME type.
          headers:
            Content-Disposition:
              description: "`inline; filename=\"<basename>\"`"
              schema:
                type: string
            Content-Security-Policy:
              schema:
                type: string
                example: "default-src 'none'; style-src 'unsafe-inline'; sandbox"
            Cache-Control:
              schema:
                type: string
                example: private, max-age=600
          content:
            image/*:
              schema:
                type: string
                format: binary
            video/*:
              schema:
                type: string
                format: binary
            audio/*:
              schema:
                type: string
                format: binary
            font/*:
              schema:
                type: string
                format: binary
            application/pdf:
              schema:
                type: string
                format: binary
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /media/batch/meta:
    post:
      operationId: batchUpdateSiteMediaMeta
      tags: [Media]
      summary: Apply metadata to several site media files
      description: |
        Write the same metadata fields to many site media files at once. Only the configured fields present in `fields` change; each file keeps its other values. Every file is handled on its own, so one failure is reported in `results` without stopping the rest. The number of files is capped by `plugins.api.batch.max_items` (default 50). Requires `api.media.write`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [files, fields]
              properties:
                files:
                  type: array
                  minItems: 1
                  description: Paths relative to the site media root.
                  items:
                    type: string
                fields:
                  type: object
                  additionalProperties:
                    oneOf:
                      - type: string
                      - type: array
                        items:
                          type: string
                  description: Metadata field key to value. An empty string (or empty array for a `tags` field) clears that field.
            example:
              files: [photos/beach.jpg, photos/dunes.jpg]
              fields:
                tags: [beach, summer]
      responses:
        "200":
          description: Per-file results.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      results:
                        type: array
                        items:
                          type: object
                          properties:
                            path:
                              type: string
                            status:
                              type: string
                              enum: [success, error]
                            message:
                              type: string
                              description: Present only when `status` is `error`.
                          required: [path, status]
                      total:
                        type: integer
                      successful:
                        type: integer
                      failed:
                        type: integer
              example:
                data:
                  results:
                    - { path: photos/beach.jpg, status: success }
                    - { path: photos/missing.jpg, status: error, message: Media file not found. }
                  total: 2
                  successful: 1
                  failed: 1
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /media/order:
    post:
      operationId: setSiteMediaOrder
      tags: [Media]
      summary: Save the manual file order of a media folder
      description: |
        Store a manual ordering for the files in a site media folder. The order is written to a `media_order.yaml` file in that folder and applied when the folder is listed. Entries are reduced to base filenames. Requires `api.media.write`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [order]
              properties:
                path:
                  type: string
                  description: Folder relative to the site media root. Omit for the root folder.
                  example: photos
                order:
                  type: array
                  description: Filenames in the desired order.
                  items:
                    type: string
            example:
              path: photos
              order: [dunes.jpg, beach.jpg, sunset.jpg]
      responses:
        "204":
          description: Order saved. No content returned.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # Menubar
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # Taxonomy
  # ---------------------------------------------------------------------------
  /taxonomy:
    get:
      operationId: listTaxonomy
      tags: [Pages]
      summary: List all taxonomy types and values
      description: |
        Return every taxonomy type (e.g. `category`, `tag`) and the set of
        values currently in use across all pages. Every type declared in
        `site.taxonomies` is included, with an empty list when no page uses
        it yet.

        Requires `api.pages.read`.
      responses:
        "200":
          description: Taxonomy map returned successfully.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    description: Map of taxonomy type to the values in use for it.
                    additionalProperties:
                      type: array
                      items:
                        type: string
                example:
                  data:
                    category: [blog, tutorials]
                    tag: [grav, php]
                    author: []
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # Config
  # ---------------------------------------------------------------------------
  /config/{scope}:
    get:
      operationId: getConfig
      tags: [Config]
      summary: Read configuration for a scope
      description: |
        Retrieve the current configuration for the given scope. Supported
        scopes are:

        | Scope | Maps to |
        |---|---|
        | `system` | `system` config |
        | `site` | `site` config |
        | `plugins/{name}` | `plugins.{name}` config |
        | `themes/{name}` | `themes.{name}` config |
        | `media`, `security` | the matching core config |
        | `scheduler`, `backups` | tool-managed core config, API super users only (403 otherwise) |
        | `<custom>` | a site-authored `user/config/<custom>.yaml` |

        Requires `api.config.read`. An unknown scope returns 404.

        The body is the effective configuration for the target layer
        selected by `X-Config-Environment` (see below), recomputed from the
        YAML files, with secret values (passwords, API keys, tokens) masked.
        `meta` lists the leaf paths the target layer overrides and the value
        each would fall back to.

        The response includes an `ETag` header for optimistic concurrency on
        subsequent PATCH requests. It is computed from the persisted
        overrides, not the full body.
      parameters:
        - $ref: "#/components/parameters/configScope"
        - name: X-Config-Environment
          in: header
          required: false
          description: |
            Environment layer to read. Empty, `default` or `base` targets the
            base `user/config`; when absent, the active environment is used if
            it has a config folder. An invalid name returns 422.
          schema:
            type: string
          example: localhost
      responses:
        "200":
          description: Configuration returned successfully.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ConfigObject"
                  meta:
                    type: object
                    properties:
                      overrides:
                        type: array
                        description: Dotted leaf paths the target layer overrides.
                        items:
                          type: string
                        example: [pages.theme]
                      fallback:
                        type: object
                        additionalProperties: true
                        description: For each override, the value it would revert to.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

    patch:
      operationId: updateConfig
      tags: [Config]
      summary: Partially update configuration
      description: |
        Deep-merge the provided values into the existing configuration for the
        given scope. The merged result is persisted to the corresponding YAML
        file and the config cache is cleared. Only the values that differ from
        the parent layer are written, and values supplied through
        `GRAV_CONFIG__*` environment variables are never persisted.

        Requires `api.config.write`. Writes to `system`, `security` and
        `plugins/api` also need an API super user, and the tool-managed
        `scheduler` and `backups` scopes are super-only for reading and
        writing (403 otherwise). An empty body, or a changed field that fails
        blueprint validation, returns 422. Masked secrets sent back unchanged
        keep their stored value.

        Send the current `ETag` in `If-Match` to detect concurrent edits.
        The response has the same structure as `GET /config/{scope}` and
        carries an `X-Invalidates` header (`config:update:<scope>`, plus
        `plugins:update:<name>` and `plugins:list` for plugin scopes).
      parameters:
        - $ref: "#/components/parameters/configScope"
        - $ref: "#/components/parameters/ifMatch"
        - name: X-Config-Environment
          in: header
          required: false
          description: |
            Environment layer to write. Empty, `default` or `base` targets the
            base `user/config`; when absent, the active environment is used if
            it has a config folder. A name that is invalid or has no folder
            returns 422.
          schema:
            type: string
          example: localhost
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConfigObject"
            example:
              pages:
                theme: quark
              debugger:
                enabled: false
      responses:
        "200":
          description: Configuration updated successfully.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
            X-Invalidates:
              description: Client cache tags to evict.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ConfigObject"
                  meta:
                    type: object
                    properties:
                      overrides:
                        type: array
                        description: Dotted leaf paths the target layer overrides after the save.
                        items:
                          type: string
                      fallback:
                        type: object
                        additionalProperties: true
                        description: For each override, the value it would revert to.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /config:
    get:
      operationId: listConfigScopes
      tags: [Config]
      summary: List configuration sections
      description: |
        Returns the configuration scopes the admin shows as tabs. Requires `api.config.read`.
        The list always starts with `system` and `site` and ends with `info` (a client-side system-information tab, not a readable scope). In between, sorted by name, come the core scopes `media` and `security` plus any site- or extension-authored custom scopes. `backups` is included for super-admins only. `scheduler` is never listed.
      responses:
        "200":
          description: Ordered list of scope names.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: string
                    example: [system, site, backups, media, security, info]
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /config/accounts:
    get:
      operationId: getAccountsConfig
      tags: [Config]
      summary: Read the accounts configuration
      description: |
        Returns the Flex accounts configuration (`flex.accounts`, stored in `user/config/flex/accounts.yaml`), the Users "Configuration" tab in classic admin. Requires super-admin (`admin.super`). The response includes an `ETag` for use with `If-Match` on PATCH.
      responses:
        "200":
          description: Accounts configuration.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ConfigObject"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"
    patch:
      operationId: updateAccountsConfig
      tags: [Config]
      summary: Update the accounts configuration
      description: |
        Deep-merges the body into the accounts configuration and writes it to the base `user/config/flex/accounts.yaml` (never an environment overlay). Nested maps merge; lists are replaced whole. Values injected through `GRAV_CONFIG__*` environment variables are not written to disk. Requires super-admin (`admin.super`).
        Send the current `ETag` in `If-Match` to detect concurrent edits. Fires `onApiConfigUpdated` with scope `flex/accounts`.
      parameters:
        - $ref: "#/components/parameters/ifMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConfigObject"
      responses:
        "200":
          description: Updated accounts configuration.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
            X-Invalidates:
              description: Client cache tags to evict (`config:update:flex/accounts`).
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ConfigObject"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /config/{scope}/revert:
    post:
      operationId: revertConfig
      tags: [Config]
      summary: Revert configuration overrides
      description: |
        Drops overrides from the active configuration layer so the values fall back to the parent layer (defaults for a base write, defaults plus base for an environment write). Requires `api.config.write`. Writes to `system`, `security` and `plugins/api` also need an API super user, and the tool-managed `scheduler` and `backups` scopes are super-only for reading and writing.
        Send `keys` (dotted paths) to remove individual overrides, or `reset: true` to delete the layer's file for that scope entirely. The target layer follows the same rules as `PATCH /config/{scope}`: the `X-Config-Environment` header picks it, an empty value (or `default` / `base`) means the base `user/config`, and when absent the active environment is used if it has a config folder.
        The response is the effective configuration after the revert, with secrets masked, and `meta` lists the paths that still override the parent layer. Reverting something the layer does not override (for example `reset: true` when the layer has no file) is still a 200, but `meta.reverted` is false and no `onApiConfigUpdated` event (so no `config.updated` webhook) fires.
      parameters:
        - $ref: "#/components/parameters/configScope"
        - $ref: "#/components/parameters/ifMatch"
        - name: X-Config-Environment
          in: header
          required: false
          description: Environment layer to revert. Empty, `default` or `base` targets the base `user/config`.
          schema:
            type: string
          example: localhost
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: Provide a non-empty `keys` array, or `reset` set to true.
              properties:
                keys:
                  type: array
                  description: Dotted paths of the overrides to drop.
                  items:
                    type: string
                  example: [pages.theme, debugger.enabled]
                reset:
                  type: boolean
                  description: Delete the layer's file for this scope, dropping every override at once. Takes precedence over `keys`.
                  default: false
      responses:
        "200":
          description: Effective configuration after the revert.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
            X-Invalidates:
              description: Client cache tags to evict.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/ConfigObject"
                  meta:
                    type: object
                    properties:
                      overrides:
                        type: array
                        description: Dotted leaf paths the active layer still overrides.
                        items:
                          type: string
                        example: [pages.markdown.extra]
                      fallback:
                        type: object
                        additionalProperties: true
                        description: For each remaining override, the value it would revert to.
                      reverted:
                        type: boolean
                        description: Whether anything on disk changed. False when the layer did not override what was asked.
                        example: true
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # Groups
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # Users
  # ---------------------------------------------------------------------------
  /users:
    get:
      operationId: listUsers
      tags: [Users]
      summary: List all users
      description: |
        Retrieve a paginated list of user accounts, sorted by username.

        Requires `api.users.read` to see every account. A caller without it
        is not rejected: the listing is narrowed to their own account only
        (a single-row page), and the filters below are ignored.

        Each row may carry an `extra` map of plugin column values (see
        `GET /users/columns`).
      parameters:
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/per_page"
        - name: search
          in: query
          required: false
          description: Case-insensitive substring match on username, email, full name and title.
          schema:
            type: string
        - name: access
          in: query
          required: false
          description: |
            Only users effectively granted this permission (group access
            included, parent keys cover children, super admins match
            everything except the super flags themselves), e.g. `admin.login`.
          schema:
            type: string
          example: admin.login
        - name: permission
          in: query
          required: false
          description: Alias for `access`, used when `access` is not sent.
          schema:
            type: string
        - name: group
          in: query
          required: false
          description: Only members of this group.
          schema:
            type: string
          example: editors
        - name: filter
          in: query
          required: false
          description: |
            Id of a plugin-declared Users tab from `GET /users/filters`. The
            owning plugin narrows the list through `onApiUserListFilter`. Omit
            for the built-in "All Users" tab. Applied on both the Flex and the
            filesystem account backends.
          schema:
            type: string
      responses:
        "200":
          description: Paginated list of users.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/User"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

    post:
      operationId: createUser
      tags: [Users]
      summary: Create a new user
      description: |
        Create a new user account. Requires `api.users.write`. The username
        must be 3-64 characters and pass Grav core's username rules (no
        leading period, no `..`, and none of `\ / ? * : ; { }` or a line
        break).

        Granting `admin.super` or `api.super` through `access` requires a
        super admin (403). `groups` is applied only for a super admin. The
        password must meet the password policy (`system.pwd_regex`, or at
        least 8 characters when none is set; see `GET /auth/password-policy`).
        The submitted fields are validated against the account blueprint,
        with failures returned as 422. Returns 409 when the username is taken.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateUserRequest"
            example:
              username: johndoe
              password: s3cureP@ss
              email: john@example.com
              fullname: John Doe
              title: Editor
              state: enabled
              access:
                api:
                  access: true
                  pages:
                    read: true
                    write: true
      responses:
        "201":
          description: User created successfully.
          headers:
            Location:
              description: URL of the newly created user resource.
              schema:
                type: string
                example: /api/v1/users/johndoe
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/User"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /users/{username}:
    get:
      operationId: getUser
      tags: [Users]
      summary: Get a single user
      description: |
        Retrieve a user account by username. The response includes an `ETag`
        header for optimistic concurrency on PATCH requests.

        Fetching your own account needs only `api.access`; any other account
        requires `api.users.read`.
      parameters:
        - $ref: "#/components/parameters/username"
      responses:
        "200":
          description: User returned successfully.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/User"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

    patch:
      operationId: updateUser
      tags: [Users]
      summary: Partially update a user
      description: |
        Apply a partial update to an existing user account. Users can update
        their own account with the basic `api.access` permission; updating
        other users requires `api.users.write`. Some fields carry their own
        gate (see `UpdateUserRequest`). Only a super admin may modify a super
        admin account (403).

        Send the current `ETag` in `If-Match` to detect concurrent edits; a
        stale value returns 409. The submitted fields are validated against
        the account blueprint (422 on failure), and a new `password` must meet
        the password policy from `GET /auth/password-policy`.
      parameters:
        - $ref: "#/components/parameters/username"
        - $ref: "#/components/parameters/ifMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateUserRequest"
            example:
              fullname: Jane Doe
              email: jane@example.com
              state: enabled
      responses:
        "200":
          description: User updated successfully.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/User"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

    delete:
      operationId: deleteUser
      tags: [Users]
      summary: Delete a user
      description: |
        Permanently remove a user account. Requires `api.users.write`. You
        cannot delete your own account; a `403 Forbidden` is returned if you
        try. Only a super admin may delete a super admin account (403).
      parameters:
        - $ref: "#/components/parameters/username"
      responses:
        "204":
          description: User deleted successfully. No content returned.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # User Avatar
  # ---------------------------------------------------------------------------
  /users/{username}/avatar:
    post:
      operationId: uploadUserAvatar
      tags: [Users]
      summary: Upload user avatar
      description: |
        Upload or replace the avatar image for the specified user. The image is
        sent as `multipart/form-data` in an `avatar` field (`file` is accepted
        as a fallback). Users can update their own avatar with `api.access`;
        updating another user's avatar requires `api.users.write`, and only a
        super admin may change a super admin's avatar.

        The file must be a real PNG, JPEG or WebP image (checked from its
        bytes, not the declared type) of at most 8 MB; anything else returns
        422.
      parameters:
        - $ref: "#/components/parameters/username"
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [avatar]
              properties:
                avatar:
                  type: string
                  format: binary
                  description: Avatar image file to upload (PNG, JPEG or WebP, max 8 MB).
                file:
                  type: string
                  format: binary
                  description: Fallback field name, read only when `avatar` is absent.
      responses:
        "201":
          description: Avatar uploaded successfully. Returns the full user object.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/User"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

    delete:
      operationId: deleteUserAvatar
      tags: [Users]
      summary: Remove user avatar
      description: |
        Remove the avatar image for the specified user. Users can remove their
        own avatar with `api.access`; removing another user's avatar requires
        `api.users.write`, and only a super admin may change a super admin's
        avatar.
      parameters:
        - $ref: "#/components/parameters/username"
      responses:
        "200":
          description: Avatar removed successfully. Returns the full user object.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/User"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # User 2FA
  # ---------------------------------------------------------------------------
  /users/{username}/2fa:
    post:
      operationId: generateUser2FA
      tags: [Users]
      summary: Generate 2FA secret and QR code
      description: |
        Generate a new two-factor authentication secret and QR code data URI
        for the specified user. The client should display the QR code for the
        user to scan with their authenticator app, then confirm activation by
        sending a current code to `POST /users/{username}/2fa/enable`.

        Generating (or regenerating) a secret saves it to the account and
        turns `twofa_enabled` off until a code is verified against the new
        secret. Users can generate their own secret with `api.access`; doing
        it for another user requires `api.users.write`, and only a super admin
        may do it for a super admin. Returns 500 when the Login plugin's 2FA
        support is not installed.
      parameters:
        - $ref: "#/components/parameters/username"
      responses:
        "200":
          description: 2FA secret and QR code generated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      secret:
                        type: string
                        description: The TOTP secret key, in space-separated groups of four characters for readability.
                        example: JBSW Y3DP EHPK 3PXP
                      qr_code:
                        type: string
                        description: QR code as a data URI (PNG image encoded as base64).
                        example: "data:image/png;base64,iVBORw0KGgo..."
              example:
                data:
                  secret: JBSW Y3DP EHPK 3PXP
                  qr_code: "data:image/png;base64,iVBORw0KGgo..."
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "500":
          description: 2FA support is not available (the Login plugin is not installed).
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  # ---------------------------------------------------------------------------
  # API Keys
  # ---------------------------------------------------------------------------
  /users/{username}/api-keys:
    get:
      operationId: listApiKeys
      tags: [Users]
      summary: List API keys for a user
      description: |
        Return all API keys for the specified user. The response includes
        key metadata but never the key hash. Users can list their own keys
        with `api.access`; listing another user's keys requires
        `api.users.read`.
      parameters:
        - $ref: "#/components/parameters/username"
      responses:
        "200":
          description: List of API keys.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/ApiKey"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

    post:
      operationId: createApiKey
      tags: [Users]
      summary: Create a new API key
      description: |
        Generate a new API key for the specified user. The raw key value is
        returned **only once** in the `api_key` field of the response -- it
        cannot be retrieved again.

        Users can create keys for their own account with `api.access`;
        creating keys for other users requires `api.users.write`, and only a
        super admin may create keys for a super admin.

        A request authenticated with a scoped API key can only create a key
        whose scopes are a subset of its own, and cannot create an unscoped
        (full-access) key; either attempt returns 403.
      parameters:
        - $ref: "#/components/parameters/username"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  description: A human-readable label for the key.
                  default: API Key
                  example: CI/CD Pipeline
                scopes:
                  type: array
                  items:
                    type: string
                  description: Optional list of permission scopes to restrict this key.
                  example: [api.pages.read, api.media.read]
                expiry_days:
                  type: integer
                  nullable: true
                  description: Number of days until the key expires. Omit or set to null for no expiry.
                  example: 90
            example:
              name: CI/CD Pipeline
              scopes: [api.pages.read, api.media.read]
              expiry_days: 90
      responses:
        "201":
          description: API key created. The raw key is returned only in this response.
          headers:
            Location:
              description: URL of the user's API keys collection.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    allOf:
                      - $ref: "#/components/schemas/ApiKey"
                      - type: object
                        properties:
                          api_key:
                            type: string
                            description: |
                              The raw API key. Store it securely -- it is
                              shown only once and cannot be retrieved later.
                            example: grav_a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /users/{username}/api-keys/{keyId}:
    delete:
      operationId: deleteApiKey
      tags: [Users]
      summary: Revoke an API key
      description: |
        Permanently delete an API key. Any future requests using this key
        will be rejected. Users can revoke their own keys with `api.access`;
        revoking another user's key requires `api.users.write`, and only a
        super admin may revoke a super admin's key. Returns 404 when the key
        does not belong to this user.
      parameters:
        - $ref: "#/components/parameters/username"
        - name: keyId
          in: path
          required: true
          description: Unique identifier of the API key.
          schema:
            type: string
          example: a1b2c3d4e5f6a7b8
      responses:
        "204":
          description: API key revoked. No content returned.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /users/columns:
    get:
      operationId: listUserListColumns
      tags: [Users]
      summary: List plugin columns for the users list
      description: |
        Return the extra columns plugins add to the users list through the `onApiUserListColumns` event, filtered by each column's `authorize` and sorted by priority. The values for each user arrive in that user's `extra` map on `GET /users`. Requires `api.users.read`.
      responses:
        "200":
          description: Column definitions.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      columns:
                        type: array
                        items:
                          $ref: "#/components/schemas/UserListColumn"
              example:
                data:
                  columns:
                    - id: my-plugin-valid-till
                      plugin: my-plugin
                      label: Valid until
                      field: subscription.valid_till
                      formatter: datetime
                      sortable: false
                      priority: 50
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /users/filters:
    get:
      operationId: listUserListFilters
      tags: [Users]
      summary: List filter tabs for the users list
      description: |
        Return the tabs shown above the users list. Plugins add tabs through the `onApiUserListFilters` event; selecting one sends `?filter=<id>` to `GET /users`. The built-in `all` tab leads the row unless a plugin hides it (only possible once that plugin contributes a visible tab of its own). Requires `api.users.read`.
      responses:
        "200":
          description: Filter tabs.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      tabs:
                        type: array
                        items:
                          $ref: "#/components/schemas/UserListFilterTab"
                      defaultFilter:
                        type: string
                        description: Id of the tab to open when no filter is selected.
                      showAll:
                        type: boolean
                        description: Whether the built-in `all` tab is present.
              example:
                data:
                  tabs:
                    - { id: all, plugin: api, label: All Users }
                    - { id: active, plugin: my-plugin, label: Active, icon: fa-bolt, priority: 10 }
                  defaultFilter: all
                  showAll: true
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /users/row-actions:
    get:
      operationId: listUserRowActions
      tags: [Users]
      summary: List plugin row actions for the users list
      description: |
        Return the per-user action buttons plugins declare through the `onApiUserListRowActions` event, filtered by each action's `authorize`, capped at 24 and sorted by priority. Run one with `POST /users/{username}/row-action`. Requires `api.users.read`.
      responses:
        "200":
          description: Row action definitions.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      actions:
                        type: array
                        items:
                          $ref: "#/components/schemas/UserRowAction"
              example:
                data:
                  actions:
                    - id: impersonate-user
                      plugin: impersonate
                      label: Impersonate
                      icon: fa-user-secret
                      action: start
                      confirm: Impersonate this user?
                      priority: 80
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /users/{username}/row-action:
    post:
      operationId: runUserRowAction
      tags: [Users]
      summary: Run a plugin row action on a user
      description: |
        Run a declared row action against one user through the `onApiUserListRowAction` event. The action's `authorize` is checked again on the server, and an action the caller may not use returns 404 just like an unknown id. Non-super callers cannot target a super-admin account. The plugin handler applies its own per-target rules on top. The result is reduced to `status`, `message` and a same-origin `url`; a handler error comes back as 200 with `status: error` rather than failing the request. Requires `api.users.read`.
      parameters:
        - $ref: "#/components/parameters/username"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [id]
              properties:
                id:
                  type: string
                  description: Id of a row action from `GET /users/row-actions`.
            example:
              id: impersonate-user
      responses:
        "200":
          description: Action result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/UserRowActionResult"
              example:
                data:
                  status: success
                  message: Now impersonating jane.
                  url: /
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /users/{username}/2fa/enable:
    post:
      operationId: enableUser2FA
      tags: [Users]
      summary: Turn on 2FA
      description: |
        Confirm two-factor authentication by verifying a code against the secret generated with `POST /users/{username}/2fa`, then turn 2FA on. Only the account owner can do this, since it proves possession of the secret, and it requires `api.access`. Any other caller gets 403, whether or not the username exists. Returns 500 when the Login plugin's 2FA support is not installed.
      parameters:
        - $ref: "#/components/parameters/username"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code]
              properties:
                code:
                  type: string
                  description: Current code from the authenticator app.
                  example: "123456"
      responses:
        "200":
          description: 2FA turned on.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      twofa_enabled:
                        type: boolean
                        enum: [true]
              example:
                data:
                  twofa_enabled: true
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /users/{username}/2fa/disable:
    post:
      operationId: disableUser2FA
      tags: [Users]
      summary: Turn off 2FA
      description: |
        Turn off two-factor authentication and clear the secret. The account owner needs `api.access` and must send a valid current `code`. A caller with `api.users.write` (or a super admin), acting through a token or key whose scope includes `api.users.write`, can turn it off for another user without a code, for lost-device recovery; only super admins can do so for a super-admin account.
      parameters:
        - $ref: "#/components/parameters/username"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                code:
                  type: string
                  description: Current authenticator code. Required when disabling your own 2FA without `api.users.write`.
                  example: "123456"
      responses:
        "200":
          description: 2FA turned off.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      twofa_enabled:
                        type: boolean
                        enum: [false]
              example:
                data:
                  twofa_enabled: false
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # System
  # ---------------------------------------------------------------------------
  /system/environments:
    get:
      operationId: getEnvironments
      summary: List available environments
      tags: [System]
      description: |
        Returns the environment Grav detected for this request and the
        environments that can be selected as configuration write targets.
        Requires `api.system.read`.

        The first entry is always the base configuration (`user/config`),
        with `name: ""` and `label: "Default"`. Every other entry is an
        existing `user/env/<name>/` folder (legacy `user/<host>/config/`
        layouts are included too). The `hasOverrides` flag is always `false`
        on the base row.
      responses:
        '200':
          description: Environment list
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      detected:
                        type: string
                        description: Environment Grav inferred from the request URL.
                        example: localhost
                      environments:
                        type: array
                        items:
                          $ref: "#/components/schemas/EnvironmentEntry"
              example:
                data:
                  detected: localhost
                  environments:
                    - name: ""
                      label: Default
                      exists: true
                      hasOverrides: false
                    - name: staging.example.com
                      label: staging.example.com
                      exists: true
                      hasOverrides: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"
    post:
      operationId: createEnvironment
      tags: [System]
      summary: Create an environment
      description: |
        Creates an empty environment folder at `user/env/<name>/config`. Requires `api.config.write`. The name must start with a letter or digit and contain only letters, digits, `.`, `_` and `-`. The names `default` and `base` are reserved, and an existing environment cannot be created again (both return 422).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  pattern: "^[a-zA-Z0-9][a-zA-Z0-9._-]*$"
                  description: Environment name, usually a hostname.
                  example: staging.example.com
      responses:
        "201":
          description: Environment created.
          headers:
            X-Invalidates:
              description: Always `system:environments`.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/EnvironmentEntry"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /system/info:
    get:
      operationId: getSystemInfo
      tags: [System]
      summary: Get system information
      description: |
        Return runtime information about the Grav installation including
        Grav version, PHP version, loaded extensions, installed plugins and
        themes, plus a grouped digest of PHP configuration. Requires
        `api.system.read`. For demo accounts, `server_software` and the
        path-bearing `php_config` values are replaced with
        `(hidden in demo mode)`.
      responses:
        "200":
          description: System information returned successfully.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/SystemInfo"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /cache:
    delete:
      operationId: clearCache
      tags: [System]
      summary: Clear the cache
      description: |
        Clear the Grav cache. The `scope` query parameter controls which
        cache pool is purged: `images`, `assets` and `tmp` clear only that
        folder (core's `images-only`, `assets-only`, `tmp-only`). Requires
        `api.system.write`; an unknown scope returns 422.
      parameters:
        - name: scope
          in: query
          description: Cache scope to clear.
          schema:
            type: string
            enum: [all, standard, images, assets, tmp]
            default: standard
      responses:
        "200":
          description: Cache cleared successfully.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      scope:
                        type: string
                        example: standard
                      message:
                        type: string
                        example: "Cache cleared successfully (scope: standard)."
                      details:
                        type: array
                        items:
                          type: string
                        description: Per-directory results from the cache clear operation.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /system/logs:
    get:
      operationId: getSystemLogs
      tags: [System]
      summary: Read log entries
      description: |
        Return paginated entries from a log file (default `grav.log`).
        Entries are returned newest-first. Optionally filter by log level
        and by a message substring. Requires `api.system.read`; demo
        accounts get 403. A registered log that has not been written yet
        returns an empty page.
      parameters:
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/per_page"
        - name: file
          in: query
          description: |
            Log file to read. Must be one of the names listed by
            `GET /system/logs/files`; anything else returns 422.
          schema:
            type: string
            default: grav.log
          example: security.log
        - name: level
          in: query
          description: Filter entries by log level (case-insensitive).
          schema:
            type: string
            enum: [DEBUG, INFO, NOTICE, WARNING, ERROR, CRITICAL, ALERT, EMERGENCY]
          example: ERROR
        - name: search
          in: query
          description: Case-insensitive substring the entry's message must contain.
          schema:
            type: string
          example: not found
      responses:
        "200":
          description: Paginated list of log entries.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/LogEntry"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"
    delete:
      operationId: clearSystemLog
      tags: [System]
      summary: Clear a log file
      description: |
        Empty out one of the log files listed by `GET /system/logs/files`.

        Requires super-admin (`admin.super`); the read endpoints only require
        `api.system.read`. The file is truncated rather than deleted, and a
        single entry recording who cleared it is left in its place. The target
        may be given in the body or as a `?file=` query parameter, and defaults
        to `grav.log`.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                file:
                  type: string
                  description: Log file to clear. Must be one of the registered files.
                  default: grav.log
                  example: grav.log
      parameters:
        - name: file
          in: query
          required: false
          description: Log file to clear, when not supplied in the body.
          schema:
            type: string
            default: grav.log
          example: security.log
      responses:
        "200":
          description: Log cleared (or already empty).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      file:
                        type: string
                        example: grav.log
                      cleared_bytes:
                        type: integer
                        description: Size of the log before it was cleared.
                        example: 450507
                      message:
                        type: string
                        example: "Log cleared: grav.log"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /system/backup:
    post:
      operationId: createBackup
      tags: [System]
      summary: Create a backup
      description: |
        Trigger a full site backup and return metadata about the generated
        archive. Requires `api.system.backup` (archives carry account
        password hashes and config secrets); demo accounts get 403.
      responses:
        "201":
          description: Backup created successfully.
          headers:
            Location:
              description: URL of the backups list.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Backup"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /system/backups:
    get:
      operationId: listBackups
      tags: [System]
      summary: List existing backups
      description: |
        Return metadata for all backup archives found on disk, newest first,
        along with the purge policy and the number of configured backup
        profiles. Requires `api.system.backup`; demo accounts get 403.
      responses:
        "200":
          description: List of backups.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      backups:
                        type: array
                        items:
                          $ref: "#/components/schemas/Backup"
                      purge:
                        type: object
                        nullable: true
                        description: The `backups.purge` configuration, used to show storage limits.
                        properties:
                          trigger:
                            type: string
                            enum: [space, number, time]
                            example: space
                          max_backups_count:
                            type: integer
                            example: 25
                          max_backups_space:
                            type: integer
                            description: Maximum total size in GB.
                            example: 5
                          max_backups_time:
                            type: integer
                            description: Maximum age in days.
                            example: 365
                      profiles_count:
                        type: integer
                        description: Number of backup profiles configured in `backups.profiles`.
                        example: 1
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /system/environments/{name}:
    delete:
      operationId: deleteEnvironment
      tags: [System]
      summary: Delete an environment
      description: |
        Recursively deletes the environment folder `user/env/<name>/` and everything in it. Requires API super authority (not just `api.config.write`), because the folder can hold `system` and `security` overrides that only a super user may write.
        Returns 404 when no `user/env/<name>/` folder exists. Refused with 422 when the name is invalid, when it is the environment Grav resolved for the current request, or when it only exists in the legacy `user/<name>/` layout, which must be removed by hand. A folder that is a symlink pointing outside `user/env/` is also refused.
      parameters:
        - name: name
          in: path
          required: true
          description: Environment name.
          schema:
            type: string
          example: staging.example.com
      responses:
        "204":
          description: Environment deleted.
          headers:
            X-Invalidates:
              description: Always `system:environments`.
              schema:
                type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /system/logs/files:
    get:
      operationId: listLogFiles
      tags: [System]
      summary: List readable log files
      description: |
        Returns the log files the admin log viewer can open. Requires `api.system.read`, and is refused with 403 for demo accounts. The core logs (`grav.log`, `security.log`, `email.log`, `scheduler.log`) are always listed first, even before they exist; any other `*.log` in the `log://` folder follows, then files plugins add through the `onApiLogFiles` event. Entries are de-duplicated by file name.
      responses:
        "200":
          description: Log file list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      files:
                        type: array
                        items:
                          type: object
                          properties:
                            file:
                              type: string
                              description: Log file name inside `log://`.
                              example: security.log
                            label:
                              type: string
                              example: Security Log
                          required: [file, label]
                      default:
                        type: string
                        description: File the viewer opens first.
                        example: grav.log
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /system/backups/{filename}:
    delete:
      operationId: deleteBackup
      tags: [System]
      summary: Delete a backup
      description: Deletes a backup archive from the `backup://` folder. Requires `api.system.backup`, and is refused with 403 for demo accounts. The filename must be a plain `.zip` name with no path components.
      parameters:
        - $ref: "#/components/parameters/BackupFilename"
      responses:
        "204":
          description: Backup deleted.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /system/backups/{filename}/download:
    get:
      operationId: downloadBackup
      tags: [System]
      summary: Download a backup
      description: Streams a backup archive as a file download. Requires `api.system.backup`, and is refused with 403 for demo accounts. The filename must be a plain `.zip` name with no path components. Errors are returned as `application/problem+json`.
      parameters:
        - $ref: "#/components/parameters/BackupFilename"
      responses:
        "200":
          description: The backup archive.
          headers:
            Content-Disposition:
              description: "`attachment; filename=\"<filename>\"`"
              schema:
                type: string
            Content-Length:
              description: Archive size in bytes.
              schema:
                type: integer
          content:
            application/zip:
              schema:
                type: string
                format: binary
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # Languages
  # ---------------------------------------------------------------------------
  /languages:
    get:
      operationId: listLanguages
      tags: [Languages]
      summary: List configured site languages
      description: |
        Return the language configuration for the site, including which
        languages are enabled, the default language, and which language is
        currently active based on the request context. On a single-language
        site `enabled` is false, `languages` is empty and `default`/`active`
        are `null`.

        Requires `api.pages.read`.
      responses:
        "200":
          description: Language configuration returned successfully.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/LanguageInfo"
              example:
                data:
                  enabled: true
                  languages:
                    - code: en
                      name: English
                      native_name: English
                      rtl: false
                      is_default: true
                    - code: fr
                      name: French
                      native_name: Français
                      rtl: false
                      is_default: false
                  default: en
                  active: en
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # GPM
  # ---------------------------------------------------------------------------
  /gpm/plugins:
    get:
      operationId: listInstalledPlugins
      tags: [GPM]
      summary: List installed plugins
      description: |
        Return a list of all installed plugins with their version, enabled
        state, and whether an update is available. Not paginated. Package
        names and descriptions written as translation keys are resolved in
        the caller's admin language.

        A plugin whose settings are drawn on an admin page (its own, or the
        page of a plugin it extends) also carries `settings_route` and, when
        that page belongs to another plugin, `settings_page`.

        Requires `api.gpm.read`.
      responses:
        "200":
          description: List of installed plugins.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Package"
              example:
                data:
                  - slug: api
                    name: API
                    version: 1.0.31
                    type: plugin
                    description: RESTful API for Grav CMS.
                    description_html: "<p>RESTful API for Grav CMS.</p>"
                    author:
                      name: Team Grav
                      email: devs@getgrav.org
                      url: https://getgrav.org
                    homepage: https://github.com/getgrav/grav-plugin-api
                    enabled: true
                    is_symlink: false
                    keywords: [api, rest]
                    updatable: false
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/plugins/{slug}:
    get:
      operationId: getInstalledPlugin
      tags: [GPM]
      summary: Get installed plugin details
      description: |
        Return the package details of a specific installed plugin, with its
        update status. When the plugin ships Admin Next custom field
        components (`admin-next/fields/*.js`) the response lists them in
        `custom_fields`; `settings_route` / `settings_page` are included as
        in `GET /gpm/plugins`. The README and changelog are served by their
        own endpoints.

        Send the `ETag` back in `If-None-Match` to get an empty 304 while
        nothing has changed.

        Requires `api.gpm.read`.
      parameters:
        - name: slug
          in: path
          required: true
          description: Plugin slug (e.g. `admin`, `api`).
          schema:
            type: string
          example: api
        - $ref: "#/components/parameters/ExtensionScriptIfNoneMatch"
      responses:
        "200":
          description: Plugin details returned successfully.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Package"
        "304":
          description: The plugin details have not changed since the ETag sent in `If-None-Match`. No body.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/themes:
    get:
      operationId: listInstalledThemes
      tags: [GPM]
      summary: List installed themes
      description: |
        Return a list of all installed themes with their version, active
        state, and whether an update is available. Not paginated. For themes,
        `enabled` means "is the active theme". Each theme also carries
        `thumbnail` and `screenshot` URLs, resized copies of the theme's own
        `thumbnail.jpg|png` / `screenshot.jpg|png` served from
        `/thumbnails/{file}` (each falls back to the other; both `null` when
        the theme ships neither).

        Requires `api.gpm.read`.
      responses:
        "200":
          description: List of installed themes.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Package"
              example:
                data:
                  - slug: quark
                    name: Quark
                    version: 2.0.4
                    type: theme
                    description: Quark is the default theme for Grav CMS.
                    description_html: "<p>Quark is the default theme for Grav CMS.</p>"
                    author:
                      name: Team Grav
                      email: devs@getgrav.org
                      url: https://getgrav.org
                    homepage: https://github.com/getgrav/grav-theme-quark
                    enabled: true
                    is_symlink: false
                    updatable: false
                    thumbnail: https://example.com/api/v1/thumbnails/3f9a1c.jpg
                    screenshot: https://example.com/api/v1/thumbnails/8b2e4d.jpg
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/themes/{slug}:
    get:
      operationId: getInstalledTheme
      tags: [GPM]
      summary: Get installed theme details
      description: |
        Return the package details of a specific installed theme, with its
        update status, `thumbnail` / `screenshot` URLs (as in
        `GET /gpm/themes`) and, when the theme ships Admin Next custom field
        components, `custom_fields`. The README and changelog are served by
        their own endpoints.

        Send the `ETag` back in `If-None-Match` to get an empty 304 while
        nothing has changed.

        Requires `api.gpm.read`.
      parameters:
        - name: slug
          in: path
          required: true
          description: Theme slug (e.g. `quark`).
          schema:
            type: string
          example: quark
        - $ref: "#/components/parameters/ExtensionScriptIfNoneMatch"
      responses:
        "200":
          description: Theme details returned successfully.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Package"
        "304":
          description: The theme details have not changed since the ETag sent in `If-None-Match`. No body.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/plugins/{slug}/readme:
    get:
      operationId: getPluginReadme
      tags: [GPM]
      summary: Get plugin README content
      description: |
        Return the raw Markdown content of the specified plugin's README.md
        file.

        Requires `api.gpm.read`.
      parameters:
        - name: slug
          in: path
          required: true
          description: Plugin slug (e.g. `admin`, `api`).
          schema:
            type: string
          example: api
      responses:
        "200":
          description: Plugin README content returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      content:
                        type: string
                        description: Raw Markdown content of the README file.
                        example: "# API Plugin\n\nRESTful API for Grav CMS."
              example:
                data:
                  content: "# API Plugin\n\nRESTful API for Grav CMS."
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ExtensionScriptInvalidSlug"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/plugins/{slug}/changelog:
    get:
      operationId: getPluginChangelog
      tags: [GPM]
      summary: Get plugin changelog content
      description: |
        Return the raw Markdown content of the specified plugin's CHANGELOG.md
        file.

        Requires `api.gpm.read`.
      parameters:
        - name: slug
          in: path
          required: true
          description: Plugin slug (e.g. `admin`, `api`).
          schema:
            type: string
          example: api
      responses:
        "200":
          description: Plugin changelog content returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      content:
                        type: string
                        description: Raw Markdown content of the CHANGELOG file.
                        example: "# v1.0.0\n\n- Initial release"
              example:
                data:
                  content: "# v1.0.0\n\n- Initial release"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ExtensionScriptInvalidSlug"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/themes/{slug}/readme:
    get:
      operationId: getThemeReadme
      tags: [GPM]
      summary: Get theme README content
      description: |
        Return the raw Markdown content of the specified theme's README.md
        file.

        Requires `api.gpm.read`.
      parameters:
        - name: slug
          in: path
          required: true
          description: Theme slug (e.g. `quark`).
          schema:
            type: string
          example: quark
      responses:
        "200":
          description: Theme README content returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      content:
                        type: string
                        description: Raw Markdown content of the README file.
                        example: "# Quark Theme\n\nDefault theme for Grav CMS."
              example:
                data:
                  content: "# Quark Theme\n\nDefault theme for Grav CMS."
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ExtensionScriptInvalidSlug"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/themes/{slug}/changelog:
    get:
      operationId: getThemeChangelog
      tags: [GPM]
      summary: Get theme changelog content
      description: |
        Return the raw Markdown content of the specified theme's CHANGELOG.md
        file.

        Requires `api.gpm.read`.
      parameters:
        - name: slug
          in: path
          required: true
          description: Theme slug (e.g. `quark`).
          schema:
            type: string
          example: quark
      responses:
        "200":
          description: Theme changelog content returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      content:
                        type: string
                        description: Raw Markdown content of the CHANGELOG file.
                        example: "# v2.0.4\n\n- Bug fixes"
              example:
                data:
                  content: "# v2.0.4\n\n- Bug fixes"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ExtensionScriptInvalidSlug"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/updates:
    get:
      operationId: checkUpdates
      tags: [GPM]
      summary: Check for updates
      description: |
        Check the GPM repository for available updates to Grav core,
        installed plugins, and installed themes. Only packages that have an
        update are listed under `plugins` / `themes`.

        Requires `api.gpm.read`.
      parameters:
        - name: flush
          in: query
          required: false
          description: Refresh the cached GPM repository data before checking (any boolean-like value, e.g. `1` or `true`).
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: Update check completed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    required: [grav, plugins, themes, total, installed]
                    properties:
                      grav:
                        type: object
                        description: Grav core version and update info. Always present.
                        required: [current, available, updatable, date, is_symlink]
                        properties:
                          current:
                            type: string
                            description: Installed Grav version.
                            example: 2.1.0
                          available:
                            type: string
                            nullable: true
                            description: Latest Grav version in the repository, or `null` when the repository could not be read.
                            example: 2.1.1
                          updatable:
                            type: boolean
                            description: Whether `available` is newer than `current`.
                            example: true
                          date:
                            type: string
                            nullable: true
                            description: Release date of the available version, as the repository reports it.
                            example: "2026-09-18"
                          is_symlink:
                            type: boolean
                            description: Whether Grav core is symlinked (a symlinked install cannot be upgraded through `POST /gpm/upgrade`).
                            example: false
                      plugins:
                        type: array
                        description: Installed plugins with an update. `version` is the installed version, `available_version` the new one.
                        items:
                          $ref: "#/components/schemas/Package"
                      themes:
                        type: array
                        description: Installed themes with an update. `version` is the installed version, `available_version` the new one.
                        items:
                          $ref: "#/components/schemas/Package"
                      total:
                        type: integer
                        description: Total number of available updates, counting Grav core as one.
                        example: 2
                      installed:
                        type: integer
                        description: Number of installed plugins and themes combined.
                        example: 14
              example:
                data:
                  grav:
                    current: 2.1.0
                    available: 2.1.1
                    updatable: true
                    date: "2026-09-18"
                    is_symlink: false
                  plugins:
                    - slug: form
                      name: Form
                      version: 8.1.0
                      type: plugin
                      description: Enables forms handling and processing.
                      description_html: "<p>Enables forms handling and processing.</p>"
                      author:
                        name: Team Grav
                        email: devs@getgrav.org
                        url: https://getgrav.org
                      homepage: https://github.com/getgrav/grav-plugin-form
                      enabled: true
                      is_symlink: false
                      available_version: 8.2.0
                      updatable: true
                  themes: []
                  total: 2
                  installed: 14
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/install:
    post:
      operationId: installPackage
      tags: [GPM]
      summary: Install a package
      description: |
        Install a plugin or theme from the GPM repository. Requires
        `api.gpm.write` permission.

        For premium packages, include a `license` field or ensure the license
        has already been registered via the license-manager plugin/API.

        Dependencies the package needs (missing or outdated plugins) are
        installed first, one at a time; their slugs are returned in
        `dependencies`. If one fails partway, the 500 detail names the
        dependencies that were already installed.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [package]
              properties:
                package:
                  type: string
                  description: Package slug to install.
                  example: shortcode-core
                type:
                  type: string
                  enum: [plugin, theme]
                  default: plugin
                  description: Package type.
                  example: plugin
                license:
                  type: string
                  description: |
                    License key for premium packages, exactly as the store that sold the package sent it (a Grav Premium key looks like `XXXXXXXX-XXXXXXXX-XXXXXXXX-XXXXXXXX`, a KahunaCart key like `KC-XXXX-XXXX-XXXX-XXXX`).
                    If omitted, the system checks `user://data/licenses.yaml` for an existing license.
                    The key is saved to `licenses.yaml` only when `package` names a package in the repository, and it is removed again (or the key it replaced is put back) if the install fails.
                  example: A1B2C3D4-E5F6A7B8-C9D0E1F2-A3B4C5D6
            example:
              package: shortcode-core
              type: plugin
      responses:
        "201":
          description: Package installed successfully.
          headers:
            X-Invalidates:
              description: Client cache tags to refresh, e.g. `plugins:create:{slug}, plugins:list, gpm:update`.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/GpmPackageActionResult"
              example:
                data:
                  message: "Plugin 'shortcode-ui' installed successfully. Dependencies installed: shortcode-core."
                  package: shortcode-ui
                  type: plugin
                  dependencies: [shortcode-core]
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: |
            `package` is missing, `type` is not `plugin`/`theme`, the package
            is already installed, the `license` is not a valid key, a premium
            package has no license, or GPM reports the package (or one of its
            dependencies) cannot be installed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 422
                title: Unprocessable Entity
                detail: "Plugin 'shortcode-core' is already installed. Use the update endpoint to update it."
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "500":
          description: A dependency or the package itself failed to install.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 500
                title: Installation Failed
                detail: "Failed to install dependency 'shortcode-core' for plugin 'shortcode-ui'."

  /gpm/remove:
    post:
      operationId: removePackage
      tags: [GPM]
      summary: Remove a package
      description: |
        Remove an installed plugin or theme. Whether it is a plugin or a
        theme is detected from what is installed. Requires `api.gpm.write`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [package]
              properties:
                package:
                  type: string
                  description: Package slug to remove.
                  example: shortcode-core
            example:
              package: shortcode-core
      responses:
        "204":
          description: Package removed. No body.
          headers:
            X-Invalidates:
              description: Client cache tags to refresh, e.g. `plugins:delete:{slug}, plugins:list, gpm:update`.
              schema:
                type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: The package is not installed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 404
                title: Not Found
                detail: "Package 'shortcode-core' is not installed."
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "500":
          description: GPM failed to remove the package.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 500
                title: Removal Failed
                detail: "Failed to remove plugin 'shortcode-core'."

  /gpm/update:
    post:
      operationId: updatePackage
      tags: [GPM]
      summary: Update a package
      description: |
        Update a single installed plugin or theme to the latest version.
        Whether it is a plugin or a theme is detected from what is
        installed. Dependencies that are missing or outdated are installed
        first and listed in `dependencies`. Requires `api.gpm.write`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [package]
              properties:
                package:
                  type: string
                  description: Package slug to update.
                  example: admin
            example:
              package: admin
      responses:
        "200":
          description: Package updated successfully.
          headers:
            X-Invalidates:
              description: Client cache tags to refresh, e.g. `plugins:update:{slug}, plugins:list, gpm:update`.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/GpmPackageActionResult"
              example:
                data:
                  message: "Package 'admin' updated successfully."
                  package: admin
                  type: plugin
                  dependencies: []
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: No plugin or theme with that slug is installed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 404
                title: Not Found
                detail: "Package 'admin' is not installed."
        "422":
          description: |
            `package` is missing, the installed package has no update, or
            GPM reports the update (or one of its dependencies) cannot be
            installed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 422
                title: Unprocessable Entity
                detail: "Plugin 'admin' is already up to date."
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "500":
          description: A dependency or the package itself failed to update.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 500
                title: Update Failed
                detail: "Failed to update 'admin'."

  /gpm/update-all:
    post:
      operationId: updateAllPackages
      tags: [GPM]
      summary: Update all packages
      description: |
        Update all installed plugins and themes that have available updates.
        The repository cache is refreshed first. Each package goes through
        the same dependency checks as `POST /gpm/update`; a package that
        fails does not stop the others, so the response is 200 even when
        some (or all) packages land in `failed`. No request body.
        Requires `api.gpm.write`.
      responses:
        "200":
          description: Update run finished. Check `failed` for packages that were not updated.
          headers:
            X-Invalidates:
              description: Always `plugins:list, themes:list, gpm:update`.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    required: [updated, failed, skipped, cascaded_dependencies]
                    properties:
                      updated:
                        type: array
                        items:
                          type: string
                        description: Slugs of packages that were updated.
                        example: [admin2, api]
                      failed:
                        type: array
                        description: Packages that were not updated, each with the reason.
                        items:
                          type: object
                          required: [package, error]
                          properties:
                            package:
                              type: string
                              example: email
                            error:
                              type: string
                              example: "Failed to install dependency 'form': Download failed."
                      skipped:
                        type: array
                        description: Packages already brought up to date as a dependency of an earlier package in the same run.
                        items:
                          type: object
                          required: [package, reason]
                          properties:
                            package:
                              type: string
                              example: form
                            reason:
                              type: string
                              example: already up to date (installed as a dependency)
                      cascaded_dependencies:
                        type: array
                        items:
                          type: string
                        description: Slugs installed or updated as dependencies of other packages during this run.
                        example: [form]
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/upgrade:
    post:
      operationId: upgradeGrav
      tags: [GPM]
      summary: Self-upgrade Grav core
      description: |
        Upgrade Grav to the latest available version. Requires
        `api.gpm.write`.

        Before installing, the upgrade runs Grav's preflight checks. When
        they block the upgrade (for example enabled plugins not marked
        compatible with the target version), the response is 409 with the
        report; resend with `{"override": true}` to proceed anyway.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                override:
                  type: boolean
                  default: false
                  description: Proceed even when preflight checks block the upgrade.
            example:
              override: true
      responses:
        "200":
          description: Grav upgraded successfully.
          headers:
            X-Invalidates:
              description: Always `grav:update, gpm:update`.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    required: [message, previous_version, new_version]
                    properties:
                      message:
                        type: string
                        example: Grav upgraded successfully.
                      previous_version:
                        type: string
                        example: 2.1.0
                      new_version:
                        type: string
                        example: 2.1.1
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: |
            Preflight checks blocked the upgrade (only when `override` was not
            set). This is a normal JSON body in the `data` envelope, not a
            problem document.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    required: [status, message, blocking, warnings, incompatible_packages, can_override]
                    properties:
                      status:
                        type: string
                        enum: [preflight_failed]
                      message:
                        type: string
                        example: Upgrade blocked by preflight checks.
                      blocking:
                        type: array
                        description: Reasons the upgrade was blocked.
                        items:
                          type: string
                      warnings:
                        type: array
                        description: Non-blocking warnings from the preflight report.
                        items:
                          type: string
                      incompatible_packages:
                        type: object
                        additionalProperties: true
                        description: Grav's report of installed packages not marked compatible with the target version (includes `target` and the `blocking` packages).
                      can_override:
                        type: boolean
                        description: Always `true`; resend with `override` to proceed.
              example:
                data:
                  status: preflight_failed
                  message: Upgrade blocked by preflight checks.
                  blocking:
                    - Some enabled plugins/themes have not been marked as compatible with Grav 2.2. Disable them before continuing.
                  warnings: []
                  incompatible_packages:
                    target: "2.2"
                  can_override: true
        "422":
          description: Grav is already at the latest version, or Grav core is installed via symlink.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 422
                title: Unprocessable Entity
                detail: Grav is already at the latest version.
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "500":
          description: The upgrade failed (download, PHP requirement, install error).
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 500
                title: Upgrade Failed
                detail: Failed to upgrade Grav core.

  /gpm/direct-install:
    post:
      operationId: directInstallPackage
      tags: [GPM]
      summary: Install from URL or file upload
      description: |
        Install a plugin or theme directly from a URL or an uploaded ZIP
        file. This bypasses the GPM repository and allows installing custom
        or development packages. Requires `api.gpm.write`.

        Send either a JSON body with `url`, or a multipart upload with the
        ZIP in `file`; when both are present `url` wins. Dependencies are not
        resolved and the response does not name the installed package.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [url]
              properties:
                url:
                  type: string
                  description: An `http://` or `https://` URL pointing to a ZIP archive of the package. Anything else (a server path, a stream such as `backup://`, `file://`) is refused with a 422; upload a local ZIP as `file` instead.
                  example: https://github.com/user/grav-plugin-custom/archive/main.zip
            example:
              url: https://github.com/user/grav-plugin-custom/archive/main.zip
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: ZIP archive to install.
      responses:
        "201":
          description: Package installed from direct source.
          headers:
            X-Invalidates:
              description: Always `plugins:list, themes:list, gpm:update`.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    required: [message]
                    properties:
                      message:
                        type: string
                        example: Package installed successfully via direct install.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Neither a `url` field nor a successfully uploaded `file` was sent, or `url` is not an `http://`/`https://` address.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 422
                title: Unprocessable Entity
                detail: Either a "url" field or an uploaded "file" is required.
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "500":
          description: Download, extraction or installation failed.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 500
                title: Installation Failed
                detail: Package extraction failed.

  /gpm/search:
    get:
      operationId: searchPackages
      tags: [GPM]
      summary: Search GPM repository
      description: |
        Search across all plugins and themes in the GPM repository by keyword.
        Matches against slug, name, description, author, and keywords
        (case-insensitive substring). Plugins are listed before themes. If
        the repository cannot be reached the result is simply empty. Each
        result carries `installed`.

        Requires `api.gpm.read`.
      parameters:
        - name: q
          in: query
          required: true
          description: Search query string.
          schema:
            type: string
          example: email
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/per_page"
      responses:
        "200":
          description: Paginated search results across plugins and themes.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Package"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/repository/plugins:
    get:
      operationId: browseRepositoryPlugins
      tags: [GPM]
      summary: Browse available plugins
      description: |
        Return a paginated list of plugins available in the GPM repository.
        Includes metadata about each package and whether it is already
        installed. Use the `q` parameter to filter by keyword.

        Requires `api.gpm.read`.
      parameters:
        - name: q
          in: query
          required: false
          description: Filter by keyword (matches slug, name, description, author, keywords).
          schema:
            type: string
        - $ref: "#/components/parameters/page"
        - name: per_page
          in: query
          required: false
          description: Number of items per page. Unlike other lists, values above the configured maximum are honoured up to 2000, so the whole repository can be fetched in one request.
          schema:
            type: integer
            minimum: 1
            maximum: 2000
            default: 20
      responses:
        "200":
          description: Paginated list of available plugins.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Package"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "502":
          $ref: "#/components/responses/GpmRepositoryUnreachable"

  /gpm/repository/themes:
    get:
      operationId: browseRepositoryThemes
      tags: [GPM]
      summary: Browse available themes
      description: |
        Return a paginated list of themes available in the GPM repository.
        Includes metadata about each theme and whether it is already
        installed. Use the `q` parameter to filter by keyword.

        Requires `api.gpm.read`.
      parameters:
        - name: q
          in: query
          required: false
          description: Filter by keyword (matches slug, name, description, author, keywords).
          schema:
            type: string
        - $ref: "#/components/parameters/page"
        - name: per_page
          in: query
          required: false
          description: Number of items per page. Unlike other lists, values above the configured maximum are honoured up to 2000, so the whole repository can be fetched in one request.
          schema:
            type: integer
            minimum: 1
            maximum: 2000
            default: 20
      responses:
        "200":
          description: Paginated list of available themes.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Package"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "502":
          $ref: "#/components/responses/GpmRepositoryUnreachable"

  /gpm/repository/{slug}:
    get:
      operationId: getRepositoryPackage
      tags: [GPM]
      summary: Get repository package details
      description: |
        Return the repository metadata of a plugin or theme in the GPM
        repository (the same fields as the repository listings, including
        `installed`, premium/licence info and, for themes, the `screenshot`
        URL). README and changelog text are not included.

        Requires `api.gpm.read`.
      parameters:
        - name: slug
          in: path
          required: true
          description: Package slug in the repository.
          schema:
            type: string
          example: admin
      responses:
        "200":
          description: Repository package details returned successfully.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Package"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/plugins/{slug}/page:
    get:
      operationId: getPluginPage
      tags: [GPM]
      summary: Get plugin admin page definition
      description: |
        Returns the admin page definition for a plugin that registers a custom
        page in the admin sidebar. The response includes the page metadata,
        blueprint reference, data and save endpoints, available actions, and
        whether the plugin provides a custom web component.

        The definition is resolved in this order: the plugin's answer to the
        `onApiPluginPageInfo` event, then `admin-next/pages/{slug}.yaml`, then
        `admin-next/pages/{slug}.js` alone (which yields a minimal
        `component` definition). The server always sets
        `has_custom_component`, and keeps `settings_route` / `settings_page`
        only when they are valid.

        Requires `api.access` only: the data behind the page answers to the
        plugin's own permissions.
      parameters:
        - name: slug
          in: path
          required: true
          description: Plugin slug (e.g. `algolia-pro`, `git-sync`).
          schema:
            type: string
          example: algolia-pro
      responses:
        "200":
          description: Plugin page definition returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/PluginPageDefinition"
              example:
                data:
                  id: algolia-pro
                  plugin: algolia-pro
                  title: Algolia Pro
                  icon: fa-magnifying-glass
                  page_type: blueprint
                  blueprint: algolia-pro
                  data_endpoint: /algolia-pro/data
                  save_endpoint: /algolia-pro/data
                  actions:
                    - id: reindex
                      label: Reindex
                      icon: fa-rotate
                      endpoint: /algolia-pro/reindex
                      confirm: Reindex every enabled Algolia index now?
                    - id: save
                      label: Save
                      icon: fa-check
                      primary: true
                  has_custom_component: false
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: No admin page found for this plugin.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 404
                title: Not Found
                detail: "No admin page found for plugin 'api'."
        "422":
          $ref: "#/components/responses/ExtensionScriptInvalidSlug"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/plugins/{slug}/page-script:
    get:
      operationId: getPluginPageScript
      tags: [GPM]
      summary: Get plugin page web component script
      description: |
        Serves the JavaScript file containing the plugin's page-level web
        component, `admin-next/pages/{slug}.js`. This is used by the admin
        panel to dynamically load plugin-provided custom page UIs. Returns
        `application/javascript` content type, not JSON. The plugin must be
        enabled; a disabled plugin's script is a 404.

        Conditional GET with `ETag` and 304 is supported; `Cache-Control` is `private, no-cache`. Requires `api.access`.
      parameters:
        - $ref: "#/components/parameters/ExtensionScriptPluginSlug"
        - $ref: "#/components/parameters/ExtensionScriptIfNoneMatch"
      responses:
        "200":
          $ref: "#/components/responses/ExtensionScriptJs"
        "304":
          $ref: "#/components/responses/ExtensionScriptNotModified"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: The plugin is not installed, is not enabled, or ships no page script.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 404
                title: Not Found
                detail: "Page component not found for plugin 'data-manager'."
        "422":
          $ref: "#/components/responses/ExtensionScriptInvalidSlug"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/plugins/{slug}/report-script/{reportId}:
    get:
      operationId: getPluginReportScript
      tags: [GPM]
      summary: Get plugin report web component script
      description: |
        Serves the JavaScript file containing a plugin's report web component.
        Used by the admin panel to dynamically load plugin-provided report UIs
        in the Tools > Reports tab. Returns `application/javascript` content, not JSON.

        Convention: plugins place report scripts at
        `admin-next/reports/{reportId}.js` within their plugin directory. The
        plugin must be enabled; a disabled plugin's script is a 404.

        Conditional GET with `ETag` and 304 is supported; `Cache-Control` is `private, no-cache`. Requires `api.access`.
      parameters:
        - name: slug
          in: path
          required: true
          description: Plugin slug (e.g. `problems`). Must not contain `/`, `\` or be `.`/`..`.
          schema:
            type: string
          example: problems
        - name: reportId
          in: path
          required: true
          description: Report component identifier matching the `component` field from `/reports`. Only the basename is used.
          schema:
            type: string
          example: problems-report
        - $ref: "#/components/parameters/ExtensionScriptIfNoneMatch"
      responses:
        "200":
          $ref: "#/components/responses/ExtensionScriptJs"
        "304":
          $ref: "#/components/responses/ExtensionScriptNotModified"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: The plugin is not installed, is not enabled, or ships no such report script.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 404
                title: Not Found
                detail: "Report component 'problems-report' not found for plugin 'problems'."
        "422":
          $ref: "#/components/responses/ExtensionScriptInvalidSlug"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/grav/changelog:
    get:
      operationId: getGravChangelog
      tags: [GPM]
      summary: Get the Grav core changelog for pending versions
      description: |
        Returns the Grav core changelog entries for versions newer than the installed `GRAV_VERSION`, assembled from the GPM repository into one Markdown document. Each version gets a `# v{version} ({date})` heading followed by its notes. When the site is already on the latest version, `content` is an empty string.

        Requires `api.gpm.read`. Admin Next uses this for the "What's new" view before a core upgrade.
      parameters:
        - name: flush
          in: query
          description: When `true`, refresh the cached GPM repository data before reading the changelog.
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: Changelog returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      content:
                        type: string
                        description: Markdown changelog covering every version newer than the installed one.
              example:
                data:
                  content: "# v2.1.1 (09/20/2026)\n\n1. [](#bugfix)\n    * Fixed a thing"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/plugins/{slug}/fields:
    get:
      operationId: getPluginCustomFieldBundle
      tags: [GPM]
      summary: Get all of a plugin's custom field scripts
      description: |
        Returns every Admin Next custom field web component a plugin ships (`admin-next/fields/*.js`) in one response, as a JSON map of field type to JavaScript source. Admin Next fetches this once per plugin instead of one request per field type.

        The response is NOT wrapped in the usual `data` envelope. A plugin with no fields folder returns an empty collection (serialized as `[]`). The plugin does not have to be enabled.

        Conditional GET is supported: the `ETag` is built from each file's name, mtime and size, and a matching `If-None-Match` returns 304. `Cache-Control` is `private, no-cache`. Requires `api.access`.
      parameters:
        - $ref: "#/components/parameters/ExtensionScriptPluginSlug"
        - $ref: "#/components/parameters/ExtensionScriptIfNoneMatch"
      responses:
        "200":
          $ref: "#/components/responses/ExtensionScriptFieldBundle"
        "304":
          $ref: "#/components/responses/ExtensionScriptNotModified"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/ExtensionScriptNotFound"
        "422":
          $ref: "#/components/responses/ExtensionScriptInvalidSlug"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/themes/{slug}/fields:
    get:
      operationId: getThemeCustomFieldBundle
      tags: [GPM]
      summary: Get all of a theme's custom field scripts
      description: |
        Theme counterpart of `GET /gpm/plugins/{slug}/fields`: returns every Admin Next custom field web component the theme ships (`admin-next/fields/*.js`) as a JSON map of field type to JavaScript source, not wrapped in the `data` envelope. The theme does not have to be active.

        Conditional GET with a stat-based `ETag` and 304 is supported; `Cache-Control` is `private, no-cache`. Requires `api.access`.
      parameters:
        - $ref: "#/components/parameters/ExtensionScriptThemeSlug"
        - $ref: "#/components/parameters/ExtensionScriptIfNoneMatch"
      responses:
        "200":
          $ref: "#/components/responses/ExtensionScriptFieldBundle"
        "304":
          $ref: "#/components/responses/ExtensionScriptNotModified"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/ExtensionScriptNotFound"
        "422":
          $ref: "#/components/responses/ExtensionScriptInvalidSlug"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/plugins/{slug}/field/{type}:
    get:
      operationId: getPluginCustomFieldScript
      tags: [GPM]
      summary: Get one plugin custom field script
      description: |
        Serves a single Admin Next custom field web component from the plugin's `admin-next/fields/{type}.js`. Admin Next loads it on demand when a blueprint uses a field type it does not know (see `GET /custom-fields` for the type-to-provider map). The plugin does not have to be enabled, so a disabled plugin's own settings form can still draw its fields.

        Conditional GET is supported with an `ETag` of the form `"{mtime-hex}-{size-hex}"` and a 304 response; `Cache-Control` is `private, no-cache`. Requires `api.access`.
      parameters:
        - $ref: "#/components/parameters/ExtensionScriptPluginSlug"
        - $ref: "#/components/parameters/ExtensionScriptFieldType"
        - $ref: "#/components/parameters/ExtensionScriptIfNoneMatch"
      responses:
        "200":
          $ref: "#/components/responses/ExtensionScriptJs"
        "304":
          $ref: "#/components/responses/ExtensionScriptNotModified"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/ExtensionScriptNotFound"
        "422":
          $ref: "#/components/responses/ExtensionScriptInvalidSlug"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/themes/{slug}/field/{type}:
    get:
      operationId: getThemeCustomFieldScript
      tags: [GPM]
      summary: Get one theme custom field script
      description: |
        Theme counterpart of `GET /gpm/plugins/{slug}/field/{type}`: serves `admin-next/fields/{type}.js` from the theme folder as JavaScript.

        Conditional GET with `ETag` and 304 is supported; `Cache-Control` is `private, no-cache`. Requires `api.access`.
      parameters:
        - $ref: "#/components/parameters/ExtensionScriptThemeSlug"
        - $ref: "#/components/parameters/ExtensionScriptFieldType"
        - $ref: "#/components/parameters/ExtensionScriptIfNoneMatch"
      responses:
        "200":
          $ref: "#/components/responses/ExtensionScriptJs"
        "304":
          $ref: "#/components/responses/ExtensionScriptNotModified"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/ExtensionScriptNotFound"
        "422":
          $ref: "#/components/responses/ExtensionScriptInvalidSlug"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/plugins/{slug}/widget-script:
    get:
      operationId: getPluginWidgetScript
      tags: [GPM]
      summary: Get a plugin floating widget script
      description: |
        Serves the floating widget web component a plugin ships at `admin-next/widgets/{slug}.js`. Admin Next loads it for widgets listed by `GET /floating-widgets`. The plugin must be enabled; a disabled plugin's script is a 404.

        Conditional GET with `ETag` and 304 is supported; `Cache-Control` is `private, no-cache`. Requires `api.access`.
      parameters:
        - $ref: "#/components/parameters/ExtensionScriptPluginSlug"
        - $ref: "#/components/parameters/ExtensionScriptIfNoneMatch"
      responses:
        "200":
          $ref: "#/components/responses/ExtensionScriptJs"
        "304":
          $ref: "#/components/responses/ExtensionScriptNotModified"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/ExtensionScriptPluginNotFound"
        "422":
          $ref: "#/components/responses/ExtensionScriptInvalidSlug"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/plugins/{slug}/panel-script:
    get:
      operationId: getPluginPanelScript
      tags: [GPM]
      summary: Get a plugin context panel script
      description: |
        Serves the page-editor context panel web component a plugin ships at `admin-next/panels/{slug}.js`. Admin Next loads it when the user opens a panel listed by `GET /context-panels`. The plugin must be enabled; a disabled plugin's script is a 404.

        Conditional GET with `ETag` and 304 is supported; `Cache-Control` is `private, no-cache`. Requires `api.access`.
      parameters:
        - $ref: "#/components/parameters/ExtensionScriptPluginSlug"
        - $ref: "#/components/parameters/ExtensionScriptIfNoneMatch"
      responses:
        "200":
          $ref: "#/components/responses/ExtensionScriptJs"
        "304":
          $ref: "#/components/responses/ExtensionScriptNotModified"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/ExtensionScriptPluginNotFound"
        "422":
          $ref: "#/components/responses/ExtensionScriptInvalidSlug"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /gpm/plugins/{slug}/modal-script/{modalId}:
    get:
      operationId: getPluginModalScript
      tags: [GPM]
      summary: Get a plugin modal script
      description: |
        Serves a modal dialog web component a plugin ships at `admin-next/modals/{modalId}.js`. A plugin can ship several modals; Admin Next mounts each as `grav-{slug}--modal-{modalId}` when it is opened through `window.__GRAV_DIALOGS.open()`, a menubar `modal` action, or an editor toolbar button. The plugin must be enabled; a disabled plugin's script is a 404.

        Conditional GET with `ETag` and 304 is supported; `Cache-Control` is `private, no-cache`. Requires `api.access`.
      parameters:
        - $ref: "#/components/parameters/ExtensionScriptPluginSlug"
        - name: modalId
          in: path
          required: true
          description: Modal component identifier; the file `admin-next/modals/{modalId}.js` is served. Only the basename is used.
          schema:
            type: string
          example: youtube-insert
        - $ref: "#/components/parameters/ExtensionScriptIfNoneMatch"
      responses:
        "200":
          $ref: "#/components/responses/ExtensionScriptJs"
        "304":
          $ref: "#/components/responses/ExtensionScriptNotModified"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/ExtensionScriptPluginNotFound"
        "422":
          $ref: "#/components/responses/ExtensionScriptInvalidSlug"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # UI Extensions registries
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # Scheduler
  # ---------------------------------------------------------------------------
  /scheduler/jobs:
    get:
      operationId: listSchedulerJobs
      tags: [Scheduler]
      summary: List scheduler jobs
      description: |
        Return all configured scheduler jobs with their cron expressions,
        enabled state, last run time, and error status. Requires
        `api.scheduler.read`.
      responses:
        "200":
          description: List of scheduler jobs.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Job"
              example:
                data:
                  - id: default-site-backup
                    command: Grav\Common\Backup\Backups::backup
                    expression: "0 3 * * *"
                    enabled: true
                    status: success
                    last_run: "2026-03-26T03:00:00+00:00"
                    last_run_trigger: cron
                    next_run: "2026-03-27T03:00:00+00:00"
                    overdue: false
                    error: null
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /scheduler/status:
    get:
      operationId: getSchedulerStatus
      tags: [Scheduler]
      summary: Get cron status
      description: |
        Return the current status of the system cron integration, including
        whether the cron tab entry is properly installed and the commands
        used. Requires `api.scheduler.read`. Every field is best-effort: a
        host that cannot report how its scheduler is triggered still gets a
        200. For demo accounts, `cron_command`, `scheduler_command` and
        `whoami` are replaced with `(hidden in demo mode)`.
      responses:
        "200":
          description: Scheduler status returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      crontab_status:
                        type: string
                        description: Whether the scheduler is being triggered by cron.
                        enum: [installed, not_installed, unknown]
                        example: installed
                      cron_detection:
                        type: string
                        description: How `crontab_status` was determined.
                        enum: [last-run, crontab, unavailable]
                        example: last-run
                      process_available:
                        type: boolean
                        description: Whether PHP can start processes (`proc_open`), which running jobs from the admin needs.
                        example: true
                      last_run:
                        type: string
                        format: date-time
                        nullable: true
                        description: Time of the last automated (non-manual) scheduler run.
                      last_manual_run:
                        type: string
                        format: date-time
                        nullable: true
                        description: Time of the last run started from the admin.
                      environment:
                        type: string
                        nullable: true
                        description: Environment this request booted with.
                        example: localhost
                      environment_has_overrides:
                        type: boolean
                        description: Whether that environment has config overrides that a plain CLI run would not load.
                      last_run_environment:
                        type: string
                        nullable: true
                        description: Environment of the last real run (`cli` for an unpinned crontab).
                        example: cli
                      cron_command:
                        type: string
                        description: The cron command as it should appear in the crontab.
                        example: "* * * * * cd /var/www/grav && bin/grav scheduler 1>> /dev/null 2>&1"
                      scheduler_command:
                        type: string
                        description: The Grav scheduler CLI command.
                        example: bin/grav scheduler
                      whoami:
                        type: string
                        description: System user running the cron process.
                        example: www-data
                      health:
                        type: object
                        description: Core's scheduler health report; empty when unavailable.
                        additionalProperties: true
                        properties:
                          status:
                            type: string
                            enum: [healthy, warning]
                          message:
                            type: string
                          last_run:
                            type: string
                            nullable: true
                          last_run_age:
                            type: integer
                            nullable: true
                            description: Seconds since the last run.
                          queue_size:
                            type: integer
                          failed_jobs_24h:
                            type: integer
                          scheduled_jobs:
                            type: integer
                          jobs_due:
                            type: integer
                          webhook_enabled:
                            type: boolean
                          health_check_enabled:
                            type: boolean
                          timestamp:
                            type: string
                            format: date-time
                      triggers:
                        type: array
                        description: Active trigger methods.
                        items:
                          type: string
                          enum: [cron, webhook]
                      webhook_installed:
                        type: boolean
                        description: Whether the scheduler-webhook plugin is installed.
                      webhook_enabled:
                        type: boolean
                        description: Whether the scheduler-webhook plugin is enabled.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /scheduler/history:
    get:
      operationId: getSchedulerHistory
      tags: [Scheduler]
      summary: Get job execution history
      description: |
        Return the most recent run of each scheduler job, ordered by run time
        (newest first) and paginated. Only the latest run per job is kept, so
        this lists one entry per job that has run, not every past run.
        Requires `api.scheduler.read`.
      parameters:
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/per_page"
      responses:
        "200":
          description: Paginated job execution history.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          type: object
                          properties:
                            job_id:
                              type: string
                              example: default-site-backup
                            status:
                              type: string
                              description: Outcome of the last run (`unknown` when not recorded).
                              example: success
                            last_run:
                              type: string
                              format: date-time
                              nullable: true
                              example: "2026-03-26T03:00:00+00:00"
                            error:
                              type: string
                              nullable: true
                              description: Output of the last run when it failed.
                              example: null
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /scheduler/run:
    post:
      operationId: triggerSchedulerRun
      tags: [Scheduler]
      summary: Trigger a scheduler run
      description: |
        Run scheduler jobs now, without waiting for cron.

        By default this runs every job that has missed the last slot its
        schedule gave it (`overdue`). That is almost always what is wanted from
        a manual trigger: a job is only "due" during the exact minute its cron
        expression names, so `due` mode from a button press usually runs
        nothing at all.

        Runs triggered this way are recorded as manual, and deliberately do not
        count as evidence that the scheduler is being triggered automatically.
        Pressing this on a site with no crontab will not make it report one.

        Requires `api.scheduler.write`. Returns 501 when PHP's `proc_open` is
        disabled on the host, and 400 for an unknown `mode`. For demo
        accounts each job's `output` is replaced with
        `(hidden in demo mode)`.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                mode:
                  type: string
                  enum: [overdue, due, all]
                  default: overdue
                  description: >
                    `overdue` runs the jobs that missed their last scheduled
                    slot. `due` runs only what a cron tick this minute would
                    have run. `all` runs every enabled job whatever its
                    schedule says.
                job:
                  type: string
                  description: >
                    Run only this job, whatever its schedule says. Ignores
                    `mode`.
                  example: default-site-backup
                force:
                  type: boolean
                  description: "Legacy alias for `mode: all`."
                  default: false
            example:
              mode: overdue
      responses:
        "200":
          description: Scheduler run completed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      message:
                        type: string
                        example: 2 jobs ran successfully.
                      mode:
                        type: string
                        enum: [overdue, due, all, job]
                        example: overdue
                      forced:
                        type: boolean
                        description: Whether the run used `all` mode.
                        example: false
                      jobs_run:
                        type: integer
                        description: Number of jobs that were executed.
                        example: 2
                      jobs_failed:
                        type: integer
                        description: How many of those jobs failed.
                        example: 0
                      duration:
                        type: number
                        description: Seconds the run took.
                        example: 3.41
                      results:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              example: default-site-backup
                            successful:
                              type: boolean
                              example: true
                            output:
                              type: string
                              description: Whatever the job wrote, trimmed.
                              example: ""
                      job_states:
                        type: object
                        description: |
                          Stored state of every job after the run, keyed by job
                          ID, as core records it (`state`, `last-run` Unix
                          timestamp, `trigger`, and `error` on failure).
                        additionalProperties:
                          type: object
                          additionalProperties: true
                          properties:
                            state:
                              type: string
                              enum: [success, failure]
                            last-run:
                              type: integer
                            trigger:
                              type: string
                              enum: [cron, manual]
                            error:
                              type: string
                              nullable: true
        "400":
          description: Unknown run mode.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: No job with the requested id.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: The requested job is disabled.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "501":
          description: Running jobs is not available because `proc_open` is disabled on the host.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /systeminfo:
    get:
      operationId: getSystemInfoAlias
      tags: [System]
      summary: Get system info overview
      description: |
        Return a system info overview including PHP info, Grav status, disk
        usage, plugin counts, and cache status. Requires
        `api.system.read`, the same as `GET /system/info`.
      responses:
        "200":
          description: System info returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      php:
                        type: object
                        description: PHP runtime details.
                        properties:
                          version:
                            type: string
                            example: 8.3.14
                          sapi:
                            type: string
                            example: fpm-fcgi
                          extensions:
                            type: array
                            items:
                              type: string
                          memory_limit:
                            type: string
                            example: 256M
                          max_execution_time:
                            type: string
                            example: "30"
                          upload_max_filesize:
                            type: string
                            example: 64M
                          post_max_size:
                            type: string
                            example: 64M
                      grav:
                        type: object
                        properties:
                          version:
                            type: string
                            example: 2.1.0
                          php_version:
                            type: string
                            example: 8.3.14
                      disk:
                        type: object
                        description: Disk usage of the volume holding the Grav root.
                        properties:
                          free_space:
                            oneOf:
                              - type: number
                              - type: boolean
                            description: Free disk space in bytes, or `false` when PHP cannot read it.
                            example: 21474836480
                          total_space:
                            oneOf:
                              - type: number
                              - type: boolean
                            description: Total disk space in bytes, or `false` when PHP cannot read it.
                            example: 53687091200
                      plugins:
                        type: object
                        properties:
                          total:
                            type: integer
                            example: 30
                          enabled:
                            type: integer
                            example: 24
                          disabled:
                            type: integer
                            example: 6
                      cache:
                        type: object
                        properties:
                          enabled:
                            type: boolean
                            example: true
                          driver:
                            type: string
                            example: auto
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # Reports
  # ---------------------------------------------------------------------------
  /reports:
    get:
      operationId: getReports
      tags: [Reports]
      summary: Get plugin-extensible reports
      description: |
        Returns an array of diagnostic reports. Built-in reports are a
        Security Check (XSS scan, `security-check`), Twig in Content
        (`twig-content`) and a YAML Linter (`yaml-linter`). Plugins can add
        their own reports via the `onApiGenerateReports` event, optionally
        specifying a web component for custom rendering. Requires
        `api.reports.read`.
      responses:
        "200":
          description: Reports returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          description: Unique report identifier.
                          example: security-check
                        title:
                          type: string
                          description: Human-readable report title.
                          example: Grav Security Check
                        provider:
                          type: string
                          description: Source of the report (core or plugin name).
                          example: core
                        component:
                          type: string
                          nullable: true
                          description: Optional web component tag name for custom rendering.
                          example: null
                        status:
                          type: string
                          enum: [success, warning, error]
                          description: Overall report status.
                          example: success
                        message:
                          type: string
                          description: Summary message for the report.
                          example: "Security Scan complete: No issues found."
                        meta:
                          type: object
                          additionalProperties: true
                          description: |
                            Report-specific summary values. The `twig-content`
                            report sends `gate`, `sandbox`, `editor_enabled`,
                            `leak_count`, `event_count`,
                            `global_request_gated` and
                            `frontmatter_request_gated`.
                        items:
                          type: array
                          description: |
                            Report-specific detail items: `{route, field}` for
                            `security-check`, `{file, error}` for
                            `yaml-linter`, and `kind: leak` / `kind: event`
                            rows for `twig-content`.
                          items:
                            type: object
                            additionalProperties: true
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /reports/twig-content/allowlist:
    post:
      operationId: addTwigSandboxAllowlistEntry
      tags: [Reports]
      summary: Add a blocked token to the Twig sandbox allowlist
      description: |
        Appends one token to the matching `security.twig_sandbox.allowed_*` list and saves it to `user/config/security.yaml`. The shipped defaults live in code, so the file only holds the site's own additions. The live config is updated, the standard cache is cleared and `onApiConfigUpdated` fires with scope `security`.

        Flat rules (`tag`, `filter`, `function`) de-duplicate case-insensitively. Class rules (`method`, `property`) need `class` and append to that class's row (or add a new row).

        Ring-buffer events matching the rule, token and class are removed, so the report stops listing a block that is now allowed; `resolved` counts them.

        Super admin only. An API key needs `admin.super` in its scopes; demo accounts are rejected.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [rule, token]
              properties:
                rule:
                  type: string
                  enum: [tag, filter, function, method, property]
                  description: Sandbox rule the token was blocked by (the suffix of a `sandbox_*` event type).
                token:
                  type: string
                  description: The tag, filter, function, method or property name to allow.
                  example: dump
                class:
                  type: string
                  description: Owning class. Required for `method` and `property`, ignored otherwise.
                  example: Grav\Common\Page\Page
            example:
              rule: filter
              token: markdown
      responses:
        "200":
          description: Token added (or already present) and the allowlist saved.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      rule:
                        type: string
                        example: filter
                      key:
                        type: string
                        description: The `security.twig_sandbox` key that was written.
                        enum: [allowed_tags, allowed_filters, allowed_functions, allowed_methods, allowed_properties]
                        example: allowed_filters
                      value:
                        description: The full new value of that key. A list of names for flat rules, or a list of `{class, methods}` rows for class rules.
                        oneOf:
                          - type: array
                            items:
                              type: string
                          - type: array
                            items:
                              $ref: "#/components/schemas/TwigContentClassMembersRow"
                        example: [markdown]
                      resolved:
                        type: integer
                        description: Number of ring-buffer events removed because this addition resolved them.
                        example: 2
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /reports/twig-content/events:
    delete:
      operationId: clearTwigContentEvents
      tags: [Reports]
      summary: Clear the Twig in Content event log
      description: |
        Empties the ring buffer of recent blocked or blanked content-Twig tokens that the Twig in Content report and the page-editor banner read. `logs/security.log` is untouched.

        Requires `api.system.write` (not the report's read permission), so demo accounts cannot clear it.
      responses:
        "200":
          description: Buffer cleared.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      cleared:
                        type: boolean
                        description: True when the buffer file was removed or did not exist; false if it could not be deleted.
                        example: true
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /reports/twig-content/page:
    get:
      operationId: getTwigContentPageStatus
      tags: [Reports]
      summary: Get Twig in Content status for one page
      description: |
        Single-page status for the page-editor banner: whether this page would show raw `{{ }}` / `{% %}` markers to visitors, and the recent blocked events recorded for its route. Checks one page rather than scanning the site.

        `leak` is null when the page has no Twig markers, uses modular/theme Twig, will render its Twig normally, or cannot be found (a missing page is not an error). `events` only includes buffer entries whose `route` matches the query value exactly.

        Requires `api.reports.read`.
      parameters:
        - name: route
          in: query
          required: true
          description: Page route, with leading slash.
          schema:
            type: string
          example: /blog/my-first-post
      responses:
        "200":
          description: Page status returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      route:
                        type: string
                        example: /blog/my-first-post
                      gate:
                        type: boolean
                        description: Value of `security.twig_content.process_enabled`, the master gate.
                        example: false
                      sandbox:
                        type: boolean
                        description: Value of `security.twig_sandbox.enabled`.
                        example: true
                      leak:
                        type: object
                        allOf:
                          - $ref: "#/components/schemas/TwigContentLeak"
                        nullable: true
                      events:
                        type: array
                        description: Recent events for this route, newest first.
                        items:
                          $ref: "#/components/schemas/TwigContentEvent"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /reports/twig-content/scan:
    get:
      operationId: scanTwigContentUsage
      tags: [Reports]
      summary: Scan page content for Twig the sandbox would block
      description: |
        Walks every page (skipping modular/theme Twig) and lists the Twig tags, filters and functions used in content that the effective sandbox allowlists do not permit, meaning what content would need before the gate is switched on. It is a lexical approximation; the authoritative signal is the render-time events in the report.

        Each list maps a token to the routes that use it. PHP encodes an empty map as `[]`, so a list with no findings arrives as an empty array rather than `{}`.

        Requires `api.reports.read`. Cost grows with the number of pages.
      responses:
        "200":
          description: Scan results returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      tags:
                        $ref: "#/components/schemas/TwigContentTokenRoutes"
                      filters:
                        $ref: "#/components/schemas/TwigContentTokenRoutes"
                      functions:
                        $ref: "#/components/schemas/TwigContentTokenRoutes"
              example:
                data:
                  tags:
                    include: [/blog/my-first-post]
                  filters:
                    raw: [/about]
                  functions:
                    source: [/about, /contact]
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /reports/twig-content/sandbox-policy:
    get:
      operationId: getTwigSandboxPolicy
      tags: [Reports]
      summary: Get the effective Twig sandbox policy
      description: |
        Read-only breakdown of the page-content Twig sandbox: for each list, the built-in defaults from code, the site's additive `allowed_*` entries, its `denied_*` removals, and the effective result. Built from configuration only; additions made by plugins through `onBuildTwigSandboxPolicy` are not included.

        Requires `api.reports.read`.
      responses:
        "200":
          description: Policy returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      enabled:
                        type: boolean
                        description: Value of `security.twig_sandbox.enabled`.
                        example: true
                      config_access:
                        type: boolean
                        description: Value of `security.twig_content.config_access`.
                        example: false
                      lists:
                        type: object
                        properties:
                          tags:
                            $ref: "#/components/schemas/TwigContentSandboxFlatList"
                          filters:
                            $ref: "#/components/schemas/TwigContentSandboxFlatList"
                          functions:
                            $ref: "#/components/schemas/TwigContentSandboxFlatList"
                          methods:
                            $ref: "#/components/schemas/TwigContentSandboxClassList"
                          properties:
                            $ref: "#/components/schemas/TwigContentSandboxClassList"
                      config_denied_paths:
                        type: object
                        description: Config paths hidden from content Twig.
                        properties:
                          defaults:
                            type: array
                            items:
                              type: string
                          added:
                            type: array
                            items:
                              type: string
                          effective:
                            type: array
                            items:
                              type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # Audit trail
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # Sidebar
  # ---------------------------------------------------------------------------
  /sidebar/items:
    get:
      operationId: getSidebarItems
      tags: [Sidebar]
      summary: Get admin sidebar navigation items
      description: |
        Returns an array of sidebar navigation items contributed by installed
        plugins through the `onApiSidebarItems` event. Each item defines a
        route, label, icon, and optional badge for rendering in the admin
        sidebar. Requires `api.access`.

        Items whose `authorize` permission (a string, or an array meaning
        any-of) the caller lacks are left out, and `authorize` itself is
        removed from the response. Labels are translated into the caller's
        admin language, and items are sorted by `priority`, highest first.
      responses:
        "200":
          description: Sidebar items returned successfully.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/SidebarItem"
              example:
                data:
                  - id: dashboard
                    plugin: admin-next
                    label: Dashboard
                    icon: gauge
                    route: /dashboard
                    priority: 100
                    badge: null
                  - id: pages
                    plugin: admin-next
                    label: Pages
                    icon: file-text
                    route: /pages
                    priority: 90
                    badge: null
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # MCP
  # ---------------------------------------------------------------------------
  /mcp/tools:
    get:
      operationId: getMcpTools
      tags: [MCP]
      summary: Get the MCP tools contributed by plugins
      description: |
        Return every MCP tool the enabled plugins describe, either in an
        `mcp.yaml` manifest at the plugin root or through the `onApiMcpTools`
        event. An MCP server calls this at startup and registers one tool per
        entry.

        Tools are filtered by the caller's permissions: a tool naming a
        permission the caller does not hold is left out, tools naming no
        permission are always included, and a super admin sees everything.
        `plugins[].tools` counts what this caller can see.

        `fingerprint` hashes the enabled-plugin set plus the manifest file
        modification times, so a client can tell whether anything changed
        without diffing the list. It is also the response's `ETag`, and a
        matching `If-None-Match` gets a 304.

        Requires `api.access`.
      parameters:
        - name: If-None-Match
          in: header
          required: false
          description: A previously received `ETag`; when it still matches, the response is an empty 304.
          schema:
            type: string
          example: '"5f1d9c2a7b3e4d08"'
      responses:
        "200":
          description: Tool manifests returned successfully.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      tools:
                        type: array
                        items:
                          $ref: "#/components/schemas/McpTool"
                      plugins:
                        type: array
                        description: The contributing plugins, with the number of tools this caller can see.
                        items:
                          type: object
                          properties:
                            slug:
                              type: string
                              example: kahunacart
                            name:
                              type: string
                              nullable: true
                              example: KahunaCart
                            version:
                              type: string
                              nullable: true
                              example: 0.1.0
                            tools:
                              type: integer
                              example: 48
                      warnings:
                        type: array
                        description: One entry per manifest entry that was skipped, and why. Contains no secrets.
                        items:
                          type: string
                        example:
                          - "kahunacart: tool 'upload_image' skipped: unsupported schema keyword 'oneOf' at properties.file"
                      fingerprint:
                        type: string
                        description: Identity of the manifest set, also sent as the ETag.
                        example: 5f1d9c2a7b3e4d08
              example:
                data:
                  tools:
                    - name: kahunacart_list_products
                      plugin: kahunacart
                      title: List products
                      description: List catalog products with paging, search and status filters.
                      method: GET
                      path: /kahunacart/products
                      permission: kahunacart.products.manage
                      annotations:
                        readOnly: true
                        destructive: false
                        idempotent: true
                      input_schema:
                        type: object
                        properties:
                          q:
                            type: string
                      path_params: []
                      query: []
                  plugins:
                    - slug: kahunacart
                      name: KahunaCart
                      version: 0.1.0
                      tools: 48
                  warnings: []
                  fingerprint: 5f1d9c2a7b3e4d08
        "304":
          description: The manifest set has not changed since the ETag the client sent.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # Dashboard
  # ---------------------------------------------------------------------------
  /dashboard/notifications:
    get:
      operationId: getDashboardNotifications
      tags: [Dashboard]
      summary: Get notifications
      description: |
        Return current notifications for the dashboard, grouped by location.
        Notifications are fetched from getgrav.org and cached per user for
        `system.session.timeout` seconds; plugins can add or change them
        through the `onApiDashboardNotifications` event. Notifications the
        caller has hidden are left out (until their `reappear_after` delay
        passes). A failed fetch is silent and returns the cached copy.
        Requires `api.system.read`.
      parameters:
        - name: location
          in: query
          description: |
            Return only this location (e.g. `feed`, `dashboard-row`). The
            result still has the `{location: [...]}` structure, with an empty
            list when the location has no notifications.
          schema:
            type: string
          example: feed
        - name: force
          in: query
          description: When `true`, bypass the notification cache and fetch fresh data.
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: Notifications returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      notifications:
                        type: object
                        description: Notifications keyed by location, newest first.
                        additionalProperties:
                          type: array
                          items:
                            $ref: "#/components/schemas/Notification"
                      last_checked:
                        type: string
                        format: date-time
                        nullable: true
                        description: When the cache was last refreshed before this request, or `null` if never.
              example:
                data:
                  notifications:
                    feed:
                      - id: 214
                        date: "2026-07-31 12:00"
                        type: info
                        location: [feed]
                        message: Run a real community on your own site with **Forum Pro** for Grav 2, now available.
                        link: https://getgrav.org/premium/forum-pro
                  last_checked: "2026-09-21T08:00:00+00:00"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /dashboard/notifications/{id}/hide:
    post:
      operationId: hideNotification
      tags: [Dashboard]
      summary: Dismiss a notification
      description: |
        Mark a notification as hidden/dismissed for the calling user so it no
        longer appears in the dashboard. The hide time is recorded, so a
        notification with `reappear_after` comes back later. The ID is not
        checked against known notifications, but it must look like one: 1-64
        letters, digits, `.`, `_` or `-`, starting with a letter or digit
        (getgrav.org uses numbers, plugins use slugs such as `login-lockout`);
        anything else is 422. Hiding an already hidden notification is fine.
        Requires `api.system.write`.
      parameters:
        - name: id
          in: path
          required: true
          description: Notification ID.
          schema:
            type: string
          example: "214"
      responses:
        "204":
          description: Notification dismissed. No content returned.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /dashboard/feed:
    get:
      operationId: getDashboardFeed
      tags: [Dashboard]
      summary: Get news feed
      description: |
        Return up to 10 of the latest posts from the getgrav.org blog Atom
        feed. The feed is cached per user for `system.session.timeout`
        seconds, and a failed fetch is silent and returns the cached copy.
        Requires `api.system.read`.
      parameters:
        - name: force
          in: query
          description: When `true`, bypass the feed cache and fetch fresh data.
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: Feed items returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      feed:
                        type: array
                        items:
                          $ref: "#/components/schemas/FeedItem"
                      last_checked:
                        type: string
                        format: date-time
                        nullable: true
                        description: When the cache was last refreshed before this request, or `null` if never.
              example:
                data:
                  feed:
                    - title: Grav 2.1 Released
                      url: https://getgrav.org/blog/grav-2-1-released
                      date: "2026-09-11T12:00:00+00:00"
                      summary: ""
                  last_checked: "2026-09-21T08:00:00+00:00"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /dashboard/stats:
    get:
      operationId: getDashboardStats
      tags: [Dashboard]
      summary: Get statistics snapshot
      description: |
        Return a summary of site statistics suitable for rendering a
        dashboard overview, including available GPM updates (the update
        counts fall back to zero when GPM cannot be reached). Requires
        `api.system.read`.
      responses:
        "200":
          description: Dashboard stats returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/DashboardStats"
              example:
                data:
                  pages:
                    total: 42
                    published: 40
                  users:
                    total: 5
                  plugins:
                    total: 12
                    active: 10
                    updatable: 2
                  themes:
                    total: 2
                    updatable: 0
                    active_updatable: false
                  grav:
                    updatable: false
                  media:
                    total: 120
                  theme: quark
                  grav_version: 2.1.0
                  php_version: 8.3.14
                  last_backup: "2026-03-26T03:00:00+00:00"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /dashboard/popularity:
    get:
      operationId: getDashboardPopularity
      tags: [Dashboard]
      summary: Get page view statistics
      description: |
        Return page view statistics from the admin popularity tracker.
        Includes daily chart data (last 14 days, oldest first), summary
        counters (today, the last 7 days, this calendar month), and the top
        10 pages by total views. Requires `api.system.read`.
      responses:
        "200":
          description: Page view statistics.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      summary:
                        type: object
                        properties:
                          today:
                            type: integer
                            example: 12
                          week:
                            type: integer
                            example: 84
                          month:
                            type: integer
                            example: 312
                      chart:
                        type: array
                        items:
                          type: object
                          properties:
                            date:
                              type: string
                              example: "Mar 28"
                            views:
                              type: integer
                              example: 27
                      top_pages:
                        type: array
                        items:
                          type: object
                          properties:
                            route:
                              type: string
                              example: "/blog"
                            views:
                              type: integer
                              example: 30
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /dashboard/widgets:
    get:
      operationId: getDashboardWidgets
      tags: [Dashboard]
      summary: Get the resolved dashboard widget list
      description: |
        Returns the widgets for the Admin Next dashboard, merged from the built-in core registry, plugin contributions (`onApiDashboardWidgets`), the site default layout, and the current user's layout. Widgets whose `authorize` permission the user lacks are left out (super-admins within their API-key scope see every widget), and widgets the site layout hides are removed entirely so a user cannot re-enable them. Each widget is annotated with its effective `visible`, `size` and `order`, and the list is sorted by `order`.

        Requires `api.access`. This endpoint exists to support Admin Next.
      responses:
        "200":
          description: Widget list and layouts returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/DashboardWidgetsResult"
              example:
                data:
                  widgets:
                    - id: core.stats
                      source: core
                      label: ADMIN_NEXT.DASHBOARD.WIDGETS.STATS
                      icon: BarChart3
                      sizes: [md, lg, xl]
                      defaultSize: xl
                      priority: 100
                      visible: true
                      size: xl
                      order: 9000
                  user_layout: {}
                  site_layout: {}
                  can_edit_site: true
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /dashboard/layout:
    patch:
      operationId: updateDashboardUserLayout
      tags: [Dashboard]
      summary: Save the current user's dashboard layout
      description: |
        Saves the calling user's dashboard layout to `admin_next.dashboard` in their account YAML. Despite the PATCH verb the stored layout is replaced as a whole: send every widget entry you want kept. Entries without a string `id` are dropped, `size` values outside `xs`-`xl` are stored as `null`, and an entry without a numeric `order` is stored without one, so that widget keeps its priority-based position.

        Requires `api.access`. Demo accounts are refused. Returns the resolved widget list, the same as `GET /dashboard/widgets`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DashboardWidgetLayout"
            example:
              preset: custom
              widgets:
                - id: core.stats
                  visible: true
                  size: lg
                  order: 10
                - id: core.news-feed
                  visible: false
                  order: 20
      responses:
        "200":
          description: Layout saved; resolved widget list returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/DashboardWidgetsResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /dashboard/site-layout:
    patch:
      operationId: updateDashboardSiteLayout
      tags: [Dashboard]
      summary: Save the site-wide default dashboard layout
      description: |
        Saves the default dashboard layout for every user to `dashboard.site_layout` in `user/config/admin-next.yaml`. The stored layout is replaced as a whole. A widget marked `visible: false` here is hidden for all users and cannot be re-enabled per user; sizes and orders act as defaults that user layouts can override.

        Requires super-admin (`admin.super`, subject to the API-key scope cap). Demo accounts are refused. Returns the resolved widget list for the caller.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DashboardWidgetLayout"
      responses:
        "200":
          description: Site layout saved; resolved widget list returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/DashboardWidgetsResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /dashboard/security/exposure-probe:
    get:
      operationId: getDashboardExposureProbe
      tags: [Dashboard]
      summary: Get storage exposure probe URLs
      description: |
        Supports the Admin Next security check that tests whether private storage folders are reachable over HTTP. For each of `user://data`, `backup://` and `tmp://` that lives inside the web root, the server makes sure a sentinel file `grav-security-probe.{dat,txt,zip,json}` exists (creating it with a random hex token if missing, never overwriting an existing file or following a symlink) and returns its public URL and token.

        The browser then requests each URL through the site's real web server: if the response body matches the token, that folder and extension are publicly exposed. Folders relocated outside the web root are skipped. The top-level `url`, `token` and `available` fields repeat the first `user://data` probe for older Admin Next builds.

        Note that this GET creates files on first call. Requires `api.system.read`.
      responses:
        "200":
          description: Probe list returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/DashboardExposureProbeResult"
              example:
                data:
                  url: https://example.com/user/data/grav-security-probe.dat
                  token: 3f9c2a7e5b1d4c8a9e0f6b2d7c4a1e8f
                  available: true
                  probes:
                    - location: user/data
                      extension: dat
                      url: https://example.com/user/data/grav-security-probe.dat
                      token: 3f9c2a7e5b1d4c8a9e0f6b2d7c4a1e8f
                      available: true
                    - location: tmp
                      extension: txt
                      url: https://example.com/tmp/grav-security-probe.txt
                      token: 8b1e4d7a2c9f0e3b6a5d8c1f4e7b0a2d
                      available: true
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # GPM: Grav changelog + Admin Next extension scripts
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # Webhooks
  # ---------------------------------------------------------------------------
  /webhooks:
    get:
      operationId: listWebhooks
      tags: [Webhooks]
      summary: List webhooks
      description: |
        Return all configured outbound webhooks with their event
        subscriptions and enabled state. Secrets are masked. Requires
        `api.webhooks.read`.
      responses:
        "200":
          description: List of webhooks.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Webhook"
              example:
                data:
                  - id: wh_abc123
                    url: https://example.com/hooks/grav
                    secret: whsec_************************************************a1b2
                    events:
                      - page.created
                      - page.updated
                    enabled: true
                    headers: {}
                    created: 1774008000
                    failure_count: 0
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

    post:
      operationId: createWebhook
      tags: [Webhooks]
      summary: Create a webhook
      description: |
        Register a new outbound webhook. The response includes a
        `secret` field that can be used to verify delivery signatures.
        The secret is only returned once at creation time. Requires
        `api.webhooks.write`.

        The URL must be a well-formed `http` or `https` URL whose host
        resolves only to public addresses (private, loopback, link-local
        and reserved targets are rejected with 422), `events` must be an
        array whose every entry is one of the names listed in the `Webhook`
        schema, and `headers` must be an object of header names to string
        values. Custom headers may not use `X-Grav-Signature`,
        `X-Grav-Event` or `X-Grav-Delivery` (in any letter case) or contain
        line breaks; each of these is a 422.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [url]
              properties:
                url:
                  type: string
                  format: uri
                  description: Destination URL for webhook deliveries.
                  example: https://example.com/hooks/grav
                events:
                  type: array
                  items:
                    type: string
                  description: |
                    Event types to subscribe to. When omitted, the webhook
                    is subscribed to `*` (all events).
                  default: ["*"]
                  example: [page.created, page.updated, page.deleted]
                enabled:
                  type: boolean
                  description: Whether the webhook is active.
                  default: true
                headers:
                  type: object
                  additionalProperties:
                    type: string
                  description: |
                    Custom HTTP headers to include in deliveries. They may
                    override `Content-Type` and `User-Agent`; the signing
                    headers (`X-Grav-Signature`, `X-Grav-Event`,
                    `X-Grav-Delivery`) are always set by Grav and cannot be
                    replaced.
                  example: {}
            example:
              url: https://example.com/hooks/grav
              events: [page.created, page.updated]
              enabled: true
      responses:
        "201":
          description: Webhook created. The `secret` is returned only once.
          headers:
            Location:
              description: URL of the newly created webhook resource.
              schema:
                type: string
                example: /api/v1/webhooks/wh_abc123
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    allOf:
                      - $ref: "#/components/schemas/Webhook"
                      - type: object
                        properties:
                          secret:
                            type: string
                            description: |
                              HMAC secret for verifying delivery signatures.
                              Store it securely -- it is shown only once.
                            example: whsec_0123456789abcdef0123456789abcdef0123456789abcdef
              example:
                data:
                  id: wh_abc123
                  url: https://example.com/hooks/grav
                  secret: whsec_0123456789abcdef0123456789abcdef0123456789abcdef
                  events: [page.created, page.updated]
                  enabled: true
                  headers: {}
                  created: 1774526400
                  failure_count: 0
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /webhooks/{id}:
    get:
      operationId: getWebhook
      tags: [Webhooks]
      summary: Get a webhook
      description: Retrieve a single webhook by its ID. The secret is masked. Requires `api.webhooks.read`.
      parameters:
        - $ref: "#/components/parameters/webhookId"
      responses:
        "200":
          description: Webhook returned.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Webhook"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

    patch:
      operationId: updateWebhook
      tags: [Webhooks]
      summary: Update a webhook
      description: |
        Partially update a webhook. Only the fields included in the request
        body are modified; the secret cannot be changed and is masked in the
        response. `url`, `events` and `headers` are validated as on create
        (422).
        Requires `api.webhooks.write`.
      parameters:
        - $ref: "#/components/parameters/webhookId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                url:
                  type: string
                  format: uri
                  example: https://example.com/hooks/grav-v2
                events:
                  type: array
                  items:
                    type: string
                  example: [page.created, page.updated, page.deleted]
                enabled:
                  type: boolean
                  example: true
                headers:
                  type: object
                  additionalProperties:
                    type: string
            example:
              url: https://example.com/hooks/grav-v2
              events: [page.created, page.updated, page.deleted]
      responses:
        "200":
          description: Webhook updated.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Webhook"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

    delete:
      operationId: deleteWebhook
      tags: [Webhooks]
      summary: Delete a webhook
      description: Permanently remove a webhook and all its delivery history. Requires `api.webhooks.write`.
      parameters:
        - $ref: "#/components/parameters/webhookId"
      responses:
        "204":
          description: Webhook deleted. No content returned.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /webhooks/{id}/deliveries:
    get:
      operationId: listWebhookDeliveries
      tags: [Webhooks]
      summary: List webhook deliveries
      description: |
        Return a paginated log of deliveries for a specific webhook,
        ordered newest first. Only the 50 most recent deliveries are kept.
        Requires `api.webhooks.read`.
      parameters:
        - $ref: "#/components/parameters/webhookId"
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/per_page"
      responses:
        "200":
          description: Paginated delivery log.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/WebhookDelivery"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /webhooks/{id}/test:
    post:
      operationId: testWebhook
      tags: [Webhooks]
      summary: Send a test delivery
      description: |
        Send a test payload to the webhook's URL to verify connectivity.
        The test event type is `test`. The delivery is recorded in the
        delivery log and counts toward the failure counter like any other.
        Requires `api.webhooks.write`.
      parameters:
        - $ref: "#/components/parameters/webhookId"
      responses:
        "200":
          description: Test delivery sent and the receiver answered with a 2xx status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/WebhookDelivery"
        "502":
          description: |
            The test delivery failed (non-2xx answer or transport error). The
            body is the same delivery record, with `success: false`.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/WebhookDelivery"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # Blueprints
  # ---------------------------------------------------------------------------
  /blueprints/pages:
    get:
      operationId: listPageTypes
      tags: [Blueprints]
      summary: List available page templates
      description: |
        Returns all page template types available in the active theme.
        Requires `api.pages.read`. Templates hidden through
        `plugins.admin.hide_page_types` (or `hide_modular_page_types`) are
        left out, and plugins can adjust the list through the
        `onAdminPageTypes` / `onAdminModularPageTypes` events.
      parameters:
        - name: modular
          in: query
          required: false
          description: |
            When `1`, `true` or `yes`, return modular templates (the ones used
            as sub-pages of a modular parent) instead of regular templates.
          schema:
            type: string
            default: "false"
          example: "true"
      responses:
        "200":
          description: List of page types.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        type:
                          type: string
                          example: blog
                        label:
                          type: string
                          example: Blog
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /blueprints/pages/{template}:
    get:
      operationId: getPageBlueprint
      tags: [Blueprints]
      summary: Get resolved blueprint for a page template
      description: |
        Returns the fully resolved blueprint for the given page template.
        Inheritance via `extends@` and `import@` directives is resolved
        server-side, producing a complete field tree ready for client-side
        form rendering. Requires `api.pages.read`.

        A template with no blueprint of its own (for example one left over
        after a theme switch) falls back to the `default` page blueprint.
        Plugins can adjust the fields through the `onApiBlueprintResolved`
        event (context `page`).
      parameters:
        - name: template
          in: path
          required: true
          schema:
            type: string
          description: |
            Page template name (e.g., `default`, `blog`, `item`). Modular
            templates include their folder, sent with an unencoded slash
            (`modular/hero`).
          example: blog
      responses:
        "200":
          description: Resolved blueprint.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BlueprintResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /blueprints/plugins/{plugin}:
    get:
      operationId: getPluginBlueprint
      tags: [Blueprints]
      summary: Get blueprint for a plugin
      description: |
        Returns the configuration blueprint for the specified plugin.
        Requires `api.config.read`. Plugins can adjust the fields through the
        `onApiBlueprintResolved` event (context `plugin`).
      parameters:
        - name: plugin
          in: path
          required: true
          schema:
            type: string
          description: Plugin slug (directory name)
          example: email
      responses:
        "200":
          description: Plugin blueprint.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BlueprintResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /blueprints/themes/{theme}:
    get:
      operationId: getThemeBlueprint
      tags: [Blueprints]
      summary: Get blueprint for a theme
      description: |
        Returns the configuration blueprint for the specified theme.
        Requires `api.config.read`. Plugins can adjust the fields through the
        `onApiBlueprintResolved` event (context `theme`).
      parameters:
        - name: theme
          in: path
          required: true
          schema:
            type: string
          description: Theme slug (directory name)
          example: quark
      responses:
        "200":
          description: Theme blueprint.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BlueprintResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /blueprints/config/{scope}:
    get:
      operationId: getConfigBlueprint
      tags: [Blueprints]
      summary: Get blueprint for system configuration
      description: |
        Returns the configuration blueprint for the specified scope.
        Requires `api.config.read`. Accepts the core scopes and site-authored
        custom scopes (a top-level `user/config/<name>.yaml` with its own
        blueprint outside core); any other scope, including core-only
        blueprints such as `streams`, returns 404.
      parameters:
        - name: scope
          in: path
          required: true
          schema:
            type: string
          description: |
            Configuration scope: `system`, `site`, `media`, `security`,
            `scheduler`, `backups`, or a custom scope name.
          example: system
      responses:
        "200":
          description: Config blueprint.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BlueprintResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /blueprints/users:
    get:
      operationId: getUserBlueprint
      tags: [Blueprints]
      summary: Get user account blueprint
      description: |
        Returns the blueprint defining the form fields for user account
        editing. Requires only `api.access`, since every signed-in user needs
        it to render their own profile form. Plugins can adjust the fields
        through the `onApiBlueprintResolved` event (context `account`).
      responses:
        "200":
          description: User account blueprint.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BlueprintResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /blueprints/users/permissions:
    get:
      operationId: getUserPermissions
      tags: [Blueprints]
      summary: Get all registered permission actions
      description: |
        Returns all registered permission actions as a nested tree structure.
        This is used to render the permissions editor in the admin panel.
        Requires `api.users.read` permission.
      responses:
        "200":
          description: Permission actions tree.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    description: Top-level permission actions, each with its nested children.
                    items:
                      $ref: "#/components/schemas/PermissionAction"
              example:
                data:
                  - name: admin
                    label: Admin
                    children:
                      - name: admin.login
                        label: Login to Admin
                      - name: admin.super
                        label: Super User
                  - name: api
                    label: API
                    children:
                      - name: api.access
                        label: API Access
                      - name: api.pages
                        label: Pages
                        children:
                          - name: api.pages.read
                            label: Read
                          - name: api.pages.write
                            label: Write
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /blueprints/plugins/{plugin}/pages/{pageId}:
    get:
      operationId: getPluginPageBlueprint
      tags: [Blueprints]
      summary: Get blueprint for a plugin's custom page
      description: |
        Returns the fully resolved blueprint for a plugin's custom admin page.
        This is used to render blueprint-form type pages that plugins register
        in the admin sidebar. Inheritance via `extends@` and `import@`
        directives is resolved server-side. Requires `api.config.read`.

        The blueprint is read from the plugin's
        `admin/blueprints/<pageId>.yaml`. When that file is missing and
        `pageId` equals the plugin slug, the plugin's main `blueprints.yaml`
        is used instead. Plugins can adjust the fields through the
        `onApiBlueprintResolved` event (context `plugin-page`).
      parameters:
        - name: plugin
          in: path
          required: true
          schema:
            type: string
          description: Plugin slug (directory name)
          example: api
        - name: pageId
          in: path
          required: true
          schema:
            type: string
          description: Page identifier registered by the plugin
          example: settings
      responses:
        "200":
          description: Plugin page blueprint returned.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BlueprintResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /blueprints/config/accounts:
    get:
      operationId: getAccountsConfigBlueprint
      tags: [Blueprints]
      summary: Get the accounts configuration blueprint
      description: |
        Returns the Flex `user-accounts` directory blueprint, the form behind the Users "Configuration" tab (edited through `/config/accounts`). Requires `api.config.read`. Plugins can adjust the fields through the `onApiBlueprintResolved` event (context `config`, template `accounts`).
        Returns 404 when the Flex Objects plugin or the `user-accounts` directory is not available.
      responses:
        "200":
          description: Accounts configuration blueprint.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BlueprintResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /blueprints/groups:
    get:
      operationId: getGroupBlueprint
      tags: [Blueprints]
      summary: Get the group editing blueprint
      description: |
        Returns the resolved `user/group.yaml` blueprint used to edit an existing group. Requires `api.users.read`. Plugins can adjust the fields through the `onApiBlueprintResolved` event (context `group`).
      responses:
        "200":
          description: Group blueprint.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BlueprintResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /blueprints/groups/new:
    get:
      operationId: getGroupNewBlueprint
      tags: [Blueprints]
      summary: Get the new-group blueprint
      description: |
        Returns the resolved `user/group_new.yaml` blueprint used by the create-group form. Requires `api.users.read`. Plugins can adjust the fields through the `onApiBlueprintResolved` event (context `group_new`).
      responses:
        "200":
          description: New-group blueprint.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BlueprintResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # Config
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # Data
  # ---------------------------------------------------------------------------
  /data/resolve:
    get:
      operationId: resolveData
      tags: [Data]
      summary: Resolve blueprint data-options@ directives
      description: |
        Resolve a `data-options@` directive from a blueprint field definition.
        The `callable` parameter names a static `Class::method` to call with
        no arguments. Results are suitable for populating select fields and
        other option-based inputs. Requires `api.pages.read` permission.

        The callable must be in `Class::method` form and on core's approved
        dynamic-data provider allowlist (themes and plugins can add their
        own); anything else returns 422. An approved callable whose class or
        method does not exist returns 404, except admin-classic's
        `pagesTypes` / `pagesModularTypes`, which fall back to core's page
        type list. The callable's `key => label` result is converted to a
        list of `{value, label}` options; a non-array result becomes an
        empty list.
      parameters:
        - name: callable
          in: query
          required: true
          description: |
            The callable string from a blueprint `data-options@` directive
            (e.g. `\Grav\Common\Page\Pages::types`). A leading backslash is
            optional.
          schema:
            type: string
          example: '\Grav\Common\Page\Pages::types'
        - name: type
          in: query
          required: false
          description: |
            Page type list to return when the callable is `pageTypes` (or the
            `pagesTypes` / `pagesModularTypes` fallback).
          schema:
            type: string
            enum: [standard, modular]
            default: standard
      responses:
        "200":
          description: Resolved data returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    description: Resolved options, in the order the callable returned them.
                    items:
                      type: object
                      required: [value, label]
                      properties:
                        value:
                          type: string
                          description: The option key, cast to a string.
                        label:
                          type: string
                          description: The option label, or the key when the label is not a string.
              example:
                data:
                  - value: default
                    label: Default
                  - value: blog
                    label: Blog
                  - value: item
                    label: Item
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # Thumbnails
  # ---------------------------------------------------------------------------
  /thumbnails/{file}:
    get:
      operationId: getThumbnail
      tags: [Thumbnails]
      summary: Serve cached thumbnail
      description: |
        Serve a cached thumbnail image. This endpoint is public and does not
        require authentication. The `file` parameter is the hashed filename
        generated by the thumbnail cache system, as found in a media item's
        `thumbnail_url` or a user's `avatar_url`. Only the last path segment
        is used. Thumbnails are never generated here: a file that is not
        already cached returns 404.
      security: []
      parameters:
        - name: file
          in: path
          required: true
          description: Cached thumbnail filename (an MD5 hash plus the output extension).
          schema:
            type: string
          example: 3f2a9c1e4b5d6a7f8e9d0c1b2a394857.jpg
      responses:
        "200":
          description: Thumbnail image binary. The `Content-Type` is detected from the cached file.
          headers:
            Cache-Control:
              description: Always `public, max-age=31536000, immutable`; the filename changes when the source image does.
              schema:
                type: string
          content:
            image/avif:
              schema:
                type: string
                format: binary
            image/jpeg:
              schema:
                type: string
                format: binary
            image/png:
              schema:
                type: string
                format: binary
            image/gif:
              schema:
                type: string
                format: binary
            image/webp:
              schema:
                type: string
                format: binary
        "404":
          $ref: "#/components/responses/NotFound"

  # ---------------------------------------------------------------------------
  # Translations
  # ---------------------------------------------------------------------------
  /translations/{lang}:
    get:
      operationId: getTranslations
      tags: [System]
      summary: Get translation strings for a language
      description: |
        Returns all translation strings for the specified language as a flat
        key-value object. Supports optional prefix filtering for partial loads.
        This endpoint does not require authentication.

        Keys the requested language does not translate are filled in from
        English (`en-US`), so the map is complete for any language. The admin
        UI language is not limited to the site's configured content
        languages.
      security: []
      parameters:
        - name: lang
          in: path
          required: true
          schema:
            type: string
          description: |
            Language code (e.g., `en`, `fr`, `pt-BR`). Short codes are
            normalized to their full locale (`en` becomes `en-US`, `fr`
            becomes `fr-FR`); a value that is not a language code falls back
            to the site's default language. The resolved code is echoed back
            in `lang`.
          example: en
        - name: prefix
          in: query
          required: false
          schema:
            type: string
          description: |
            Only return keys that start with this prefix followed by a dot
            (case-insensitive), e.g. `PLUGIN_ADMIN` matches `PLUGIN_ADMIN.*`.
          example: PLUGIN_ADMIN
      responses:
        "200":
          description: Translation strings.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    required: [lang, dir, count, checksum, strings]
                    properties:
                      lang:
                        type: string
                        description: The resolved locale code.
                        example: en-US
                      dir:
                        type: string
                        enum: [ltr, rtl]
                        description: Text direction of the language.
                        example: ltr
                      count:
                        type: integer
                        description: Number of strings returned.
                        example: 1868
                      checksum:
                        type: string
                        description: MD5 hash for cache invalidation.
                        example: ca482ce9db905019b7f2be290f018553
                      strings:
                        type: object
                        additionalProperties:
                          type: string
                        description: Flat key-value map of all translation strings.
                        example:
                          PLUGIN_ADMIN.TITLE: Title
                          PLUGIN_ADMIN.CONTENT: Content

# =============================================================================
# Components
# =============================================================================

  # ---------------------------------------------------------------------------
  # me
  # ---------------------------------------------------------------------------
  /me:
    get:
      operationId: getCurrentUser
      tags: [Users]
      summary: Get the current user
      description: |
        Return the authenticated user's profile, resolved permission map and demo-mode state (the same `user` object the login endpoints return), plus the Grav and admin plugin versions. Requires `api.access`.
      responses:
        "200":
          description: Current user profile.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    allOf:
                      - $ref: "#/components/schemas/AuthUserProfile"
                      - type: object
                        properties:
                          grav_version:
                            type: string
                            example: 2.1.0
                          admin_version:
                            type: string
                            nullable: true
                            description: Version of the enabled `admin2` (or classic `admin`) plugin, or null when neither is enabled.
                            example: 2.1.13
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # Site media extras
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # menubar
  # ---------------------------------------------------------------------------
  /menubar/items:
    get:
      operationId: listMenubarItems
      tags: [Menubar]
      summary: List plugin toolbar buttons
      description: |
        Return the toolbar buttons plugins register through the `onApiMenubarItems` event, filtered to those whose `authorize` the current user satisfies. Requires `api.access`.
      responses:
        "200":
          description: Toolbar items.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/MenubarItem"
              example:
                data:
                  - id: warm-cache
                    plugin: warm-cache
                    label: Warm Cache
                    icon: fa-tachometer
                    action: warm
                    confirm: Warm the cache?
                    placement: start
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /menubar/actions/{plugin}/{action}:
    post:
      operationId: executeMenubarAction
      tags: [Menubar]
      summary: Run a toolbar action
      description: |
        Run a plugin's toolbar action through the `onApiMenubarAction` event. When a registered toolbar item matches this plugin and action, the caller must satisfy its `authorize`, or the call returns 403. An action with no matching item only needs `api.access`, and its handler is expected to check permissions itself. A handler that runs and reports a failure still returns 200, with `status: error` in the body; 404 means no plugin handled the action. Requires `api.access`.
      parameters:
        - name: plugin
          in: path
          required: true
          description: Slug of the plugin that owns the action.
          schema:
            type: string
          example: warm-cache
        - name: action
          in: path
          required: true
          description: Action key from the toolbar item.
          schema:
            type: string
          example: warm
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
              description: Free-form payload passed to the handler as `body`.
      responses:
        "200":
          description: The handler's result, passed through as returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    additionalProperties: true
                    properties:
                      status:
                        type: string
                        example: success
                      message:
                        type: string
              example:
                data:
                  status: success
                  message: Cache warmed (42 pages).
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # Pages: language tools and preview
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # blueprint-upload
  # ---------------------------------------------------------------------------
  /blueprint-upload:
    post:
      operationId: uploadBlueprintFile
      tags: [Media]
      summary: Upload a file for a blueprint file field
      description: |
        Destination-aware upload for blueprint `type: file` fields (a theme logo, a plugin asset, an avatar), mirroring classic admin's `taskFilesUpload`. Requires `api.media.write`.
        The target folder comes from the field's `destination`: a Grav stream (`theme://images`, `user://media`), a `self@:subpath` token resolved against `scope`, or a plain path relative to `user/`. Absolute paths and `..` segments are rejected.
        Every multipart file part is saved (nested file arrays are flattened). Each file is capped at 64 MB, and the field's own `filesize` can only tighten that. Extensions listed in `security.uploads_dangerous_extensions` are refused in any dot-separated part of the name (so `shell.php.jpg` fails), as are config-type extensions (`yaml`, `yml`, `json`, `twig`, `env`, `neon`, `lock`), page content (`md`, `markdown`) and stylesheets (`css`, `scss`, `sass`, `less`). Uploads into `user/accounts/` are restricted to image extensions, and uploads resolving into `user/config/` or `user/env/*/config/` are refused with 403. SVG files are sanitized after saving.
        A developer can allow page content or stylesheets for one field by listing them in that field's blueprint, for example `allow_extensions: [css]`. The server reads this from the blueprint that owns `scope` (a plugin or theme config blueprint, the page template's blueprint, or the account blueprint), never from the request: the request only names the field in `field`, and the upload's `destination` must be that field's own `destination`. `allow_extensions` can only lift `md`, `markdown`, `css`, `scss`, `sass` and `less`; dangerous and config-type extensions, the accounts image-only rule and the config directory block always apply.
        `self@:` against a `users/<username>` scope for another account needs `api.users.write` or super-admin.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [destination, file]
              properties:
                file:
                  type: string
                  format: binary
                  description: File to upload. Any field name works, and several file parts may be sent in one request.
                destination:
                  type: string
                  description: The blueprint field's `destination` value.
                  example: theme://images/logo
                scope:
                  type: string
                  description: |
                    Owner of the blueprint, used to resolve `self@:` destinations. One of `plugins/<slug>`, `themes/<slug>`, `pages/<route>`, `users/<username>`.
                  example: themes/quark
                field:
                  type: string
                  description: |
                    Full name of the blueprint field the upload belongs to, as the form knows it (`custom_css`, `header.stylesheet`). Used together with `scope` to look up the field's `allow_extensions` in the blueprint on the server. Without it, or without `scope`, page content and stylesheets are refused.
                  example: custom_css
                random_name:
                  type: boolean
                  description: Replace the file name with a random one (extension kept).
                  default: false
                avoid_overwriting:
                  type: boolean
                  description: Add a suffix instead of replacing an existing file of the same name.
                  default: false
                accept:
                  type: string
                  description: Comma-separated allowlist of extensions (`.pdf`, `*.jpg`) or MIME patterns (`image/*`). May also be sent as an array.
                  example: "image/*,.pdf"
                filesize:
                  type: number
                  description: Per-field maximum size in MB.
                  example: 5
      responses:
        "201":
          description: Files saved.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/BlueprintUploadedFile"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"
    delete:
      operationId: deleteBlueprintFile
      tags: [Media]
      summary: Delete a file saved by a blueprint file field
      description: |
        Remove a file previously saved through `POST /blueprint-upload`, plus its `.meta.yaml` sidecar when present. Requires `api.media.write`.
        `path` is the logical path the upload returned, relative to the user root (`user/` prefix optional). Absolute input, `..` and null bytes are rejected. Targets in `user/config/` or `user/env/*/config/` are refused with 403. Under `user/accounts/` only image files may be deleted, and without `api.users.write` only the caller's own avatar (403 otherwise). The same extension denylist as the upload applies, so page content (`.md`) and stylesheets can't be deleted here unless `field` and `scope` name a blueprint field that allows them with `allow_extensions` and the file sits in the folder that field's `destination` resolves to.
        The call is idempotent: a file that no longer exists returns 204.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [path]
              properties:
                path:
                  type: string
                  description: Logical path of the file, relative to the user root.
                  example: user/themes/quark/images/logo/logo.png
                field:
                  type: string
                  description: Full name of the blueprint field the file belongs to. Only consulted for page content and stylesheets, to look up the field's `allow_extensions` on the server.
                  example: custom_css
                scope:
                  type: string
                  description: Owner of the field's blueprint (`plugins/<slug>`, `themes/<slug>`, `pages/<route>`, `users/<username>`). Required with `field`.
                  example: themes/quark
      responses:
        "204":
          description: File deleted, or it was already gone.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # blueprint-files
  # ---------------------------------------------------------------------------
  /blueprint-files:
    get:
      operationId: listBlueprintFiles
      tags: [Media]
      summary: List files for a blueprint folder field
      description: |
        Read-only file listing for blueprint fields that declare a `folder:` option (filepicker, mediapicker), mirroring classic admin's `taskGetFilesInFolder`. Requires `api.media.read`.
        `folder` may be a Grav stream (`user://media`, `theme://images`, `account://`), a `self@:subpath` token resolved against `scope`, or a plain path relative to `user/`. A folder that does not exist yet returns an empty list with `meta.exists: false`.
        The page-media literals `@self` / `self@` (with no subpath) are not handled here: the endpoint returns a 422 validation error whose `errors` entry for `folder` carries the message `PAGE_MEDIA_ONLY`, telling the client to use `GET /pages/{route}/media` instead.
        The result uses the paginated envelope but is never actually paginated: every matching file is returned on page 1.
      parameters:
        - name: folder
          in: query
          required: true
          description: Folder to list (stream, `self@:` token, or path relative to `user/`).
          schema:
            type: string
          example: theme://images
        - name: scope
          in: query
          required: false
          description: Blueprint owner used to resolve `self@:` (`plugins/<slug>`, `themes/<slug>`, `pages/<route>`, `users/<username>`).
          schema:
            type: string
          example: themes/quark
        - name: accept
          in: query
          required: false
          description: Comma-separated filter of extensions (`.pdf`, `*.jpg`) or MIME patterns (`image/*`). `*` matches everything.
          schema:
            type: string
          example: "image/*"
      responses:
        "200":
          description: Files in the folder.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/MediaItem"
                      meta:
                        type: object
                        properties:
                          folder:
                            type: string
                            nullable: true
                            description: Logical folder path relative to the user root, when it can be derived.
                            example: themes/quark/images
                          scope:
                            type: string
                            nullable: true
                            example: themes/quark
                          exists:
                            type: boolean
                            description: Whether the resolved folder exists on disk.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: |
            Validation failed: missing `folder`, traversal, an unresolvable stream or scope, or a `@self` / `self@` folder (its `errors` entry for `folder` carries `PAGE_MEDIA_ONLY`).
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # Blueprints (groups, accounts config)
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # groups
  # ---------------------------------------------------------------------------
  /groups:
    get:
      operationId: listGroups
      tags: [Groups]
      summary: List user groups
      description: |
        Returns a paginated list of user groups sorted by group name. Requires `api.users.read`. Uses the Flex `user-groups` directory when it is registered and falls back to reading `groups` config otherwise.
      parameters:
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/per_page"
        - name: search
          in: query
          required: false
          description: Filter groups by text. With Flex this uses the directory's search; without it, a case-insensitive match on group name, readable name and description.
          schema:
            type: string
          example: editors
      responses:
        "200":
          description: Paginated list of groups.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/Group"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"
    post:
      operationId: createGroup
      tags: [Groups]
      summary: Create a user group
      description: |
        Adds a group to the base `user/config/groups.yaml` (visible in every environment) and clears the standard cache. Requires super-admin (`admin.super`). `enabled` defaults to true and `access` to an empty map. Fires `onApiGroupCreated`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              allOf:
                - type: object
                  required: [groupname]
                  properties:
                    groupname:
                      type: string
                      pattern: "^[a-zA-Z0-9_-]{1,200}$"
                      description: Group identifier and storage key. Cannot be changed later.
                      example: editors
                - $ref: "#/components/schemas/GroupWriteRequest"
      responses:
        "201":
          description: Group created.
          headers:
            Location:
              description: URL of the new group.
              schema:
                type: string
            X-Invalidates:
              description: Client cache tags to evict.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Group"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: A group with that name already exists.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /groups/{name}:
    parameters:
      - name: name
        in: path
        required: true
        description: Group name.
        schema:
          type: string
        example: editors
    get:
      operationId: getGroup
      tags: [Groups]
      summary: Get a user group
      description: Returns a single group. Requires `api.users.read`. The response includes an `ETag` for use with `If-Match` on PATCH.
      responses:
        "200":
          description: The group.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Group"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"
    patch:
      operationId: updateGroup
      tags: [Groups]
      summary: Update a user group
      description: |
        Updates the supplied fields of a group and saves `user/config/groups.yaml`. Requires super-admin (`admin.super`). `access` replaces the whole access map rather than merging into it. Renaming is not supported: `groupname` in the body is ignored. Send the current `ETag` in `If-Match` to detect concurrent edits. Fires `onApiGroupUpdated`.
      parameters:
        - $ref: "#/components/parameters/ifMatch"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GroupWriteRequest"
      responses:
        "200":
          description: Updated group.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
            X-Invalidates:
              description: Client cache tags to evict.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/Group"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"
    delete:
      operationId: deleteGroup
      tags: [Groups]
      summary: Delete a user group
      description: Removes a group from `user/config/groups.yaml`. Requires super-admin (`admin.super`). Accounts that still list the group are not modified. Fires `onApiGroupDeleted`.
      responses:
        "204":
          description: Group deleted.
          headers:
            X-Invalidates:
              description: Client cache tags to evict.
              schema:
                type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # Demo mode
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # demo
  # ---------------------------------------------------------------------------
  /demo/status:
    get:
      operationId: getDemoStatus
      tags: [Demo]
      summary: Get demo reset status
      description: Returns the demo-mode reset state that drives the admin banner and countdown. Any authenticated user may call it; no specific permission is needed.
      responses:
        "200":
          description: Demo status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/DemoStatus"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /demo/baseline:
    post:
      operationId: captureDemoBaseline
      tags: [Demo]
      summary: Capture the demo baseline
      description: |
        Snapshots the configured writable demo resources as the baseline that resets restore. Requires super-admin (`admin.super`). Demo accounts are always refused, even when they are super. When no writable resources are configured nothing is captured and `captured` is false (still 200). Fires `onApiDemoBaselineCaptured` on success.
      responses:
        "200":
          description: Capture result.
          headers:
            X-Invalidates:
              description: "`demo:status` when a baseline was captured."
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      captured:
                        type: boolean
                      message:
                        type: string
                        example: Demo baseline captured.
                      status:
                        $ref: "#/components/schemas/DemoStatus"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /demo/reset:
    post:
      operationId: resetDemo
      tags: [Demo]
      summary: Reset demo content to the baseline
      description: |
        Restores the demo content from the captured baseline, waiting for any background reset already in progress. Requires super-admin (`admin.super`). Demo accounts are always refused, even when they are super. Without a baseline nothing happens and `reset` is false (still 200). Fires `onApiDemoReset`.
      responses:
        "200":
          description: Reset result.
          headers:
            X-Invalidates:
              description: Always `demo:status`.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      reset:
                        type: boolean
                      message:
                        type: string
                        example: Demo content reset to baseline.
                      status:
                        $ref: "#/components/schemas/DemoStatus"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # System
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # admin
  # ---------------------------------------------------------------------------
  /admin/languages:
    get:
      operationId: listAdminLanguages
      tags: [Languages]
      summary: List admin interface languages
      description: |
        Returns the locales the admin interface itself can be shown in, one per `languages/*.yaml` file shipped by the admin2 plugin (as opposed to `GET /languages`, which lists the site's content languages). Requires `api.system.read`. Sorted by native name; an empty list when admin2 is not installed.
      responses:
        "200":
          description: Admin interface languages.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      languages:
                        type: array
                        items:
                          type: object
                          properties:
                            code:
                              type: string
                              example: fr
                            name:
                              type: string
                              description: English name, or the code when unknown.
                              example: French
                            native_name:
                              type: string
                              description: Name in the language itself, or the code when unknown.
                              example: Français
                            rtl:
                              type: boolean
                              description: Whether the language is written right to left.
                          required: [code, name, native_name, rtl]
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # audit
  # ---------------------------------------------------------------------------
  /audit/status:
    get:
      operationId: getAuditStatus
      tags: [Audit]
      summary: Get audit trail status
      description: |
        Whether the audit trail is available and enabled, with its detail level and retention settings. The admin uses it to decide whether to show the Audit tab. Works even when the trail is disabled.

        Requires super admin (`api.super`); a scoped API key must carry the `admin.super` scope, the same as every other super-only endpoint. Demo accounts are rejected, since the log holds other visitors' IPs and user agents.
      responses:
        "200":
          description: Status returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      enabled:
                        type: boolean
                        description: True only when `plugins.api.audit.enabled` is on and SQLite is available.
                        example: true
                      available:
                        type: boolean
                        description: Whether the `pdo_sqlite` extension is loaded.
                        example: true
                      coverage:
                        type: string
                        enum: [standard, detailed]
                        description: "`detailed` also stores field-level before/after values for page edits."
                        example: standard
                      retention:
                        type: object
                        properties:
                          days:
                            type: integer
                            description: Entries older than this are pruned; 0 keeps them indefinitely.
                            example: 90
                          max_rows:
                            type: integer
                            description: Hard cap on stored entries; 0 means no cap.
                            example: 100000
                      total:
                        type: integer
                        nullable: true
                        description: Number of stored events. Null when disabled or the count failed.
                        example: 1284
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /audit/events:
    get:
      operationId: listAuditEvents
      tags: [Audit]
      summary: List audit events
      description: |
        Paginated audit events, newest first, with optional filters. The trail records API-layer activity, which covers everything done through Admin Next plus API-key clients; classic admin actions that bypass the API are not recorded.

        `per_page` defaults to 50. The pagination `links` carry the active filters along with `page` and `per_page`.

        Requires super admin (`api.super`); a scoped API key needs the `admin.super` scope. Demo accounts are rejected. Returns 404 when the trail is disabled and 503 when SQLite is missing.
      parameters:
        - $ref: "#/components/parameters/page"
        - name: per_page
          in: query
          description: Events per page. Defaults to 50, capped at `plugins.api.pagination.max_per_page` (default 1000).
          schema:
            type: integer
            minimum: 1
            default: 50
        - $ref: "#/components/parameters/AuditFilterEvent"
        - $ref: "#/components/parameters/AuditFilterActor"
        - $ref: "#/components/parameters/AuditFilterTargetType"
        - $ref: "#/components/parameters/AuditFilterSeverity"
        - $ref: "#/components/parameters/AuditFilterFrom"
        - $ref: "#/components/parameters/AuditFilterTo"
        - $ref: "#/components/parameters/AuditFilterQuery"
      responses:
        "200":
          description: Events returned.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/AuditEvent"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/AuditDisabled"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "503":
          $ref: "#/components/responses/AuditUnavailable"

  /audit/facets:
    get:
      operationId: getAuditFacets
      tags: [Audit]
      summary: Get audit filter options
      description: |
        Distinct event codes and actors found in the log, for the filter drop-downs.

        Requires super admin (`api.super`); a scoped API key needs the `admin.super` scope. Demo accounts are rejected. Returns 404 when the trail is disabled and 503 when SQLite is missing.
      responses:
        "200":
          description: Facets returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      events:
                        type: array
                        items:
                          type: string
                        example: [config.update, page.update, user.login, user.login.failed]
                      actors:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              nullable: true
                              example: admin
                            name:
                              type: string
                              nullable: true
                              example: Admin User
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/AuditDisabled"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "503":
          $ref: "#/components/responses/AuditUnavailable"

  /audit/export:
    get:
      operationId: exportAuditLog
      tags: [Audit]
      summary: Download the audit log
      description: |
        Downloads every event matching the filters (newest first, capped at 100,000 rows) as an attachment, not wrapped in the JSON envelope. CSV is the default; `format=json` returns a bare JSON array of events. Any other `format` value falls back to CSV.

        CSV columns: `id, ts, event, severity, actor_id, actor_name, actor_roles, auth_method, ip, user_agent, target_type, target_id, status, context`. Array values are JSON-encoded, and cells starting with `=`, `+`, `-`, `@`, tab or CR are prefixed with `'` to stop spreadsheet formula execution.

        Sent with `Cache-Control: no-store`. Requires super admin (`api.super`); a scoped API key needs the `admin.super` scope. Demo accounts are rejected. Returns 404 when the trail is disabled and 503 when SQLite is missing.
      parameters:
        - name: format
          in: query
          required: false
          schema:
            type: string
            enum: [csv, json]
            default: csv
        - $ref: "#/components/parameters/AuditFilterEvent"
        - $ref: "#/components/parameters/AuditFilterActor"
        - $ref: "#/components/parameters/AuditFilterTargetType"
        - $ref: "#/components/parameters/AuditFilterSeverity"
        - $ref: "#/components/parameters/AuditFilterFrom"
        - $ref: "#/components/parameters/AuditFilterTo"
        - $ref: "#/components/parameters/AuditFilterQuery"
      responses:
        "200":
          description: The log file, as `audit-log.csv` or `audit-log.json`.
          headers:
            Content-Disposition:
              description: "`attachment; filename=\"audit-log.csv\"` or `attachment; filename=\"audit-log.json\"`."
              schema:
                type: string
          content:
            text/csv:
              schema:
                type: string
              example: |
                id,ts,event,severity,actor_id,actor_name,actor_roles,auth_method,ip,user_agent,target_type,target_id,status,context
                42,1758441600000,page.update,info,admin,"Admin User","[""admin""]",jwt,127.0.0.1,Mozilla/5.0,page,/blog/my-first-post,,
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AuditEvent"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/AuditDisabled"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "503":
          $ref: "#/components/responses/AuditUnavailable"

  # ---------------------------------------------------------------------------
  # Translations editor (/i18n)
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # i18n
  # ---------------------------------------------------------------------------
  /i18n/sources:
    get:
      operationId: listI18nSources
      tags: [Translations Editor]
      summary: List translation sources
      description: |
        Every source (Grav core, plugins, themes, and this site's own overrides) that ships at least one string in the requested language, with the key namespaces it contributes. Drives the editor's browse pane.

        Requires `api.translations.read`.
      parameters:
        - name: lang
          in: query
          required: false
          description: Language to count keys in. Defaults to the editor's default language (the site default if any source ships it, else `en`, else the first available).
          schema:
            type: string
            pattern: "^[a-zA-Z]{2,3}(-[a-zA-Z0-9]{2,4})?$"
          example: en
      responses:
        "200":
          description: Sources returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      lang:
                        type: string
                        example: en
                      providers:
                        type: array
                        items:
                          type: object
                          properties:
                            id:
                              type: string
                              description: "`system:core`, `plugin:<slug>`, `theme:<slug>` or `user:overrides`."
                              example: theme:quark
                            kind:
                              type: string
                              enum: [system, plugin, theme, user]
                              example: theme
                            slug:
                              type: string
                              example: quark
                            label:
                              type: string
                              example: Quark
                            enabled:
                              type: boolean
                              description: For plugins, whether the plugin is enabled; for themes, whether it is the active theme; always true for core and site overrides.
                              example: true
                            key_count:
                              type: integer
                              example: 42
                            namespaces:
                              type: array
                              items:
                                type: object
                                properties:
                                  name:
                                    type: string
                                    example: THEME_QUARK
                                  key_count:
                                    type: integer
                                    example: 42
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /i18n/languages:
    get:
      operationId: listI18nLanguages
      tags: [Translations Editor]
      summary: List translation languages
      description: |
        Every language code that any source ships, flagged with whether this site has an override file for it, plus the editor's default language.

        Requires `api.translations.read`.
      responses:
        "200":
          description: Languages returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      default:
                        type: string
                        example: en
                      languages:
                        type: array
                        items:
                          type: object
                          properties:
                            code:
                              type: string
                              example: fr
                            has_overrides:
                              type: boolean
                              example: false
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /i18n/coverage:
    get:
      operationId: getI18nCoverage
      tags: [Translations Editor]
      summary: Get translation coverage per language
      description: |
        For every language, how many of the source language's keys it has (shipped or overridden) and how many are missing. Measured against the source language's key set only, so stale keys that exist only in an old translation do not count.

        Requires `api.translations.read`.
      parameters:
        - $ref: "#/components/parameters/I18nSourceLang"
      responses:
        "200":
          description: Coverage returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      source_lang:
                        type: string
                        example: en
                      coverage:
                        type: array
                        items:
                          type: object
                          properties:
                            code:
                              type: string
                              example: fr
                            total:
                              type: integer
                              description: Number of keys in the source language.
                              example: 1868
                            translated:
                              type: integer
                              example: 1702
                            missing:
                              type: integer
                              example: 166
                            overridden:
                              type: integer
                              description: Number of site overrides in this language (any key, not only source keys).
                              example: 3
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /i18n/keys:
    get:
      operationId: listI18nKeys
      tags: [Translations Editor]
      summary: List translation keys
      description: |
        The editor matrix: one row per key, sorted by key, with the source-language value and a cell per requested language. Keys that only exist as site overrides (typos, strings from removed plugins) are included so they can be fixed.

        `q` matches both the key and the source-language value, case-insensitively. `status` is applied to the composed row: a row matches when any requested language's cell has that state; `unknown` matches keys no source ships.

        `per_page` is capped at 500. Requires `api.translations.read`.
      parameters:
        - $ref: "#/components/parameters/I18nSourceLang"
        - name: langs
          in: query
          required: false
          description: Comma-separated language codes to include as cells. Codes no source ships are ignored; defaults to the source language alone.
          schema:
            type: string
          example: en,fr,de
        - name: q
          in: query
          required: false
          description: Search text, matched against the key and the source value.
          schema:
            type: string
          example: search
        - name: provider
          in: query
          required: false
          description: Limit to keys shipped by one source id (see `GET /i18n/sources`).
          schema:
            type: string
          example: theme:quark
        - name: namespace
          in: query
          required: false
          description: Limit to keys equal to or under this dotted prefix.
          schema:
            type: string
          example: PLUGIN_ADMIN
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [all, shipped, overridden, missing, unknown]
            default: all
        - $ref: "#/components/parameters/page"
        - name: per_page
          in: query
          description: Rows per page. Capped at 500.
          schema:
            type: integer
            minimum: 1
            maximum: 500
            default: 20
      responses:
        "200":
          description: Rows returned.
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/PaginatedEnvelope"
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: "#/components/schemas/I18nKeyRow"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /i18n/keys/{key}:
    get:
      operationId: getI18nKey
      tags: [Translations Editor]
      summary: Get one translation key
      description: |
        One key across every language any source ships. An unknown key still returns 200, with `known: false` and `missing` cells.

        Requires `api.translations.read`.
      parameters:
        - name: key
          in: path
          required: true
          description: Dotted translation key. Only letters, digits, `_`, `.` and `-` are routed; anything else is a 404.
          schema:
            type: string
            pattern: "^[A-Za-z0-9_.\\-]+$"
          example: PLUGIN_ADMIN.TITLE
        - $ref: "#/components/parameters/I18nSourceLang"
      responses:
        "200":
          description: Key returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/I18nKeyRow"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /i18n/overrides/{lang}:
    get:
      operationId: getI18nOverrides
      tags: [Translations Editor]
      summary: Get a language's override file as YAML
      description: |
        The site's override file for one language (`user/languages/<lang>.yaml`), for the advanced YAML editor. Without `namespace` this is the raw file text (empty string when no file exists). With `namespace`, only overrides under that prefix are returned, re-dumped as nested YAML, so "edit as YAML" covers only what is on screen.

        Requires `api.translations.read`. An invalid language code returns 422, as it does for PATCH and PUT.
      parameters:
        - $ref: "#/components/parameters/I18nOverrideLang"
        - name: namespace
          in: query
          required: false
          description: Dotted key prefix to limit the output to.
          schema:
            type: string
          example: THEME_QUARK
      responses:
        "200":
          description: Overrides returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      lang:
                        type: string
                        example: fr
                      scoped:
                        type: boolean
                        description: True when `namespace` was given.
                        example: true
                      namespace:
                        type: string
                        nullable: true
                        example: THEME_QUARK
                      count:
                        type: integer
                        description: Number of overrides included.
                        example: 2
                      yaml:
                        type: string
                        example: "THEME_QUARK:\n  SEARCH: Rechercher\n"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"
    patch:
      operationId: patchI18nOverrides
      tags: [Translations Editor]
      summary: Set or remove individual overrides
      description: |
        Inline edits to one language's override file. `set` writes values; `unset` removes overrides so the shipped value shows again. A `set` whose value equals what the source ships is stored as a removal instead (reported in `reverted`), so the file only holds real differences. Keys no source ships are still written and reported in `unknown`.

        The response includes a fresh matrix row for every touched key, with a cell for this language only. Sends `X-Invalidates: i18n:update, translations:update`.

        Requires `api.translations.write`; demo accounts are rejected.
      parameters:
        - $ref: "#/components/parameters/I18nOverrideLang"
        - $ref: "#/components/parameters/I18nSourceLang"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: At least one of `set` or `unset` must be non-empty.
              properties:
                set:
                  type: object
                  additionalProperties:
                    type: string
                  description: Map of dotted key to new value. A JSON array is rejected with 422; numeric keys inside an object are ignored.
                  example:
                    THEME_QUARK.SEARCH: Rechercher
                unset:
                  type: array
                  items:
                    type: string
                  description: Keys whose override should be removed. Non-string entries are ignored.
                  example: [PLUGIN_ADMIN.TITLE]
      responses:
        "200":
          description: Overrides saved.
          headers:
            X-Invalidates:
              $ref: "#/components/headers/I18nInvalidates"
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      written:
                        type: object
                        additionalProperties:
                          type: string
                        description: Keys stored as overrides, with their values.
                      removed:
                        type: array
                        items:
                          type: string
                        description: Keys whose existing override was deleted.
                      reverted:
                        type: array
                        items:
                          type: string
                        description: Keys in `set` that matched the shipped value and so were not stored.
                      unknown:
                        type: array
                        items:
                          type: string
                        description: Keys in `set` that no source ships.
                      rows:
                        type: array
                        items:
                          $ref: "#/components/schemas/I18nKeyRow"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"
    put:
      operationId: replaceI18nOverrides
      tags: [Translations Editor]
      summary: Replace a language's overrides from YAML
      description: |
        Advanced-mode save. Parses `yaml` (a mapping, nested or dotted) and replaces the language's overrides with it. With `namespace`, only overrides under that prefix are replaced and everything else in the file is kept; a key outside the namespace fails the whole save with 422, and the error names the out-of-scope keys (up to five). Empty `yaml` deletes every override in scope.

        Values equal to the shipped value are dropped (`dropped`); keys no source ships are saved but reported (`unknown`) rather than rejected. Sends `X-Invalidates: i18n:update, translations:update`.

        Requires `api.translations.write`; demo accounts are rejected.
      parameters:
        - $ref: "#/components/parameters/I18nOverrideLang"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [yaml]
              properties:
                yaml:
                  type: string
                  description: YAML mapping of keys to strings. A YAML list is rejected.
                  example: "THEME_QUARK:\n  SEARCH: Rechercher\n"
                namespace:
                  type: string
                  description: Dotted key prefix to limit the replacement to. Omit or send an empty string to replace the whole file.
                  example: THEME_QUARK
      responses:
        "200":
          description: Overrides saved.
          headers:
            X-Invalidates:
              $ref: "#/components/headers/I18nInvalidates"
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      count:
                        type: integer
                        description: Number of overrides now stored from this YAML.
                        example: 1
                      dropped:
                        type: array
                        items:
                          type: string
                        description: Keys dropped because they matched the shipped value.
                      unknown:
                        type: array
                        items:
                          type: string
                        description: Keys saved that no source ships.
                      removed:
                        type: integer
                        description: How many fewer in-scope overrides exist than before.
                        example: 0
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /i18n/translate:
    get:
      operationId: getI18nTranslateStatus
      tags: [Translations Editor]
      summary: Check whether machine translation is available
      description: |
        Whether the AI Translate plugin can propose translations, and if not, why: it is not installed, not enabled, or has no configured provider. The editor hides its translate actions when unavailable.

        Requires `api.translations.read`.
      responses:
        "200":
          description: Status returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      available:
                        type: boolean
                        example: false
                      installed:
                        type: boolean
                        example: true
                      enabled:
                        type: boolean
                        example: true
                      reason:
                        type: string
                        nullable: true
                        enum: [not_installed, not_enabled, not_configured, null]
                        example: not_configured
                      max_keys:
                        type: integer
                        description: Maximum keys accepted by one `POST /i18n/translate`.
                        example: 200
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"
    post:
      operationId: translateI18nKeys
      tags: [Translations Editor]
      summary: Propose machine translations
      description: |
        Sends up to 200 keys' source values to the AI Translate plugin and returns proposals. Nothing is written; the caller reviews the proposals and saves them with `PATCH /i18n/overrides/{lang}`.

        Placeholders are masked before sending and checked afterwards; a proposal whose placeholders came back altered is refused (`placeholders_mangled`). ICU strings that need a human, keys with no source value, and strings with nothing translatable are skipped without calling the provider. Proposals come back in the order the keys were sent.

        Requires `api.translations.write` (it spends provider credits); demo accounts are rejected. Returns 422 when machine translation is unavailable or the provider call fails.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [target_lang, keys]
              properties:
                source_lang:
                  type: string
                  description: Defaults to the editor's default language. Must differ from `target_lang`.
                  example: en
                target_lang:
                  type: string
                  example: fr
                keys:
                  type: array
                  minItems: 1
                  maxItems: 200
                  items:
                    type: string
                  description: Keys to translate. Non-string entries are ignored.
                  example: [THEME_QUARK.SEARCH, PLUGIN_ADMIN.TITLE]
      responses:
        "200":
          description: Proposals returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      source_lang:
                        type: string
                        example: en
                      target_lang:
                        type: string
                        example: fr
                      proposals:
                        type: array
                        items:
                          type: object
                          properties:
                            key:
                              type: string
                              example: THEME_QUARK.SEARCH
                            source:
                              type: string
                              nullable: true
                              example: Search
                            value:
                              type: string
                              nullable: true
                              description: Proposed translation, or null when refused or skipped.
                              example: Rechercher
                            ok:
                              type: boolean
                              example: true
                            reason:
                              type: string
                              nullable: true
                              enum: [no_source, icu_needs_human, nothing_to_translate, provider_returned_nothing, placeholders_mangled, null]
                              example: null
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /i18n/import/translation-strings:
    get:
      operationId: getI18nImportStatus
      tags: [Translations Editor]
      summary: Preview an import from the translation-strings plugin
      description: |
        What the old translation-strings plugin has configured and what importing it would do, per language. Counts are exact; `keys` lists at most 50 keys per language. Read-only.

        Requires `api.translations.read`. Returns 422 when the plugin's config holds a language code the editor cannot store.
      responses:
        "200":
          description: Import preview returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      present:
                        type: boolean
                        description: True when the plugin has at least one override configured.
                        example: true
                      plugin_enabled:
                        type: boolean
                        description: While enabled, the plugin's values still win over `user/languages`.
                        example: true
                      config_path:
                        type: string
                        description: Absolute path of the plugin's config file. Demo accounts get `(hidden in demo mode)` instead.
                        example: /var/www/grav/user/config/plugins/translation-strings.yaml
                      pending:
                        type: integer
                        description: Keys an import would write (`new` plus `conflict`).
                        example: 4
                      total:
                        type: integer
                        example: 6
                      languages:
                        type: array
                        items:
                          type: object
                          properties:
                            code:
                              type: string
                              example: fr
                            total:
                              type: integer
                              example: 6
                            new:
                              type: integer
                              example: 3
                            already:
                              type: integer
                              description: Already overridden with the same value.
                              example: 1
                            conflict:
                              type: integer
                              description: Overridden with a different value; importing replaces it with the plugin's value.
                              example: 1
                            shipped:
                              type: integer
                              description: Same as the shipped value, so importing stores nothing.
                              example: 1
                            unknown:
                              type: integer
                              example: 0
                            keys:
                              type: array
                              maxItems: 50
                              items:
                                type: object
                                properties:
                                  key:
                                    type: string
                                    example: THEME_QUARK.SEARCH
                                  status:
                                    type: string
                                    enum: [new, already, conflict, shipped]
                                  unknown:
                                    type: boolean
                                  current:
                                    type: string
                                    nullable: true
                                    description: The site's existing override, if any.
                                  value:
                                    type: string
                                    description: The plugin's value.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"
    post:
      operationId: importI18nTranslationStrings
      tags: [Translations Editor]
      summary: Import overrides from the translation-strings plugin
      description: |
        Merges every override configured in the translation-strings plugin into `user/languages/<lang>.yaml`. Existing overrides for other keys are kept; where both name the same key, the plugin's value wins because that is what the site shows today.

        The plugin is not disabled; the client does that afterwards through the config endpoint. `plugin_enabled` reports its current state. Sends `X-Invalidates: translations:update`.

        Requires `api.translations.write`; demo accounts are rejected. Returns 422 when the plugin has nothing configured, or when its config holds a language code the editor cannot store (checked before anything is written).
      responses:
        "200":
          description: Import completed.
          headers:
            X-Invalidates:
              description: Client cache tags to refresh.
              schema:
                type: string
              example: translations:update
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      imported:
                        type: integer
                        description: Overrides written, across all languages.
                        example: 4
                      reverted:
                        type: integer
                        description: Values skipped because they matched the shipped value.
                        example: 1
                      unknown:
                        type: array
                        items:
                          type: string
                        description: Imported keys that no source ships.
                      languages:
                        type: array
                        items:
                          type: object
                          properties:
                            code:
                              type: string
                              example: fr
                            written:
                              type: integer
                              example: 4
                            reverted:
                              type: integer
                              example: 1
                            unknown:
                              type: integer
                              example: 0
                            path:
                              type: string
                              description: Absolute path of the override file written.
                              example: /var/www/grav/user/languages/fr.yaml
                      plugin_enabled:
                        type: boolean
                        example: true
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # admin-next
  # ---------------------------------------------------------------------------
  /admin-next/preferences:
    get:
      operationId: getAdminNextPreferences
      tags: [Admin Next]
      summary: Get resolved Admin Next preferences
      description: |
        Returns the full preferences payload Admin Next reads once on boot: site branding (with ready-to-use logo URLs), site defaults, site-only behavioral settings, media upload constraints, the current user's saved overrides, and the `effective` map that merges them all.

        Resolution order for user-overridable keys is built-in defaults, then site defaults (`ui.defaults`), then the user's overrides (`admin_next.preferences` in the account YAML). If the user has not picked an admin language, a classic-admin `language:` value on the account is honored. Site-only settings (`ui.settings`) are applied last and cannot be overridden per user.

        Requires `api.access`. The response carries an `ETag` header computed from the body (conditional GETs are not answered with 304).
      responses:
        "200":
          description: Resolved preferences returned.
          headers:
            ETag:
              $ref: "#/components/headers/ETag"
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/PreferencesPayload"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /admin-next/preferences/user:
    patch:
      operationId: updateAdminNextUserPreferences
      tags: [Admin Next]
      summary: Update the current user's preference overrides
      description: |
        Patches the calling user's overrides, stored under `admin_next.preferences` in their account YAML. Only the user-overridable keys (see `PreferencesValues`) are accepted; site-only keys such as `autoSaveEnabled` or `menubarLinks` are ignored here.

        Keys not present in the body are left alone. A key set to `null` removes that override so the site default applies again. Values that fail validation (unknown enum value, wrong type) are silently dropped rather than rejected; numeric values are clamped to their allowed range.

        Requires `api.access`. Demo accounts are refused. Returns the full resolved payload, the same as `GET /admin-next/preferences`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PreferencesValues"
            example:
              colorMode: dark
              accentHue: 200
              fontSize: null
      responses:
        "200":
          description: Overrides saved; resolved preferences returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/PreferencesPayload"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"
    delete:
      operationId: resetAdminNextUserPreferences
      tags: [Admin Next]
      summary: Reset the current user's preferences to site defaults
      description: |
        Removes every override the calling user has saved (the whole `admin_next.preferences` block), backing the "Reset to site defaults" action in Admin Next. Dashboard layout (`admin_next.dashboard`) is not touched.

        Requires `api.access`. Demo accounts are refused. Returns the full resolved payload.
      responses:
        "200":
          description: Overrides cleared; resolved preferences returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/PreferencesPayload"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /admin-next/preferences/site:
    patch:
      operationId: updateAdminNextSitePreferences
      tags: [Admin Next]
      summary: Update site-wide preference defaults and settings
      description: |
        Writes site-level preferences to `user/config/admin-next.yaml`. The flat body is split in two:

        User-overridable keys (`PreferencesValues`) are merged into `ui.defaults`, so only the keys sent change. Send `null` for a key to remove the saved default so it falls back to the built-in value. Invalid values are dropped.

        Site-only keys (`PreferencesSiteSettings`: auto-save, collaboration, menubar links) are merged into `ui.settings`, so only the keys sent change.

        Any other key is ignored. Requires super-admin (`admin.super`, subject to the API-key scope cap). Demo accounts are refused. Returns the full resolved payload.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              allOf:
                - $ref: "#/components/schemas/PreferencesValues"
                - $ref: "#/components/schemas/PreferencesSiteSettings"
            example:
              colorMode: ""
              accentHue: 271
              fontFamily: inter
              autoSaveEnabled: true
              menubarLinks:
                - label: Docs
                  url: https://learn.getgrav.org
                  icon: book
                  external: true
      responses:
        "200":
          description: Site preferences saved; resolved preferences returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/PreferencesPayload"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /admin-next/branding:
    patch:
      operationId: updateAdminNextBranding
      tags: [Admin Next]
      summary: Update site branding
      description: |
        Updates the site branding block (`ui.branding` in `user/config/admin-next.yaml`): logo mode, logo text, logo height, sign-in title/subtitle, and the "Powered by Grav" toggle. The body is merged over the current branding, so you can send just the fields you want to change. Every field is normalized (unknown modes fall back to `default`, text is trimmed and truncated, `logoHeight` is clamped to 16-44 or 0).

        Logo and favicon files are managed with `POST`/`DELETE /admin-next/branding/logo`; the `logoLight`, `logoDark` and `favicon` fields here accept only a basename inside `user/media/admin-next/`.

        Requires super-admin (`admin.super`, subject to the API-key scope cap). Demo accounts are refused. Returns the full resolved payload.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BrandingSettings"
            example:
              mode: text
              text: Acme CMS
              title: Acme Content Admin
              showPoweredBy: false
      responses:
        "200":
          description: Branding saved; resolved preferences returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/PreferencesPayload"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /admin-next/branding/logo:
    post:
      operationId: uploadAdminNextBrandingLogo
      tags: [Admin Next]
      summary: Upload a logo or favicon
      description: |
        Uploads a light logo, dark logo, or favicon to `user/media/admin-next/` and records it in the site branding. The file type is determined from the file contents, never the client-declared MIME type: PNG, JPEG, WebP and ICO are accepted as raster images; anything else must parse as an SVG document and is sanitized (script and foreignObject elements, `on*` attributes, and `javascript:`/`data:` URIs are removed). Maximum size is 4 MB.

        The stored file is named `logo-{variant}-{hash}.{ext}` (or `favicon-favicon-{hash}.{ext}`), and the previous file for that variant is deleted. Uploading a light or dark logo switches the branding mode to `custom` unless it is currently `text`.

        Requires super-admin (`admin.super`, subject to the API-key scope cap). Demo accounts are refused.
      parameters:
        - $ref: "#/components/parameters/BrandingLogoVariant"
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
                  description: The image file. The field may also be named `logo`.
                variant:
                  type: string
                  enum: [light, dark, favicon]
                  description: Alternative to the `variant` query parameter; the query parameter wins when both are sent.
              required: [file]
      responses:
        "201":
          description: Logo stored; resolved preferences returned (see `branding_urls` for the new file's URL).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/PreferencesPayload"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Missing or invalid `variant`, no file uploaded, file larger than 4 MB, or content that is neither a supported raster image nor a valid SVG.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 422
                title: Unprocessable Entity
                detail: Logo must be a valid SVG image.
        "429":
          $ref: "#/components/responses/TooManyRequests"
    delete:
      operationId: deleteAdminNextBrandingLogo
      tags: [Admin Next]
      summary: Delete a logo or favicon
      description: |
        Deletes the stored file for one branding variant and clears its path in the site branding. If both logo variants end up empty while the mode is `custom`, the mode reverts to `default` so Admin Next shows the built-in Grav logo. Succeeds even when no file was set for the variant.

        Requires super-admin (`admin.super`, subject to the API-key scope cap). Demo accounts are refused. Returns the full resolved payload.
      parameters:
        - $ref: "#/components/parameters/BrandingLogoVariant"
      responses:
        "200":
          description: Variant cleared; resolved preferences returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/PreferencesPayload"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Missing or invalid `variant`.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 422
                title: Unprocessable Entity
                detail: "Query parameter 'variant' must be 'light', 'dark', or 'favicon'."
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # Dashboard: widgets, layouts, security probe
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # custom-fields
  # ---------------------------------------------------------------------------
  /custom-fields:
    get:
      operationId: getCustomFieldRegistry
      tags: [UI Extensions]
      summary: List custom field types provided by plugins and themes
      description: |
        Returns a map of custom blueprint field type to the package that provides its Admin Next web component, so Admin Next can fill its field registry at startup. Enabled plugins and all installed themes are scanned for `admin-next/fields/*.js`. When two packages ship the same type, the later one wins (themes are scanned after plugins).

        Use `kind` and `slug` to build the script URL: `/gpm/{kind}/{slug}/field/{type}` or the bundle at `/gpm/{kind}/{slug}/fields`. When nothing is registered the map is serialized as an empty array (`[]`).

        Requires `api.access`, the same as the field scripts it points to: Admin Next loads it at startup for every account, including editors who cannot manage packages.
      responses:
        "200":
          description: Field registry returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    additionalProperties:
                      $ref: "#/components/schemas/CustomFieldProvider"
              example:
                data:
                  seo-magic-preview:
                    slug: seo-magic
                    kind: plugins
                  color-swatch:
                    slug: quark
                    kind: themes
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # editor
  # ---------------------------------------------------------------------------
  /editor/toolbar-buttons:
    get:
      operationId: getEditorToolbarButtons
      tags: [UI Extensions]
      summary: List markdown editor toolbar buttons contributed by plugins
      description: |
        Collects buttons that plugins add to the Admin Next default (CodeMirror) markdown editor toolbar through the `onApiMarkdownEditorButtons` event. Buttons whose `authorize` permission the user lacks are removed, and the `authorize` field itself is stripped from the output. Buttons without `authorize` are visible to every authenticated user. The list is returned in event order.

        Requires `api.access`.
      responses:
        "200":
          description: Toolbar buttons returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/EditorToolbarButton"
              example:
                data:
                  - id: youtube
                    plugin: youtube
                    label: YouTube Video
                    icon: "<svg viewBox=\"0 0 24 24\">...</svg>"
                    modal:
                      component: youtube-insert
                      title: Insert YouTube Video
                      size: md
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # floating-widgets
  # ---------------------------------------------------------------------------
  /floating-widgets:
    get:
      operationId: getFloatingWidgets
      tags: [UI Extensions]
      summary: List floating widgets contributed by plugins
      description: |
        Collects persistent floating widgets (chat assistants, notification panels and the like) that plugins register for the Admin Next shell through the `onApiFloatingWidgets` event. Widgets whose `authorize` permission the user lacks are removed and `authorize` is stripped. `routes` is normalized to a de-duplicated list of leading-slash paths and dropped when empty. The list is returned in event order.

        The widget's web component is loaded from `GET /gpm/plugins/{plugin}/widget-script`. Requires `api.access`.
      responses:
        "200":
          description: Floating widgets returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/FloatingWidget"
              example:
                data:
                  - id: ai-pro-chat
                    plugin: ai-pro
                    label: AI Assistant
                    icon: bot
                    priority: 10
                    autoLoad: true
                    routes: [/pages]
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # context-panels
  # ---------------------------------------------------------------------------
  /context-panels:
    get:
      operationId: getContextPanels
      tags: [UI Extensions]
      summary: List page-editor context panels contributed by plugins
      description: |
        Collects slide-in panels that plugins register for the Admin Next page editor through the `onApiContextPanels` event. Each panel adds a trigger button to the editor toolbar in the listed `contexts`, and its web component is loaded from `GET /gpm/plugins/{plugin}/panel-script`.

        A panel may declare `authorize` (a permission string, or an array where any one grants access); panels the caller doesn't pass are left out, and `authorize` is stripped from the response. The list is sorted by `priority`, highest first, keeping registration order among equal priorities. Requires `api.access`.
      responses:
        "200":
          description: Context panels returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/ContextPanel"
              example:
                data:
                  - id: revisions-pro
                    plugin: revisions-pro
                    label: Revision History
                    icon: history
                    contexts: [pages]
                    priority: 10
                    width: 900
                    badgeEndpoint: /revisions-pro/badge
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # settings
  # ---------------------------------------------------------------------------
  /settings/panels:
    get:
      operationId: getSettingsPanels
      tags: [UI Extensions]
      summary: List Settings page panels contributed by plugins
      description: |
        Collects blueprint-driven panels that plugins register through the `onApiAdminSettingsPanels` event. Admin Next renders them as sections inside its Settings page instead of as separate sidebar entries. Panels are sorted by `priority`, highest first. No permission filtering happens here; the registering plugin decides, using the `user` passed in the event, whether to add a panel.

        Requires `api.access`.
      responses:
        "200":
          description: Settings panels returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/SettingsPanel"
              example:
                data:
                  - id: login-settings
                    plugin: api
                    label: Login & Security
                    description: Authentication, sessions and 2FA
                    icon: fa-shield-alt
                    blueprint: login-settings
                    data_endpoint: /login-settings/data
                    save_endpoint: /login-settings/save
                    priority: 0
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  # ---------------------------------------------------------------------------
  # Users: invitations
  # ---------------------------------------------------------------------------

  # ---------------------------------------------------------------------------
  # invitations
  # ---------------------------------------------------------------------------
  /invitations:
    get:
      operationId: listInvitations
      tags: [Users]
      summary: List pending invitations
      description: |
        Returns all pending user invitations, most recent first. Expired invitations are purged from the store before listing. The pre-set `access` map is not included, but each entry carries its `token`, which is the credential the invitee uses to accept.

        Requires `api.users.write`, like creating, resending and revoking: the token alone accepts the invite, so read-only users cannot list them.
      responses:
        "200":
          description: Invitations returned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: object
                    properties:
                      invitations:
                        type: array
                        items:
                          $ref: "#/components/schemas/Invitation"
              example:
                data:
                  invitations:
                    - token: 9c1f0e6b2a7d4c8e5f3a1b9d0c7e2f4a
                      email: jane@example.com
                      fullname: Jane Doe
                      groups: [editors]
                      created: 1790000000
                      created_by: admin
                      created_by_name: Site Admin
                      expires: 1790604800
                      expired: false
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"
    post:
      operationId: createInvitation
      tags: [Users]
      summary: Invite a new user
      description: |
        Creates a time-limited invitation for a new account with pre-set permissions and groups, and emails the invite link when the Email plugin is configured. The recipient accepts through the public `/auth/invite/{token}` endpoints and picks their own username and password; the account gets exactly the access set here.

        Super flags (`admin.super`, `api.super`, and scalar `admin`/`api` grants) are stripped from `access`, and `groups` is ignored, unless the caller is a super-admin within their API-key scope. Any earlier pending invite for the same email is replaced.

        If email is not configured or sending fails, the invite is still created and the response includes `link` and a `warning` so the admin can share it manually. The link host is taken from `admin_base_url`, the Referer or the Origin header only when that origin is the site's own or listed in the API's CORS origins.

        Requires `api.users.write`. Demo accounts are refused. The response sets `X-Invalidates: invitations:list`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InvitationCreateRequest"
            example:
              email: jane@example.com
              fullname: Jane Doe
              access:
                api:
                  access: true
                  pages:
                    read: true
                    write: true
              groups: [editors]
              expiration: 604800
              message: Welcome aboard!
              admin_base_url: https://example.com/admin
      responses:
        "201":
          description: Invitation created.
          headers:
            Location:
              description: Points at `/auth/invite/{token}`, the public endpoint that validates the new invitation.
              schema:
                type: string
            X-Invalidates:
              description: Client cache tags to evict (`invitations:list`).
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/InvitationCreated"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: A user with that email already exists.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 409
                title: Conflict
                detail: A user with that email already exists.
        "422":
          $ref: "#/components/responses/ValidationError"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /invitations/{token}:
    delete:
      operationId: deleteInvitation
      tags: [Users]
      summary: Revoke an invitation
      description: |
        Deletes a pending invitation so its link no longer works. Requires `api.users.write`. Demo accounts are refused. The response sets `X-Invalidates: invitations:list`.
      parameters:
        - $ref: "#/components/parameters/InvitationToken"
      responses:
        "204":
          description: Invitation revoked.
          headers:
            X-Invalidates:
              description: Client cache tags to evict (`invitations:list`).
              schema:
                type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: No invitation with that token.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 404
                title: Not Found
                detail: Invitation not found.
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /invitations/{token}/resend:
    post:
      operationId: resendInvitation
      tags: [Users]
      summary: Resend an invitation email
      description: |
        Sends the invitation email again for a pending, unexpired invite. The token and expiry are unchanged. Unlike `POST /invitations`, this fails with 422 when email is not configured. The link host follows the same allowlist rules as invitation creation.

        Requires `api.users.write`. Demo accounts are refused.
      parameters:
        - $ref: "#/components/parameters/InvitationToken"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
                  description: Optional personal note included in the email.
                admin_base_url:
                  type: string
                  format: uri
                  description: Admin Next base URL used to build the link; accepted only for the site's own origin or a configured CORS origin.
      responses:
        "200":
          description: Email sent.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    $ref: "#/components/schemas/InvitationCreated"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: No pending invitation with that token, or it has expired.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 404
                title: Not Found
                detail: Invitation not found or expired.
        "422":
          description: Email is not configured on this site.
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              example:
                status: 422
                title: Unprocessable Entity
                detail: Email is not configured. Share the invite link manually.
        "429":
          $ref: "#/components/responses/TooManyRequests"
components:
  # ---------------------------------------------------------------------------
  # Security Schemes
  # ---------------------------------------------------------------------------
  securitySchemes:
    apiToken:
      type: apiKey
      in: header
      name: X-API-Token
      description: |
        Recommended transport for JWT access tokens obtained from `POST /auth/token`.
        Uses a custom header so the token survives FastCGI / PHP-FPM / CGI environments
        (MAMP, shared hosts) that strip the standard `Authorization` header before it
        reaches PHP. Accepts either a bare JWT or the `Bearer <jwt>` form.

    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        Alternative transport for JWT access tokens — standard HTTP Bearer semantics.
        Works on most hosts but may be stripped by some FastCGI / CGI setups; prefer
        `X-API-Token` when portability across hosts matters.

    apiKey:
      type: apiKey
      in: header
      name: X-API-Key
      description: API key generated via `POST /users/{username}/api-keys`.

    sessionAuth:
      type: apiKey
      in: cookie
      name: grav-site
      description: Session cookie set by the Grav admin panel login.

  # ---------------------------------------------------------------------------
  # Reusable Parameters
  # ---------------------------------------------------------------------------
  parameters:
    page:
      name: page
      in: query
      description: Page number for pagination (1-based).
      schema:
        type: integer
        minimum: 1
        default: 1

    per_page:
      name: per_page
      in: query
      description: Number of items per page. Capped at `pagination.max_per_page` in the plugin config (default 1000); values below 1 count as 1.
      schema:
        type: integer
        minimum: 1
        default: 20

    pageRoute:
      name: route
      in: path
      required: true
      description: |
        The page route without leading slash (e.g. `blog/my-post`). Supports
        nested routes with `/` separators.
      schema:
        type: string
      example: blog/my-first-post

    mediaFilename:
      name: filename
      in: path
      required: true
      description: Name of the media file.
      schema:
        type: string
      example: hero-image.jpg

    mediaMetaPath:
      name: path
      in: query
      required: true
      description: |
        Path of the site media file relative to the media root
        (e.g. `photos/beach.jpg`).
      schema:
        type: string
      example: photos/beach.jpg

    configScope:
      name: scope
      in: path
      required: true
      description: |
        Configuration scope. One of: `system`, `site`, `media`, `security`,
        `scheduler`, `backups`, `plugins/{name}`, `themes/{name}`, or the
        name of a site-authored `user/config/<name>.yaml` file. The slash in
        `plugins/{name}` and `themes/{name}` is sent unencoded.
      schema:
        type: string
      examples:
        system:
          value: system
          summary: System configuration
        site:
          value: site
          summary: Site configuration
        plugin:
          value: plugins/api
          summary: Plugin configuration
        theme:
          value: themes/quark
          summary: Theme configuration

    username:
      name: username
      in: path
      required: true
      description: The username of the user account.
      schema:
        type: string
        pattern: "^[a-zA-Z0-9_-]{3,64}$"
      example: admin

    ifMatch:
      name: If-Match
      in: header
      required: false
      description: |
        ETag value from the most recent GET response. When provided, the
        server returns `409 Conflict` if the resource has changed since
        the ETag was generated.
      schema:
        type: string
      example: '"a3f8b2c1d4e5f6a7b8c9d0e1f2a3b4c5"'

    lang:
      name: lang
      in: query
      required: false
      description: |
        Two-letter language code. When provided, the request operates on
        the page translation for that language. If omitted, the default
        site language is used.
      schema:
        type: string
      example: en

    webhookId:
      name: id
      in: path
      required: true
      description: Unique identifier of the webhook.
      schema:
        type: string
      example: wh_abc123

  # ---------------------------------------------------------------------------
  # Reusable Headers
  # ---------------------------------------------------------------------------

    BackupFilename:
      name: filename
      in: path
      required: true
      description: Backup archive file name (a plain `.zip` name, no path).
      schema:
        type: string
        pattern: "^[^/\\\\]+\\.zip$"
      example: grav-backup-2026-03-22T09-30-00.zip

    AuditFilterEvent:
      name: event
      in: query
      required: false
      description: Exact event code.
      schema:
        type: string
      example: page.update
    AuditFilterActor:
      name: actor
      in: query
      required: false
      description: Exact actor id or actor name.
      schema:
        type: string
      example: admin
    AuditFilterTargetType:
      name: target_type
      in: query
      required: false
      description: Exact target type (for example `page`, `media`, `user`, `group`, `config`, `package`).
      schema:
        type: string
      example: page
    AuditFilterSeverity:
      name: severity
      in: query
      required: false
      description: Exact severity. Recorded events use `info`, `notice` or `warning`.
      schema:
        type: string
      example: warning
    AuditFilterFrom:
      name: from
      in: query
      required: false
      description: Earliest timestamp, inclusive, in Unix epoch milliseconds.
      schema:
        type: integer
        format: int64
      example: 1758000000000
    AuditFilterTo:
      name: to
      in: query
      required: false
      description: Latest timestamp, inclusive, in Unix epoch milliseconds.
      schema:
        type: integer
        format: int64
      example: 1758441600000
    AuditFilterQuery:
      name: q
      in: query
      required: false
      description: Case-insensitive substring match against actor name, target id, event code and IP.
      schema:
        type: string
      example: blog
    I18nSourceLang:
      name: source_lang
      in: query
      required: false
      description: Language used as the reference. An invalid or missing code falls back to the editor's default language (the site default if any source ships it, else `en`, else the first available).
      schema:
        type: string
        pattern: "^[a-zA-Z]{2,3}(-[a-zA-Z0-9]{2,4})?$"
      example: en
    I18nOverrideLang:
      name: lang
      in: path
      required: true
      description: Language code of the override file (`user/languages/<lang>.yaml`).
      schema:
        type: string
        pattern: "^[a-zA-Z]{2,3}(-[a-zA-Z0-9]{2,4})?$"
      example: fr

    BrandingLogoVariant:
      name: variant
      in: query
      required: false
      description: Which branding file to act on. Required unless sent as a `variant` form/body field instead.
      schema:
        type: string
        enum: [light, dark, favicon]
      example: light

    ExtensionScriptPluginSlug:
      name: slug
      in: path
      required: true
      description: Installed plugin slug. Must not contain `/`, `\` or be `.`/`..`.
      schema:
        type: string
      example: seo-magic

    ExtensionScriptThemeSlug:
      name: slug
      in: path
      required: true
      description: Installed theme slug. Must not contain `/`, `\` or be `.`/`..`.
      schema:
        type: string
      example: quark

    ExtensionScriptFieldType:
      name: type
      in: path
      required: true
      description: Custom field type; the file `admin-next/fields/{type}.js` is served. Only the basename is used.
      schema:
        type: string
      example: seo-magic-preview

    ExtensionScriptIfNoneMatch:
      name: If-None-Match
      in: header
      required: false
      description: ETag from an earlier response. When it matches (or is `*`), the server answers 304 without a body.
      schema:
        type: string
      example: '"66f1c2a0-1a2b"'

    InvitationToken:
      name: token
      in: path
      required: true
      description: Invitation token, as returned by `GET /invitations` or `POST /invitations`.
      schema:
        type: string
      example: 9c1f0e6b2a7d4c8e5f3a1b9d0c7e2f4a
  headers:
    ETag:
      description: Entity tag for optimistic concurrency control.
      schema:
        type: string
      example: '"a3f8b2c1d4e5f6a7b8c9d0e1f2a3b4c5"'

  # ---------------------------------------------------------------------------
  # Reusable Responses
  # ---------------------------------------------------------------------------

    I18nInvalidates:
      description: Client cache tags the admin should refresh after this change.
      schema:
        type: string
      example: "i18n:update, translations:update"
  responses:
    Unauthorized:
      description: Authentication is required or the provided credentials are invalid.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            status: 401
            title: Unauthorized
            detail: Invalid username or password.

    Forbidden:
      description: The authenticated user does not have the required permission.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            status: 403
            title: Forbidden
            detail: "Missing required permission: api.pages.write"

    NotFound:
      description: The requested resource was not found.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            status: 404
            title: Not Found
            detail: "Page not found at route: /nonexistent"

    Conflict:
      description: |
        The resource has been modified since the provided ETag was
        generated. Fetch the latest version and retry.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            status: 409
            title: Conflict
            detail: The resource has been modified since you last retrieved it. Please fetch the latest version and try again.

    ValidationError:
      description: The request body failed validation.
      content:
        application/problem+json:
          schema:
            allOf:
              - $ref: "#/components/schemas/ErrorResponse"
              - type: object
                properties:
                  errors:
                    type: array
                    items:
                      $ref: "#/components/schemas/ValidationError"
          example:
            status: 422
            title: Unprocessable Entity
            detail: "Missing required fields: title, route"
            errors:
              - field: title
                message: "The 'title' field is required."
              - field: route
                message: "The 'route' field is required."

    TooManyRequests:
      description: Rate limit exceeded.
      headers:
        X-RateLimit-Limit:
          description: Maximum requests allowed per window.
          schema:
            type: integer
        X-RateLimit-Remaining:
          description: Requests remaining in the current window.
          schema:
            type: integer
        X-RateLimit-Reset:
          description: UTC epoch timestamp when the rate limit window resets.
          schema:
            type: integer
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            status: 429
            title: Too Many Requests
            detail: Rate limit exceeded. Try again later.

  # ---------------------------------------------------------------------------
  # Request bodies
  # ---------------------------------------------------------------------------

    AuditDisabled:
      description: The audit trail is not enabled (`plugins.api.audit.enabled` is off).
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            status: 404
            title: Not Found
            detail: The audit trail is not enabled.
    AuditUnavailable:
      description: The `pdo_sqlite` PHP extension is not installed, so the audit trail cannot run.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            status: 503
            title: Service Unavailable
            detail: The audit trail requires the SQLite PHP extension, which is not installed.

    ExtensionScriptJs:
      description: JavaScript web component source.
      headers:
        ETag:
          description: Stat-based validator `"{mtime-hex}-{size-hex}"`.
          schema:
            type: string
        Cache-Control:
          description: Always `private, no-cache` (store, but revalidate every time).
          schema:
            type: string
      content:
        application/javascript:
          schema:
            type: string
            description: Raw JavaScript source defining the custom element.

    ExtensionScriptFieldBundle:
      description: Map of field type to JavaScript source (not wrapped in the `data` envelope).
      headers:
        ETag:
          description: MD5 of each script's name, mtime and size.
          schema:
            type: string
        Cache-Control:
          description: Always `private, no-cache`.
          schema:
            type: string
      content:
        application/json:
          schema:
            type: object
            additionalProperties:
              type: string
              description: JavaScript source for that field type.
          example:
            seo-magic-preview: "class extends HTMLElement { /* ... */ }"
            seo-magic-score: "class extends HTMLElement { /* ... */ }"

    ExtensionScriptNotModified:
      description: The script is unchanged since the ETag sent in `If-None-Match`. No body.
      headers:
        ETag:
          description: The current validator.
          schema:
            type: string
        Cache-Control:
          description: Always `private, no-cache`.
          schema:
            type: string

    ExtensionScriptNotFound:
      description: The package is not installed, or it does not ship the requested script.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            status: 404
            title: Not Found
            detail: "Widget component not found for plugin 'ai-pro'."

    ExtensionScriptPluginNotFound:
      description: The plugin is not installed, is not enabled, or does not ship the requested script.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            status: 404
            title: Not Found
            detail: "Plugin 'ai-pro' is not enabled."

    ExtensionScriptInvalidSlug:
      description: The package slug is empty or contains a path separator.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            status: 422
            title: Unprocessable Entity
            detail: "Invalid package slug '..'."

    GpmRepositoryUnreachable:
      description: The GPM repository could not be reached.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            status: 502
            title: Bad Gateway
            detail: Unable to reach GPM repository.
  requestBodies:
    MediaMetadataWrite:
      required: true
      content:
        application/json:
          schema:
            type: object
            properties:
              fields:
                type: object
                additionalProperties:
                  oneOf:
                    - type: string
                    - type: array
                      items:
                        type: string
                description: |
                  Map of metadata field key to value. Only keys matching a
                  configured field are written; an empty string (or empty array
                  for a `tags` field) clears a field. A `tags` field accepts an
                  array of strings, or a comma-separated string. A bare
                  `{key: value}` object (without the `fields` wrapper) is also
                  accepted.
                example:
                  alt: A dog running on the beach
                  title: Beach Day
                  caption: ""
                  description: Our trip to the coast last summer.
                  tags: [beach, dog, summer]

  # ---------------------------------------------------------------------------
  # Schemas
  # ---------------------------------------------------------------------------
  schemas:
    # -- Media metadata --------------------------------------------------------
    MediaMetadata:
      type: object
      description: Editable metadata for a media file, plus its read-only stored keys.
      properties:
        filename:
          type: string
          example: beach.jpg
        has_meta:
          type: boolean
          description: Whether a `.meta.yaml` sidecar currently exists for the file.
          example: true
        fields:
          type: array
          description: The configured editable fields with their current values.
          items:
            type: object
            properties:
              key:
                type: string
                example: alt
              label:
                type: string
                example: Alt Text
              type:
                type: string
                enum: [text, textarea, tags]
                example: text
              value:
                description: |
                  A string for `text`/`textarea` fields, or an array of strings
                  for a `tags` field.
                oneOf:
                  - type: string
                  - type: array
                    items:
                      type: string
                example: A dog running on the beach
        extra:
          type: object
          additionalProperties: true
          description: |
            Other keys stored in the sidecar (EXIF, dimensions, upload info)
            that the editor does not manage and never overwrites.
          example:
            width: 1600
            height: 1067

    # -- Blueprint -------------------------------------------------------------
    BlueprintResponse:
      type: object
      properties:
        data:
          $ref: "#/components/schemas/Blueprint"

    Blueprint:
      type: object
      description: A resolved blueprint schema defining form fields for content or configuration editing.
      properties:
        name:
          type: string
          description: Blueprint identifier (template name, plugin slug, etc.)
          example: blog
        title:
          type: string
          description: Human-readable title.
          example: Blog
        type:
          type: string
          nullable: true
          description: Blueprint type (plugin, theme, etc.)
        child_type:
          type: string
          nullable: true
          description: Default child page type for this template.
          example: item
        validation:
          type: string
          description: Validation mode (`strict` or `loose`).
          example: loose
        fields:
          type: array
          description: Top-level field definitions (typically a `tabs` container).
          items:
            $ref: "#/components/schemas/BlueprintField"

    PermissionAction:
      type: object
      description: A registered permission action, as returned by `GET /blueprints/users/permissions`.
      required: [name, label]
      properties:
        name:
          type: string
          description: Full dotted permission name.
          example: api.pages.read
        label:
          type: string
          description: Translated label in the caller's admin language.
          example: Read
        children:
          type: array
          description: Child actions. Omitted on leaf actions.
          items:
            $ref: "#/components/schemas/PermissionAction"

    BlueprintField:
      type: object
      description: A single form field definition.
      required: [name, type]
      properties:
        name:
          type: string
          description: Dot-notation field path (e.g. `header.content.limit`).
          example: header.title
        type:
          type: string
          description: |
            Field type. Standard types include: `text`, `textarea`, `select`, `toggle`,
            `checkbox`, `radio`, `markdown`, `editor`, `filepicker`, `pagemedia`,
            `taxonomy`, `list`, `array`, `tabs`, `tab`, `section`, `fieldset`,
            `columns`, `column`, `spacer`, `display`, `hidden`.
          example: text
        label:
          type: string
          description: Display label (may contain translation keys like `PLUGIN_ADMIN.TITLE`).
        help:
          type: string
          description: Help text shown below the field.
        placeholder:
          type: string
        default:
          description: Default value for the field.
        description:
          type: string
          description: Extended description (may contain HTML if `markdown` is true).
        title:
          type: string
          description: Section or spacer title.
        options:
          type: object
          additionalProperties:
            type: string
          description: Key-value options for select, radio, toggle fields.
          example: { "asc": "Ascending", "desc": "Descending" }
        validate:
          type: object
          description: Validation rules.
          properties:
            type:
              type: string
              example: int
            required:
              type: boolean
            pattern:
              type: string
            min:
              type: number
            max:
              type: number
        fields:
          type: array
          description: Nested child fields (for tabs, tab, section, fieldset, columns, list).
          items:
            $ref: "#/components/schemas/BlueprintField"
        toggleable:
          type: boolean
        multiple:
          type: boolean
        markdown:
          type: boolean
          description: If true, the description contains HTML/markdown.
        underline:
          type: boolean
        size:
          type: string
          enum: [small, medium, large]
        rows:
          type: integer
          description: Number of rows for textarea fields.

    # -- Sidebar --------------------------------------------------------------
    SidebarItem:
      type: object
      description: A sidebar navigation item contributed by a plugin.
      properties:
        id:
          type: string
          description: Unique identifier for the sidebar item.
          example: dashboard
        plugin:
          type: string
          description: Plugin slug that registered this item.
          example: admin-next
        label:
          type: string
          description: Display label for the sidebar item.
          example: Dashboard
        icon:
          type: string
          description: Icon identifier (e.g. Lucide icon name).
          example: gauge
        route:
          type: string
          description: Admin route this item navigates to.
          example: /dashboard
        priority:
          type: integer
          description: Sort priority (higher values appear first).
          example: 100
        badge:
          type: string
          nullable: true
          description: Optional static badge text (e.g. a count or status indicator). Plugins may also send a number.
          example: null
        badgeEndpoint:
          type: string
          description: "Optional API path returning `{count: N}`, fetched by the admin to show a live badge that overrides `badge`."
          example: /my-plugin/badge
      additionalProperties: true

    # -- MCP Tool --------------------------------------------------------------
    McpTool:
      type: object
      description: One MCP tool contributed by a plugin.
      properties:
        name:
          type: string
          description: The tool name an MCP client registers, `{prefix}_{name}` from the manifest. 64 characters or fewer.
          example: kahunacart_list_products
        plugin:
          type: string
          description: Slug of the plugin that contributed the tool.
          example: kahunacart
        title:
          type: string
          nullable: true
          description: Human title an MCP client may show.
          example: List products
        description:
          type: string
          description: What the tool does and returns, and when to use it.
          example: List catalog products with paging, search and status filters.
        method:
          type: string
          enum: [GET, POST, PATCH, PUT, DELETE]
          example: GET
        path:
          type: string
          description: Route path relative to the API base. `{name}` placeholders are filled from the arguments.
          example: /kahunacart/products
        permission:
          type: string
          nullable: true
          description: The permission the route enforces, checked again by the client before it calls.
          example: kahunacart.products.manage
        annotations:
          type: object
          description: Always fully populated, with the per-method defaults applied.
          properties:
            readOnly:
              type: boolean
            destructive:
              type: boolean
            idempotent:
              type: boolean
        input_schema:
          type: object
          description: |
            JSON Schema for the arguments, always present and always an object.
            A tool with no arguments carries `{"type":"object","properties":{}}`.
            Restricted to the subset an MCP client can convert: `type`,
            `description`, `default`, `enum`, `nullable`, `minimum`, `maximum`,
            `minLength`, `maxLength`, `pattern`, `format`, `items`, `properties`,
            `required` and `additionalProperties`.
            `additionalProperties: true` at the root means what it means on a
            nested object: arguments the schema does not declare are passed
            through, as query parameters for a `GET` and into the body for every
            other method. It never appears on a tool that designates a `body`.
          additionalProperties: true
        path_params:
          type: array
          description: The `{name}` placeholders in `path`, in order. Each is a required property of `input_schema`.
          items:
            type: string
          example: [id]
        query:
          type: array
          description: For write methods, the properties to send as query-string parameters rather than in the JSON body. Ignored for GET, which sends every non-path property as a query parameter.
          items:
            type: string
          example: []
        body:
          type: string
          nullable: true
          description: The one property whose value is the whole request body, or null when the body is assembled from the properties left over after the path and query parameters.
          example: null

    # -- Plugin Page Definition ------------------------------------------------
    PluginPageDefinition:
      type: object
      description: Admin page definition registered by a plugin. The plugin supplies it, so it may carry further keys of its own, which are passed through unchanged.
      additionalProperties: true
      properties:
        id:
          type: string
          description: Unique page identifier.
          example: api-settings
        plugin:
          type: string
          description: Plugin slug that registered this page.
          example: api
        title:
          type: string
          description: Page title.
          example: API Settings
        icon:
          type: string
          description: Icon identifier for the page.
          example: plug
        page_type:
          type: string
          description: |
            Page rendering type. `blueprint` renders a blueprint form
            (`blueprint`, `data_endpoint`, `save_endpoint`); `component`
            mounts the plugin's web component from
            `GET /gpm/plugins/{slug}/page-script`.
          example: blueprint
        blueprint:
          type: string
          nullable: true
          description: Name of the plugin blueprint the form is drawn from (for `blueprint` pages).
          example: algolia-pro
        data_endpoint:
          type: string
          nullable: true
          description: API path, relative to the API base, to fetch current data for the form.
          example: /algolia-pro/data
        save_endpoint:
          type: string
          nullable: true
          description: API path, relative to the API base, to save form data.
          example: /algolia-pro/data
        actions:
          type: array
          description: Buttons for the page toolbar.
          items:
            type: object
            additionalProperties: true
            properties:
              id:
                type: string
                description: Action identifier; `save` is the form's own save.
                example: reindex
              label:
                type: string
                example: Reindex
              icon:
                type: string
                example: fa-rotate
              endpoint:
                type: string
                description: API path the action POSTs to. Without one, Admin Next dispatches a `grav:plugin-page-action` event for the plugin's own scripts to handle.
                example: /algolia-pro/reindex
              confirm:
                type: string
                description: Confirmation prompt shown before the action runs.
              primary:
                type: boolean
                description: Render as the primary button.
              upload:
                type: boolean
                description: The action takes a file upload.
        has_custom_component:
          type: boolean
          description: Whether the plugin ships `admin-next/pages/{slug}.js`. Always set by the server.
          example: false
        settings_route:
          type: string
          description: Hash route inside the page where this plugin's settings are drawn (e.g. `#settings`). Only kept when it starts with `#`.
          example: "#settings"
        settings_page:
          type: string
          description: Slug of another plugin whose admin page draws these settings (used by add-ons without a page of their own). Only kept alongside a valid `settings_route` and when that plugin has an Admin Next page.
          example: kahunacart

    # -- Page -----------------------------------------------------------------
    Page:
      type: object
      description: Full page object including content, media, and optional children.
      properties:
        route:
          type: string
          description: The page route (e.g. `/blog/my-post`).
          example: /blog/my-first-post
        raw_route:
          type: string
          description: |
            Structural route of the page. Differs from `route` for the home
            page (`/home` instead of `/`) and for its children when
            `home.hide_in_urls` is on. Prefer this when addressing the page
            in later API calls.
          example: /blog/my-first-post
        parent_route:
          type: string
          description: |
            Structural route of the parent page, taken from the real
            hierarchy; `/` for a top-level page. Use this rather than
            splitting `route` when moving or reparenting.
          example: /blog
        slug:
          type: string
          description: URL-safe slug derived from the directory name.
          example: my-first-post
        folder:
          type: string
          description: The on-disk folder name, including any numeric ordering prefix.
          example: 01.my-first-post
        title:
          type: string
          description: The page title from the header.
          example: My First Post
        menu:
          type: string
          description: The menu label (`menu` from the header, falling back to the title).
          example: My First Post
        template:
          type: string
          description: The Twig template used to render this page.
          example: blog
        header:
          type: object
          additionalProperties: true
          description: The full frontmatter header as a key-value object.
          example:
            title: My First Post
            taxonomy:
              category: [blog]
              tag: [intro, grav]
        taxonomy:
          type: object
          additionalProperties:
            type: array
            items:
              type: string
          description: Taxonomy types and their values for this page.
          example:
            category: [blog]
            tag: [intro, grav]
        published:
          type: boolean
          description: |
            Whether the page is published. This is a single boolean and cannot
            distinguish a draft from a page that is scheduled or expired — use
            `publish_state` for that.
          example: true
        publish_date:
          type: string
          format: date-time
          nullable: true
          description: |
            The page's `publish_date` frontmatter value as ISO 8601, parsed
            with the page's own `dateformat`. Null when unset.
          example: "2026-12-01T09:00:00+00:00"
        unpublish_date:
          type: string
          format: date-time
          nullable: true
          description: |
            The page's `unpublish_date` frontmatter value as ISO 8601, parsed
            with the page's own `dateformat`. Null when unset.
          example: "2027-01-01T00:00:00+00:00"
        publish_state:
          type: string
          enum: [published, unpublished, scheduled, expired]
          description: |
            The page's effective publishing state, resolved the way Grav core
            resolves it: an explicit `published:` in the frontmatter wins
            outright and makes both dates inert, and the dates are only
            consulted when `system.pages.publish_dates` is enabled.

            * `published` — live now.
            * `unpublished` — an explicit `published: false` draft.
            * `scheduled` — `publish_date` is in the future.
            * `expired` — `unpublish_date` is in the past.
          example: published
        visible:
          type: boolean
          description: Whether the page is visible in navigation menus.
          example: true
        routable:
          type: boolean
          description: Whether the page is routable (accessible via URL).
          example: true
        date:
          type: string
          format: date-time
          nullable: true
          description: Page date as ISO 8601.
          example: "2026-03-20T12:00:00+00:00"
        modified:
          type: string
          format: date-time
          nullable: true
          description: Last modification time as ISO 8601.
          example: "2026-03-22T09:30:00+00:00"
        order:
          $ref: "#/components/schemas/PageOrder"
        has_children:
          type: boolean
          description: Whether this page has child pages. Useful for tree/column UIs to show expand indicators without loading children.
          example: true
        content:
          type: string
          description: |
            Raw Markdown content of the page body. Left out when
            `summary=true` is requested.
          example: |
            # Hello World

            This is the body of the post.
        content_html:
          type: string
          description: |
            Rendered HTML content. Only present when `render=true` is
            requested.
          example: "<h1>Hello World</h1>\n<p>This is the body of the post.</p>"
        summary:
          type: string
          description: |
            Plain-text summary (rendered, tags stripped, truncated to
            `summary_size`, default 300 characters). Only present when
            `summary=true` is requested.
          example: This is the body of the post.
        media:
          type: array
          description: Media files attached to this page.
          items:
            $ref: "#/components/schemas/MediaItem"
        children:
          type: array
          description: |
            Child pages. Only present when `children=true` is requested.
            Each child is a full Page object (recursion depth controlled by
            `children_depth`).
          items:
            $ref: "#/components/schemas/Page"
        language:
          type: string
          nullable: true
          description: |
            Language code of the page content (e.g. `en`, `fr`). `null` for a
            page file without a language extension.
          example: en
        translated_languages:
          type: object
          additionalProperties:
            type: string
            nullable: true
          description: |
            Map of language code to the page's route in that language, for
            every language with a translation file (an untyped
            `<template>.md` counts for the default language). Serialized as
            an empty array `[]` on a single-language site. Only present when
            `translations=true` is requested.
          example:
            en: /blog/my-first-post
            fr: /blog/mon-premier-article
        untranslated_languages:
          type: array
          description: |
            Configured languages that this page has not been translated into.
            Only present when `translations=true` is requested.
          items:
            type: string
          example: [de, es]
        has_default_file:
          type: boolean
          description: |
            Whether an untyped `<template>.md` exists, which makes every
            language count as translated through the fallback. Only present
            when `translations=true` is requested.
          example: false
        explicit_language_files:
          type: array
          description: |
            Language codes backed by their own `<template>.<lang>.md` file,
            as opposed to the `<template>.md` fallback. Only present when
            `translations=true` is requested.
          items:
            type: string
          example: [en, fr]
        permissions:
          type: object
          description: |
            What the authenticated caller may do to THIS page: the account's
            `api.pages.*` permissions with the page's own `header.permissions`
            rules applied on top (a page rule can both grant and deny). Present
            on every page record the API returns, including listing rows.
          properties:
            create:
              type: boolean
              description: May add child pages under this page.
            read:
              type: boolean
            update:
              type: boolean
            delete:
              type: boolean
            publish:
              type: boolean
              description: Falls back to `update` when the page states no publish rule.
            list:
              type: boolean
          example:
            create: false
            read: true
            update: true
            delete: true
            publish: true
            list: true
      required:
        - route
        - slug
        - title
        - template
        - header
        - taxonomy
        - published
        - visible
        - routable
        - date
        - modified
        - order

    PageSummary:
      type: object
      description: |
        Lightweight page representation used in list responses. Excludes
        content, media, and children for performance.
      properties:
        route:
          type: string
          example: /blog/my-first-post
        raw_route:
          type: string
          description: Structural route of the page (see `Page.raw_route`).
          example: /blog/my-first-post
        parent_route:
          type: string
          description: Structural route of the parent page; `/` for a top-level page.
          example: /blog
        slug:
          type: string
          example: my-first-post
        folder:
          type: string
          description: The on-disk folder name, including any numeric ordering prefix.
          example: 01.my-first-post
        title:
          type: string
          example: My First Post
        menu:
          type: string
          description: The menu label (`menu` from the header, falling back to the title).
          example: My First Post
        template:
          type: string
          example: blog
        header:
          type: object
          additionalProperties: true
        taxonomy:
          type: object
          additionalProperties:
            type: array
            items:
              type: string
        published:
          type: boolean
          example: true
        publish_date:
          type: string
          format: date-time
          nullable: true
          example: "2026-12-01T09:00:00+00:00"
        unpublish_date:
          type: string
          format: date-time
          nullable: true
          example: "2027-01-01T00:00:00+00:00"
        publish_state:
          type: string
          enum: [published, unpublished, scheduled, expired]
          description: |
            Effective publishing state. `published` is only a boolean, so a
            scheduled or expired page needs this field to be told apart from a
            plain draft.
          example: published
        visible:
          type: boolean
          example: true
        routable:
          type: boolean
          example: true
        date:
          type: string
          format: date-time
          nullable: true
          example: "2026-03-20T12:00:00+00:00"
        modified:
          type: string
          format: date-time
          nullable: true
          example: "2026-03-22T09:30:00+00:00"
        order:
          $ref: "#/components/schemas/PageOrder"
        has_children:
          type: boolean
          description: Whether the page has any child pages.
          example: true
        language:
          type: string
          nullable: true
          description: Language code of the page content (e.g. `en`, `fr`).
          example: en
        translated_languages:
          type: object
          additionalProperties:
            type: string
            nullable: true
          description: |
            Map of language code to the page's route in that language (see
            `Page.translated_languages`). Only present when
            `translations=true` is requested.
          example:
            en: /blog/my-first-post
        untranslated_languages:
          type: array
          description: Only present when `translations=true` is requested.
          items:
            type: string
          example: [fr]
        has_default_file:
          type: boolean
          description: Only present when `translations=true` is requested (see `Page.has_default_file`).
          example: false
        explicit_language_files:
          type: array
          description: Only present when `translations=true` is requested (see `Page.explicit_language_files`).
          items:
            type: string
          example: [en]
        permissions:
          type: object
          description: |
            What the caller may do to this page: the account's `api.pages.*`
            permissions with the page's own `header.permissions` rules
            applied on top.
          properties:
            create:
              type: boolean
            read:
              type: boolean
            update:
              type: boolean
            delete:
              type: boolean
            publish:
              type: boolean
            list:
              type: boolean
      required:
        - route
        - slug
        - title
        - template
        - header
        - taxonomy
        - published
        - visible
        - routable
        - date
        - modified
        - order

    PageOrder:
      description: |
        The folder's numeric ordering prefix, or `false` for a folder without
        one. The type depends on the page object behind the response: Flex
        pages (used by `GET /pages` when the Flex pages directory is
        available) give an integer such as `1`, while regular pages give the
        raw prefix string including its dot, such as `"01."`.
      oneOf:
        - type: integer
        - type: string
        - type: boolean
      example: 1

    PageRouteStub:
      type: object
      description: |
        Minimal payload returned by a move or copy when the resulting page
        cannot be resolved at its new route straight away.
      required: [route, slug]
      properties:
        route:
          type: string
          example: /tutorials/renamed-post
        slug:
          type: string
          example: renamed-post
      additionalProperties: false

    # -- Media ----------------------------------------------------------------
    MediaItem:
      type: object
      description: A media file, either from a page or the site-level media folder.
      properties:
        filename:
          type: string
          description: The file name.
          example: hero-image.jpg
        path:
          type: string
          description: |
            Site media only. The folder holding the file, relative to the media
            root, without the filename (an empty string for the root).
          example: blog/2026
        url:
          type: string
          description: |
            URL of the file. For site media stored where the web server refuses
            to serve it directly (for example a multi-site `user/env/...`
            folder), this points at `GET /media/raw/{path}` instead.
          example: /user/images/hero-image.jpg
        type:
          type: string
          description: MIME type of the file.
          example: image/jpeg
        size:
          type: integer
          description: File size in bytes.
          example: 204800
        alt:
          type: string
          description: Alt text from the file's `.meta.yaml` sidecar. Only present when set.
          example: A dog running on the beach
        title:
          type: string
          description: Title from the file's `.meta.yaml` sidecar. Only present when set.
          example: Beach Day
        thumbnail_url:
          type: string
          description: URL of a cached thumbnail served by `GET /thumbnails/{file}`. Only present for raster images whose thumbnail could be generated.
          example: /api/v1/thumbnails/3f2a9c1e4b5d6a7f8e9d0c1b2a394857.jpg
        dimensions:
          type: object
          nullable: true
          description: Image dimensions. Only present for image files.
          properties:
            width:
              type: integer
              example: 1920
            height:
              type: integer
              example: 1080
        modified:
          type: string
          format: date-time
          description: Last modification time as ISO 8601.
          example: "2026-03-22T09:30:00+00:00"
      required:
        - filename
        - url
        - type
        - size
        - modified

    FolderInfo:
      type: object
      properties:
        name:
          type: string
          description: Folder name.
          example: "2026"
        path:
          type: string
          description: Full relative path from the media root.
          example: blog/2026
        children_count:
          type: integer
          description: Number of immediate subdirectories.
          example: 1
        file_count:
          type: integer
          description: Number of files in this folder (excluding metadata files).
          example: 5

    # -- User -----------------------------------------------------------------
    User:
      type: object
      description: A Grav user account.
      properties:
        username:
          type: string
          example: admin
        email:
          type: string
          format: email
          nullable: true
          example: admin@example.com
        fullname:
          type: string
          nullable: true
          example: Site Administrator
        title:
          type: string
          nullable: true
          description: Job title or role label.
          example: Administrator
        state:
          type: string
          enum: [enabled, disabled]
          description: Whether the account is active.
          example: enabled
        language:
          type: string
          description: Preferred admin language code, or an empty string when unset.
          example: en
        content_editor:
          type: string
          description: Preferred content editor, or an empty string for the default.
          example: ""
        groups:
          type: array
          items:
            type: string
          description: Groups the account belongs to.
          example: [editors]
        avatar_url:
          type: string
          nullable: true
          description: |
            URL of a 200px thumbnail of the user's avatar (served by
            `GET /thumbnails/{file}`), or `null` if no avatar is set.
          example: /api/v1/thumbnails/3f2a9c1e4b5d6a7f8e9d0c1b2a394857.jpg
        twofa_enabled:
          type: boolean
          description: Whether two-factor authentication is currently enabled for this user.
          example: false
        twofa_secret:
          type: boolean
          description: Whether a 2FA secret exists for this user (does not expose the actual secret).
          example: false
        access:
          type: object
          additionalProperties: true
          description: |
            Permission tree. Keys are dot-separated permission groups, values
            are booleans or nested objects.
          example:
            admin:
              super: true
            api:
              access: true
        created:
          type: string
          format: date-time
          nullable: true
          example: "2026-01-15T10:00:00+00:00"
        modified:
          type: string
          format: date-time
          nullable: true
          example: "2026-03-22T09:30:00+00:00"
        twofa_global_enabled:
          type: boolean
          description: |
            Only on `GET` and `PATCH /users/{username}`. Whether 2FA support
            is available on this site (the Login plugin is installed), so the
            client knows whether to offer enrollment. Not part of the ETag.
        extra:
          type: object
          description: |
            Only on `GET /users`. Plugin-supplied scalar values for the columns
            declared by `GET /users/columns`, keyed by each column's `field`.
            Absent when no plugin supplies values for the user.
          additionalProperties:
            description: A string, number, boolean or null.
      additionalProperties:
        description: |
          Values of custom fields the site added to the account blueprint are
          included too, when the account has a value for them.
      required:
        - username
        - state
        - access

    # -- Config ---------------------------------------------------------------
    ConfigObject:
      type: object
      additionalProperties: true
      description: |
        An arbitrary configuration object. Its structure depends on the scope.
        For scalar values, the response wraps the value as `{"value": ...}`.
      example:
        pages:
          theme: quark
        debugger:
          enabled: false

    # -- System ---------------------------------------------------------------
    SystemInfo:
      type: object
      description: Runtime information about the Grav installation.
      properties:
        grav_version:
          type: string
          description: The installed Grav core version.
          example: 1.7.48
        php_version:
          type: string
          description: PHP runtime version.
          example: 8.3.14
        php_extensions:
          type: array
          items:
            type: string
          description: List of loaded PHP extensions.
          example: [curl, gd, intl, json, mbstring, openssl, pdo, zip]
        server_software:
          type: string
          description: Web server identification string.
          example: Apache/2.4.58 (Unix)
        environment:
          type: string
          description: The active Grav environment name.
          example: localhost
        plugins:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
                example: API
              version:
                type: string
                example: 1.0.0-beta.1
              enabled:
                type: boolean
                example: true
          description: Installed plugins with version and enabled state.
        themes:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
                example: Quark
              version:
                type: string
                example: 2.0.4
              active:
                type: boolean
                example: true
          description: Installed themes with version and active state.
        php_config:
          type: object
          description: |
            PHP `ini` settings grouped by section. Keys are section labels
            (`Upload & POST`, `Memory & Execution`, `Error Handling`,
            `Paths & Environment`, `Session`, `OPcache`, `Security`,
            `Date & Locale`); each value maps a setting name to its value as
            a string.
          additionalProperties:
            type: object
            additionalProperties:
              type: string
          example:
            "Memory & Execution":
              memory_limit: 256M
              max_execution_time: 30s
      required:
        - grav_version
        - php_version
        - php_extensions
        - server_software
        - environment
        - plugins
        - themes
        - php_config

    LogEntry:
      type: object
      description: A single parsed entry from a log file such as `grav.log`.
      properties:
        date:
          type: string
          nullable: true
          description: Timestamp of the log entry, as written in the log.
          example: "2026-03-22 09:30:15"
        logger:
          type: string
          description: Monolog channel name that wrote the entry.
          example: grav
        level:
          type: string
          description: Log level.
          enum: [DEBUG, INFO, NOTICE, WARNING, ERROR, CRITICAL, ALERT, EMERGENCY]
          example: ERROR
        message:
          type: string
          description: The log message.
          example: "Class 'Grav\\Plugin\\FooPlugin' not found"
      required:
        - date
        - logger
        - level
        - message

    Backup:
      type: object
      description: Metadata about a backup archive.
      properties:
        filename:
          type: string
          description: Name of the backup archive file.
          example: grav-backup-2026-03-22T09-30-00.zip
        path:
          type: string
          description: Absolute filesystem path. Only present on creation responses.
          example: /var/www/grav/backup/grav-backup-2026-03-22T09-30-00.zip
        title:
          type: string
          nullable: true
          description: Backup profile title derived from the filename. Only present on list responses.
          example: Default Site Backup
        size:
          type: integer
          description: Archive file size in bytes.
          example: 52428800
        date:
          type: string
          nullable: true
          description: Creation timestamp in ISO 8601, on both creation and list responses.
          example: "2026-03-22T09:30:00+00:00"
      required:
        - filename
        - size

    ApiKey:
      type: object
      description: |
        API key metadata. The key hash is never exposed; only the prefix
        (first 12 characters plus `...`) is shown.
      properties:
        id:
          type: string
          description: Unique identifier for the key.
          example: a1b2c3d4e5f6a7b8
        name:
          type: string
          description: Human-readable label for the key.
          example: CI/CD Pipeline
        prefix:
          type: string
          description: First 12 characters of the key plus `...` for identification.
          example: grav_a1b2c3d4...
        scopes:
          type: array
          items:
            type: string
          description: Permission scopes this key is restricted to. Empty means unrestricted.
          example: [api.pages.read, api.media.read]
        active:
          type: boolean
          description: Whether the key is currently active.
          example: true
        created:
          type: integer
          nullable: true
          description: Unix timestamp when the key was created.
          example: 1711094400
        last_used:
          type: integer
          nullable: true
          description: Unix timestamp when the key was last used, or `null` if never used.
          example: 1711180800
        expires:
          type: integer
          nullable: true
          description: Unix timestamp when the key expires, or `null` for no expiry.
          example: null
      required:
        - id
        - name
        - prefix
        - scopes
        - active

    # -- Auth -----------------------------------------------------------------
    TokenResponse:
      type: object
      description: |
        The bare JWT token pair. Every auth endpoint that issues tokens also
        includes the `user` profile; see `AuthTokenPairWithUser`.
      properties:
        access_token:
          type: string
          description: Short-lived JWT access token.
          example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
        refresh_token:
          type: string
          description: Long-lived refresh token for obtaining new access tokens.
          example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
        token_type:
          type: string
          description: Token type. Always `Bearer`.
          example: Bearer
          enum: [Bearer]
        expires_in:
          type: integer
          description: Access token lifetime in seconds.
          example: 3600
      required:
        - access_token
        - refresh_token
        - token_type
        - expires_in

    # -- Pagination Envelope --------------------------------------------------
    PaginatedEnvelope:
      type: object
      description: Standard paginated response wrapper.
      properties:
        data:
          type: array
          items: {}
        meta:
          type: object
          properties:
            pagination:
              type: object
              properties:
                page:
                  type: integer
                  description: Current page number (1-based).
                  example: 1
                per_page:
                  type: integer
                  description: Items per page.
                  example: 20
                total:
                  type: integer
                  description: Total number of items across all pages.
                  example: 42
                total_pages:
                  type: integer
                  description: Total number of pages.
                  example: 3
                located_at_index:
                  type: integer
                  description: |
                    Zero-based position of the item requested with a `locate`
                    query in the full result set. Present only when the
                    endpoint supports `locate` and the item was found.
                  example: 37
              required: [page, per_page, total, total_pages]
          required: [pagination]
        links:
          type: object
          description: Pagination links. Each carries the request's other query parameters (filters, search, sort), so following one stays within the same filtered list; only `page` and `per_page` change.
          properties:
            self:
              type: string
              description: URL of the current page.
              example: "/api/v1/pages?page=1&per_page=20"
            first:
              type: string
              description: URL of the first page. Present when `page > 1`.
              example: "/api/v1/pages?page=1&per_page=20"
            prev:
              type: string
              description: URL of the previous page. Present when `page > 1`.
              example: "/api/v1/pages?page=1&per_page=20"
            next:
              type: string
              description: URL of the next page. Present when more pages exist.
              example: "/api/v1/pages?page=2&per_page=20"
            last:
              type: string
              description: URL of the last page. Present when more pages exist.
              example: "/api/v1/pages?page=3&per_page=20"
          required: [self]
      required: [data, meta, links]

    # -- Error ----------------------------------------------------------------
    ErrorResponse:
      type: object
      description: |
        Error response following RFC 7807 Problem Details. Returned with
        content type `application/problem+json`.
      properties:
        status:
          type: integer
          description: HTTP status code.
          example: 422
        title:
          type: string
          description: Short, human-readable summary of the problem type.
          example: Unprocessable Entity
        detail:
          type: string
          description: Human-readable explanation specific to this occurrence.
          example: "Missing required fields: title"
        errors:
          type: array
          description: |
            Field-level validation errors. Only present on 422 responses
            when individual field errors are available.
          items:
            $ref: "#/components/schemas/ValidationError"
      required:
        - status
        - title
        - detail

    ValidationError:
      type: object
      description: A single field-level validation error.
      properties:
        field:
          type: string
          description: The name of the field that failed validation.
          example: title
        message:
          type: string
          description: Human-readable description of the validation failure.
          example: "The 'title' field is required."
      required:
        - field
        - message

    # -- Request Bodies -------------------------------------------------------
    CreatePageRequest:
      type: object
      description: Request body for creating a new page.
      required: [route, title]
      properties:
        route:
          type: string
          description: Full route for the new page (e.g. `/blog/my-new-post`).
          example: /blog/my-new-post
        title:
          type: string
          description: Page title.
          example: My New Post
        template:
          type: string
          description: Template name. Defaults to `default` if omitted.
          default: default
          example: blog
        content:
          type: string
          description: Raw Markdown body content.
          default: ""
          example: |
            # Hello World

            This is the body of my new post.
        header:
          type: object
          additionalProperties: true
          description: |
            Additional frontmatter fields to merge into the page header.
            The `title` from the top-level field is always included.
          example:
            taxonomy:
              category: [blog]
              tag: [new]
        order:
          oneOf:
            - type: integer
              nullable: true
            - type: string
              enum: [auto]
          description: |
            Numeric ordering prefix. When set to an integer, creates a visible
            (ordered) page directory (e.g. `03.my-new-post`). When `null`,
            creates an unordered page. Pass the string `"auto"` to mirror the
            admin-classic add-page behavior: the API scans the parent's direct
            children and assigns the next available number when any sibling
            already uses numeric prefixes; otherwise the new page is created
            without a numeric prefix.
          example: 3
        kind:
          type: string
          enum: [page, folder, module]
          default: page
          description: |
            What to create. `page` writes the folder plus `<template>.md`.
            `folder` creates only the directory (a routing/grouping
            container, no Markdown file). `module` creates a modular
            sub-page: the slug is prefixed with `_` unless it already is.
          example: page
        lang:
          type: string
          description: |
            Language code. When provided, creates the page in the specified
            language (as `<template>.<lang>.md`). Must be one of the site's
            configured languages (422 otherwise). If omitted, uses the default
            site language.
          example: en

    UpdatePageRequest:
      type: object
      description: |
        Request body for partially updating a page. All fields are optional;
        only provided fields are changed.
      properties:
        title:
          type: string
          description: New page title.
          example: Updated Post Title
        content:
          type: string
          description: New raw Markdown body content.
          example: |
            # Updated

            New content here.
        template:
          type: string
          description: Change the page template.
          example: blog
        header:
          type: object
          additionalProperties: true
          description: |
            Header fields to merge into the existing frontmatter. Existing
            fields not included here are preserved, and a `null` value removes
            that key. Lists are replaced wholesale rather than merged. With
            `header_mode: replace` the header is replaced instead of merged.
          example:
            taxonomy:
              tag: [updated]
        header_mode:
          type: string
          enum: [merge, replace]
          default: merge
          description: |
            `replace` treats `header` as the complete frontmatter (as sent by
            a raw-frontmatter editor), so keys left out are deleted. Any other
            value merges.
          example: merge
        published:
          type: boolean
          description: Set the published state.
          example: true
        visible:
          type: boolean
          description: Set the visible state.
          example: true

    MovePageRequest:
      type: object
      description: Request body for moving a page.
      required: [parent]
      properties:
        parent:
          type: string
          description: Route of the new parent page (use `/` for root).
          example: /tutorials
        slug:
          type: string
          description: New slug. Defaults to the current slug if omitted.
          example: renamed-post
        order:
          type: integer
          nullable: true
          description: |
            New ordering prefix. Defaults to the current order if omitted;
            `null` moves the page to an unnumbered folder.
          example: 5

    CreateUserRequest:
      type: object
      description: Request body for creating a new user account.
      required: [username, password, email]
      properties:
        username:
          type: string
          description: |
            Unique username, 3-64 characters. Validated by Grav core's
            `User::isValidUsername()`: it may not contain `\ / ? * : ; { }` or a
            newline, may not contain `..`, and may not start with a period.
            Letters, numbers, periods, hyphens and underscores are the
            recommended set.
          minLength: 3
          maxLength: 64
          example: johndoe
        password:
          type: string
          format: password
          description: Password for the new account.
          example: s3cureP@ss
        email:
          type: string
          format: email
          description: Email address.
          example: john@example.com
        fullname:
          type: string
          description: Full display name.
          default: ""
          example: John Doe
        title:
          type: string
          description: Job title or role label.
          default: ""
          example: Editor
        state:
          type: string
          enum: [enabled, disabled]
          description: Initial account state.
          default: enabled
          example: enabled
        access:
          type: object
          additionalProperties: true
          description: |
            Permission tree for the new user. Granting `admin.super` or
            `api.super` requires the caller to be a super admin (403 otherwise).
          example:
            api:
              access: true
              pages:
                read: true
                write: true
        groups:
          type: array
          items:
            type: string
          description: Group memberships. Only applied when the caller is a super admin; silently ignored otherwise.
          example: [editors]
      additionalProperties:
        description: |
          Custom fields the site added to the account blueprint
          (`user/blueprints/user/account.yaml`) are saved too. Keys the
          blueprint doesn't declare are ignored.

    UpdateUserRequest:
      type: object
      description: |
        Request body for partially updating a user. All fields are optional;
        only provided fields are changed, but the body may not be empty (422).

        Field gates: `email`, `fullname`, `title`, `language`,
        `content_editor` and `password` are open to anyone
        allowed to edit the account (including the account owner). `twofa_enabled`
        and `twofa_secret` are ignored here: two-factor state only changes through
        the `/users/{username}/2fa` endpoints, which check a code. `state` and
        `access` require `api.users.write` (403 when sent without it).
        `groups` requires a super admin (403 otherwise). Changing the password
        or disabling the account invalidates every outstanding API token for
        that account.
      properties:
        email:
          type: string
          format: email
          example: jane@example.com
        fullname:
          type: string
          example: Jane Doe
        title:
          type: string
          example: Senior Editor
        language:
          type: string
          description: Preferred admin language code.
          example: en
        content_editor:
          type: string
          description: Preferred content editor; empty for the default.
          example: ""
        state:
          type: string
          enum: [enabled, disabled]
          description: Requires `api.users.write`.
          example: enabled
        password:
          type: string
          format: password
          description: New password. Only set if a non-empty value is provided.
        access:
          type: object
          additionalProperties: true
          description: |
            Updated permission tree. Requires `api.users.write`; granting
            `admin.super` or `api.super` also requires a super admin.
        groups:
          type: array
          items:
            type: string
          description: Group memberships. Requires a super admin.
      additionalProperties:
        description: |
          Custom fields the site added to the account blueprint are saved too.
          Keys the blueprint doesn't declare are ignored.

    # -- Languages ------------------------------------------------------------
    LanguageInfo:
      type: object
      description: Site language configuration.
      properties:
        enabled:
          type: boolean
          description: Whether multi-language support is enabled.
          example: true
        languages:
          type: array
          description: The configured languages, in configuration order.
          items:
            type: object
            required: [code, name, native_name, rtl, is_default]
            properties:
              code:
                type: string
                example: fr
              name:
                type: string
                description: English name of the language (falls back to the code).
                example: French
              native_name:
                type: string
                description: The language's own name for itself (falls back to the code).
                example: Français
              rtl:
                type: boolean
                description: Whether the language is written right to left.
                example: false
              is_default:
                type: boolean
                example: false
        default:
          type: string
          nullable: true
          description: The default language code, or `null` when multi-language is off.
          example: en
        active:
          type: string
          nullable: true
          description: |
            The currently active language based on the request context,
            falling back to the default. `null` when multi-language is off.
          example: en
      required:
        - enabled
        - languages
        - default
        - active

    TranslationInfo:
      type: object
      description: Translation status for a page across configured languages.
      properties:
        route:
          type: string
          description: The page route.
          example: /blog/my-first-post
        default_language:
          type: string
          nullable: true
          description: The default language code, or `null` when none is configured.
          example: en
        translated:
          type: object
          additionalProperties:
            type: string
            nullable: true
          description: |
            Map of language code to the page's route in that language (with
            each ancestor's slug localized too), for every language that has
            a translation file. A default-language entry is included when the
            page has an untyped `<template>.md`. `null` when the route cannot
            be composed. Serialized as an empty array `[]` on a
            single-language site.
          example:
            en: /blog/my-first-post
            fr: /blog/mon-premier-article
        untranslated:
          type: array
          items:
            type: string
          description: Language codes that do not yet have a translation.
          example: [de, es]
      required:
        - route
        - default_language
        - translated
        - untranslated

    # -- GPM ------------------------------------------------------------------
    Package:
      type: object
      description: |
        A Grav plugin or theme package. Which optional fields appear depends
        on the endpoint: installed-package responses carry `enabled`,
        `is_symlink` and `updatable`; repository responses carry `installed`;
        optional blueprint fields (`docs`, `bugs`, `dependencies`,
        `compatibility`, `keywords`, `icon`, premium info) appear only when
        the package sets them.
      properties:
        slug:
          type: string
          nullable: true
          description: Package slug identifier.
          example: admin2
        name:
          type: string
          nullable: true
          description: Human-readable package name. A translation key is resolved in the caller's admin language.
          example: Admin2
        version:
          type: string
          nullable: true
          description: Installed version for installed packages, or the latest version for repository packages. Always a string, even when the blueprint writes a bare number.
          example: 2.1.13
        type:
          type: string
          enum: [plugin, theme]
          description: Package type.
          example: plugin
        description:
          type: string
          nullable: true
          description: Short description as authored (Markdown allowed). A translation key is resolved in the caller's admin language.
          example: The next-generation admin for Grav.
        description_html:
          type: string
          nullable: true
          description: "`description` rendered to HTML (safe mode), or `null` when there is no description."
          example: "<p>The next-generation admin for Grav.</p>"
        author:
          type: object
          nullable: true
          description: Package author, or `null` when the blueprint names none.
          properties:
            name:
              type: string
              nullable: true
              example: Team Grav
            email:
              type: string
              nullable: true
              example: devs@getgrav.org
            url:
              type: string
              nullable: true
              example: https://getgrav.org
        homepage:
          type: string
          nullable: true
          description: URL to the package homepage (falls back to the blueprint's `url`).
          example: https://github.com/getgrav/grav-plugin-admin2
        docs:
          type: string
          description: Documentation URL. Only present when the blueprint sets it.
        bugs:
          type: string
          description: Issue tracker URL. Only present when the blueprint sets it.
        enabled:
          type: boolean
          description: Installed packages only. For plugins, whether the plugin is enabled; for themes, whether it is the active theme.
          example: true
        is_symlink:
          type: boolean
          description: Installed packages only. Whether the package folder is a symlink.
          example: false
        updatable:
          type: boolean
          description: Installed packages only. Whether a newer version is available in the repository.
          example: false
        available_version:
          type: string
          nullable: true
          description: The newer version available in the repository. Only present when `updatable` is true.
          example: 2.1.14
        installed:
          type: boolean
          description: Repository and search responses only. Whether the package is installed locally.
          example: true
        premium:
          type: boolean
          description: Present (and `true`) only for premium packages.
          example: true
        licensed:
          type: boolean
          description: Premium packages only. Whether a licence key covering this package is on file.
          example: false
        vendor:
          type: string
          description: Premium packages only, when set. Who sells the package.
          example: Trilby Media
        purchase_url:
          type: string
          description: Premium packages only, when known. Where to buy a licence.
          example: https://licensing.getgrav.org/buy/algolia-pro
        dependencies:
          type: array
          description: Dependencies as written in the package blueprint (slug strings or `{name, version}` objects). Only present when set.
          items: {}
        compatibility:
          type: object
          description: Compatibility metadata, e.g. `grav` and `api` version constraints. Values are strings or lists of strings. Only present when known.
          additionalProperties: {}
          example:
            grav: ">=2.0.0"
        keywords:
          type: array
          items:
            type: string
          description: Tags or keywords associated with the package. Only present when set.
          example: [admin, panel, manager]
        icon:
          type: string
          description: Font Awesome icon name. Only present when set.
          example: empire
        screenshot:
          type: string
          nullable: true
          description: |
            Themes only. In repository responses, the full URL of the
            repository screenshot (only present when there is one). In
            installed-theme responses, a `/thumbnails/{file}` URL of the
            theme's own screenshot, or `null`.
        thumbnail:
          type: string
          nullable: true
          description: Installed themes only. A `/thumbnails/{file}` URL of the theme's thumbnail (capped at 500px), or `null`.
        custom_fields:
          type: object
          description: "`GET /gpm/plugins/{slug}` and `GET /gpm/themes/{slug}` only, when the package ships Admin Next custom fields. Maps each field type to itself."
          additionalProperties:
            type: string
          example:
            seo-magic-preview: seo-magic-preview
        settings_route:
          type: string
          description: Installed plugins only, when set. Hash route inside the admin page where the plugin's settings are drawn.
          example: "#settings"
        settings_page:
          type: string
          description: Installed plugins only, when set. Slug of the plugin whose admin page draws these settings.
          example: kahunacart
      required:
        - slug
        - name
        - version
        - type
        - description
        - description_html
        - author
        - homepage

    GpmPackageActionResult:
      type: object
      description: Result of installing or updating a single package.
      required: [message, package, type, dependencies]
      properties:
        message:
          type: string
          description: Human-readable summary; names installed dependencies when there were any.
          example: "Plugin 'shortcode-ui' installed successfully."
        package:
          type: string
          example: shortcode-ui
        type:
          type: string
          enum: [plugin, theme]
          example: plugin
        dependencies:
          type: array
          description: Slugs of dependencies installed or updated first.
          items:
            type: string
          example: [shortcode-core]

    # -- Scheduler ------------------------------------------------------------
    Job:
      type: object
      description: A configured scheduler job.
      properties:
        id:
          type: string
          description: Unique job identifier.
          example: default-site-backup
        command:
          type: string
          description: The command or callable to execute, or `(closure)` for a job registered as a PHP closure.
          example: Grav\Common\Backup\Backups::backup
        expression:
          type: string
          nullable: true
          description: Cron expression for the schedule.
          example: "0 3 * * *"
        enabled:
          type: boolean
          description: Whether the job is enabled.
          example: true
        status:
          type: string
          enum: [success, failure, pending]
          description: Status of the last run; `pending` when the job has never run.
          example: success
        last_run:
          type: string
          format: date-time
          nullable: true
          description: Timestamp of the last execution.
          example: "2026-03-26T03:00:00+00:00"
        last_run_trigger:
          type: string
          nullable: true
          enum: [cron, manual, null]
          description: >
            What started the last execution: `cron` for an automated trigger,
            `manual` for a run somebody asked for. `null` for a job that has
            never run, or one last run by a version that did not record this.
          example: cron
        next_run:
          type: string
          format: date-time
          nullable: true
          description: When the schedule next calls for this job to run.
          example: "2026-03-27T03:00:00+00:00"
        overdue:
          type: boolean
          description: >
            Whether the job has missed the last slot its schedule gave it.
            These are the jobs a run in `overdue` mode picks up.
          example: false
        error:
          type: string
          nullable: true
          description: Error message from the last run, or `null` if successful.
          example: null
      required:
        - id
        - command
        - expression
        - enabled
        - status

    # -- Webhooks -------------------------------------------------------------
    Webhook:
      type: object
      description: An outbound webhook configuration.
      properties:
        id:
          type: string
          description: Unique webhook identifier.
          example: wh_abc123
        url:
          type: string
          format: uri
          description: Destination URL for deliveries.
          example: https://example.com/hooks/grav
        secret:
          type: string
          description: |
            HMAC-SHA256 signing secret (`whsec_` prefix). Returned in full only
            by `POST /webhooks`; every other response masks it, keeping the
            first 6 and last 4 characters.
          example: whsec_************************************************a1b2
        events:
          type: array
          items:
            type: string
            enum:
              - "*"
              - page.created
              - page.updated
              - page.deleted
              - page.moved
              - page.translated
              - pages.reordered
              - media.uploaded
              - media.deleted
              - user.created
              - user.updated
              - user.deleted
              - config.updated
              - gpm.installed
              - gpm.removed
              - grav.upgraded
          description: Event types subscribed to. `*` means all events (the default).
          example: [page.created, page.updated]
        enabled:
          type: boolean
          description: Whether the webhook is active.
          example: true
        headers:
          type: object
          additionalProperties:
            type: string
          description: |
            Custom HTTP headers included in deliveries. An empty set is
            serialized as an empty JSON array (`[]`).
          example: {}
        created:
          type: integer
          description: Creation time as a Unix timestamp.
          example: 1774008000
        failure_count:
          type: integer
          description: Consecutive failed deliveries. Reset to 0 on a successful delivery; the webhook is disabled at 5.
          example: 0
        disabled_reason:
          type: string
          description: Present only when the webhook was disabled automatically after repeated failures.
          example: Auto-disabled after 5 consecutive failures
      required:
        - id
        - url
        - secret
        - events
        - enabled
        - created

    WebhookDelivery:
      type: object
      description: A record of a single webhook delivery attempt.
      properties:
        id:
          type: string
          description: Unique delivery identifier (also sent as the `X-Grav-Delivery` header).
          example: dlv_1a2b3c4d5e6f7a8b
        event:
          type: string
          description: The event type that triggered this delivery (`test` for test deliveries).
          example: page.updated
        url:
          type: string
          format: uri
          description: The URL the payload was delivered to.
          example: https://example.com/hooks/grav
        request_headers:
          type: object
          additionalProperties:
            type: string
          description: HTTP headers sent with the delivery, including `X-Grav-Signature`, `X-Grav-Event` and `X-Grav-Delivery`.
        request_body:
          type: object
          description: The JSON payload that was sent.
          properties:
            event:
              type: string
              example: page.updated
            timestamp:
              type: string
              format: date-time
              example: "2026-03-25T14:30:00+00:00"
            data:
              type: object
              additionalProperties: true
            webhook_id:
              type: string
              example: wh_abc123
        created:
          type: integer
          description: Time of the delivery attempt as a Unix timestamp.
          example: 1774449000
        status_code:
          type: integer
          description: HTTP status code returned by the receiver, or `0` when the request failed before a response.
          example: 200
        response_body:
          type: string
          description: Response body from the receiver, truncated to 1000 characters. Absent when the request failed.
        error:
          type: string
          description: Transport error message. Present only when the request failed before a response.
        success:
          type: boolean
          description: Whether the delivery was successful (2xx response).
          example: true
        duration_ms:
          type: integer
          description: Request duration in milliseconds.
          example: 234
      required:
        - id
        - event
        - url
        - status_code
        - success
        - created

    # -- Dashboard ------------------------------------------------------------
    Notification:
      type: object
      description: |
        A dashboard notification. Notifications are published by getgrav.org
        (`notifications2.json`) and passed through as-is, and plugins can add
        their own through the `onApiDashboardNotifications` event, so fields
        beyond `id` are optional and extra fields may appear.
      additionalProperties: true
      properties:
        id:
          oneOf:
            - type: integer
            - type: string
          description: Notification identifier, used by the hide endpoint.
          example: 214
        date:
          type: string
          description: Publication date as published (for example `2026-07-31 12:00`).
          example: "2026-07-31 12:00"
        type:
          type: string
          description: Notification kind, such as `info`, `warning` or `promo`.
          example: info
        location:
          type: array
          items:
            type: string
          description: Locations the notification is shown in.
          example: [feed]
        title:
          type: string
          description: Optional heading.
          example: KahunaCart
        message:
          type: string
          description: Notification text (Markdown).
          example: Run a real community on your own site with **Forum Pro** for Grav 2, now available.
        link:
          type: string
          description: Optional URL for more information.
          example: https://getgrav.org/premium/forum-pro
        icon:
          type: string
          description: Optional icon (usually an emoji).
        image:
          type: string
          description: Optional image URL.
        action:
          type: object
          description: Optional call-to-action button.
          properties:
            label:
              type: string
              example: Learn more
            url:
              type: string
              example: https://kahunacart.com
        reappear_after:
          type: string
          description: |
            Relative time (a PHP date modifier such as `+1 week`) after which a
            hidden notification is shown again. Without it, hiding is permanent.
      required:
        - id

    FeedItem:
      type: object
      description: A news item from the getgrav.org blog Atom feed.
      properties:
        title:
          type: string
          description: Article title.
          example: Grav 2.1 Released
        url:
          type: string
          format: uri
          description: URL to the full article.
          example: https://getgrav.org/blog/grav-2-1-released
        date:
          type: string
          format: date-time
          description: The entry's `updated` date.
          example: "2026-09-11T12:00:00+00:00"
        summary:
          type: string
          description: The entry's summary; empty when the feed has none.
          example: ""
      required:
        - title
        - url
        - date
        - summary

    DashboardStats:
      type: object
      description: Summary statistics for the dashboard overview.
      properties:
        pages:
          type: object
          properties:
            total:
              type: integer
              description: Number of pages (the root is not counted).
              example: 42
            published:
              type: integer
              example: 40
        users:
          type: object
          properties:
            total:
              type: integer
              description: Number of user accounts.
              example: 5
        plugins:
          type: object
          properties:
            total:
              type: integer
              description: Number of installed plugins.
              example: 12
            active:
              type: integer
              description: Number of enabled plugins.
              example: 10
            updatable:
              type: integer
              description: Plugins with an update available in GPM.
              example: 2
        themes:
          type: object
          properties:
            total:
              type: integer
              example: 2
            updatable:
              type: integer
              example: 0
            active_updatable:
              type: boolean
              description: Whether the active theme has an update available.
              example: false
        grav:
          type: object
          properties:
            updatable:
              type: boolean
              description: Whether a Grav core update is available.
              example: false
        media:
          type: object
          properties:
            total:
              type: integer
              description: Number of files under `user/media` (sidecar `.meta.yaml`, `media_order.yaml` and dot files are not counted). Cached for 5 minutes.
              example: 120
        theme:
          type: string
          nullable: true
          description: Currently active theme slug.
          example: quark
        grav_version:
          type: string
          description: Installed Grav version.
          example: 2.1.0
        php_version:
          type: string
          description: PHP runtime version.
          example: 8.3.14
        last_backup:
          type: string
          format: date-time
          nullable: true
          description: Modification time of the most recent backup archive, or `null` if no backups exist.
          example: "2026-03-26T03:00:00+00:00"
      required:
        - pages
        - users
        - plugins
        - themes
        - grav
        - media
        - theme
        - grav_version
        - php_version
        - last_backup

    # -- Batch ----------------------------------------------------------------
    BatchResult:
      type: object
      description: Result of a single operation within a batch request.
      properties:
        route:
          type: string
          description: The page route that was operated on.
          example: /blog/draft-one
        status:
          type: string
          enum: [success, error]
          description: Whether this individual operation succeeded.
          example: success
        message:
          type: string
          description: Error message. Present only when `status` is `error`.
          example: Page permissions deny 'publish' on this page.
      required:
        - route
        - status

    AuthUserProfile:
      type: object
      description: The logged-in user's profile and resolved permissions, as returned by `GET /me` and inside every login token response.
      properties:
        username:
          type: string
          example: admin
        fullname:
          type: string
          nullable: true
        email:
          type: string
          nullable: true
        avatar_url:
          type: string
          nullable: true
        super_admin:
          type: boolean
        access:
          type: object
          additionalProperties: true
          description: Resolved permission map, including permissions inherited from groups.
        content_editor:
          type: string
          description: Preferred content editor, or an empty string for the default.
        demo_mode:
          type: object
          properties:
            enabled:
              type: boolean
              description: Whether this is a demo account whose writes are blocked.
            writable:
              type: array
              description: Routes a demo account may still write to. Empty for normal accounts.
              items:
                type: string
            reset_interval:
              type: integer
              description: Minutes between automatic demo content resets.
            seconds_until_reset:
              type: integer
              nullable: true
              description: Seconds until the next demo reset; null for normal accounts.
      required: [username, super_admin, access]

    AuthTokenPairWithUser:
      description: A JWT token pair plus the profile of the user it was issued to.
      allOf:
        - $ref: "#/components/schemas/TokenResponse"
        - type: object
          properties:
            user:
              $ref: "#/components/schemas/AuthUserProfile"
          required: [user]

    AuthTwoFactorChallenge:
      type: object
      description: Returned instead of tokens when the account has 2FA enabled. Finish the login with `POST /auth/2fa/verify`.
      properties:
        requires_2fa:
          type: boolean
          enum: [true]
        challenge_token:
          type: string
        expires_in:
          type: integer
          description: Challenge lifetime in seconds.
        token_type:
          type: string
          enum: [Challenge]
      required: [requires_2fa, challenge_token, expires_in, token_type]

    AuthPasswordPolicy:
      type: object
      description: The site's password policy, for client-side validation.
      properties:
        regex:
          type: string
          description: The raw `system.pwd_regex` value; empty when none is set.
        min_length:
          type: integer
          description: Minimum length parsed from the regex (0 when the regex has none), or 8 when no regex is set, since that floor is enforced instead.
        rules:
          type: array
          description: Checklist rules, from `system.pwd_rules` when configured, otherwise parsed from the regex, or a single 8-character length rule when no regex is set.
          items:
            type: object
            properties:
              id:
                type: string
                example: digit
              label:
                type: string
                example: At least one number
              pattern:
                type: string
                description: Regex fragment the password must contain a match for.
                example: "\\d"
            required: [id, label, pattern]

    AuthSsoProvider:
      type: object
      description: A single sign-on button for the login screen.
      properties:
        id:
          type: string
          description: Provider id used in `/auth/sso/{provider}/start`.
        label:
          type: string
        icon:
          type: string
          description: Icon class, or empty.
        plugin:
          type: string
          description: Slug of the plugin that provides it, or empty.
      required: [id, label, icon, plugin]

    MenubarItem:
      type: object
      description: A plugin toolbar button. Plugins may add further keys, which are passed through.
      additionalProperties: true
      properties:
        id:
          type: string
        plugin:
          type: string
          description: Owning plugin slug; the first path segment of the action route.
        label:
          type: string
        icon:
          type: string
          description: Font Awesome icon class.
        action:
          type: string
          description: Action key for `POST /menubar/actions/{plugin}/{action}`.
        confirm:
          type: string
          description: Optional confirmation prompt shown before running the action.
        variant:
          type: string
          description: Visual emphasis mapped to theme tokens. Unknown values fall back to the default style.
          example: primary
        showLabel:
          type: boolean
        size:
          type: string
          enum: [sm, md]
        placement:
          type: string
          enum: [start, end]
        priority:
          type: integer
          description: Order within the zone; higher renders earlier.

    PageLanguageVersion:
      type: object
      description: One language version of a page, as returned by the compare endpoint.
      properties:
        lang:
          type: string
        exists:
          type: boolean
          description: Whether the page has its own file for this language, rather than a fallback.
        title:
          type: string
        content:
          type: string
          description: Raw Markdown body.
        header:
          type: object
          additionalProperties: true
          description: Frontmatter.
        modified:
          type: string
          format: date-time
          nullable: true

    UserListColumn:
      type: object
      description: A plugin-declared extra column for the users list. Other keys the plugin sets are passed through.
      additionalProperties: true
      properties:
        id:
          type: string
        plugin:
          type: string
        label:
          type: string
        field:
          type: string
          description: Key looked up in each user's `extra` map.
        formatter:
          type: string
          enum: [text, link, date, datetime, boolean, number, badge]
        labelField:
          type: string
          description: For the `link` formatter only, the `extra` key holding the visible link text.
        sortable:
          type: boolean
          description: Client-side sort of the current page only.
        priority:
          type: integer
      required: [id, field, formatter, sortable]

    UserListFilterTab:
      type: object
      description: A tab above the users list. Other keys the plugin sets are passed through.
      additionalProperties: true
      properties:
        id:
          type: string
          description: Sent as `?filter=<id>` to `GET /users`. `all` is the built-in tab.
        plugin:
          type: string
        label:
          type: string
        icon:
          type: string
        priority:
          type: integer
        badge:
          type: string
          nullable: true
          description: Static badge text or count.
        badgeEndpoint:
          type: string
          description: "API path that returns `{ count: N }` for a live badge."
      required: [id]

    UserRowAction:
      type: object
      description: A plugin-declared action button for each row of the users list.
      properties:
        id:
          type: string
        plugin:
          type: string
        label:
          type: string
        icon:
          type: string
        action:
          type: string
          description: Verb passed back to the plugin's handler.
        confirm:
          type: string
          description: Optional confirmation prompt.
        priority:
          type: integer
      required: [id, plugin, label, priority]

    UserRowActionResult:
      type: object
      description: The outcome of a row action.
      properties:
        status:
          type: string
          enum: [success, error]
        message:
          type: string
          description: Toast text, at most 512 characters.
        url:
          type: string
          description: Same-origin URL to navigate to; dropped when the handler returns anything else.
      required: [status]

    Group:
      type: object
      description: A user group and the access rules it grants.
      properties:
        groupname:
          type: string
          description: Group identifier and storage key.
          example: editors
        readableName:
          type: string
          description: Display name (empty string when unset).
          example: Editors
        description:
          type: string
          example: Can edit and publish pages.
        icon:
          type: string
          description: Icon name (empty string when unset).
          example: users
        enabled:
          type: boolean
          example: true
        access:
          type: object
          additionalProperties: true
          description: Nested permission map. Always a JSON object, `{}` when empty.
          example:
            api:
              access: true
              pages:
                read: true
                write: true
      required: [groupname, readableName, description, icon, enabled, access]

    GroupWriteRequest:
      type: object
      description: Writable group fields. On create, omitted fields fall back to defaults; on update, only the fields sent are changed.
      properties:
        readableName:
          type: string
          example: Editors
        description:
          type: string
          example: Can edit and publish pages.
        icon:
          type: string
          example: users
        enabled:
          type: boolean
          default: true
        access:
          type: object
          additionalProperties: true
          description: Nested permission map. Replaces the stored map whole; a non-object value is stored as an empty map.

    DemoStatus:
      type: object
      description: Demo-mode reset state.
      properties:
        baseline_exists:
          type: boolean
          description: Whether a baseline has been captured. Resets do nothing without one.
        writable:
          type: array
          description: Permissions demo accounts may still write with (`plugins.api.demo.writable`), e.g. `api.pages.write`.
          items:
            type: string
        roots:
          type: array
          description: Names of the writable folders a reset restores (e.g. `pages`, `media`), limited to folders that exist on disk.
          items:
            type: string
        reset_interval:
          type: integer
          description: Minutes between automatic resets.
          example: 30
        reset_on_request:
          type: boolean
          description: Whether stale content is reset lazily during API requests.
        reset_on_schedule:
          type: boolean
          description: Whether a scheduler job performs resets.
        last_reset:
          type: integer
          nullable: true
          description: Unix timestamp of the last reset, or null if none has run.
          example: 1790000000
        seconds_until_reset:
          type: integer
          nullable: true
          description: Seconds until the next reset is due; 0 means due now, null when no baseline exists.
          example: 1234

    EnvironmentEntry:
      type: object
      description: An environment configuration folder.
      properties:
        name:
          type: string
          description: Environment name (empty string for the base configuration).
          example: staging.example.com
        label:
          type: string
          example: staging.example.com
        exists:
          type: boolean
        hasOverrides:
          type: boolean
          description: Whether the environment folder contains any configuration overrides.

    BlueprintUploadedFile:
      type: object
      description: A file saved by a blueprint file-field upload.
      properties:
        name:
          type: string
          description: Saved file name (may differ from the uploaded name with `random_name` or `avoid_overwriting`).
          example: logo.png
        path:
          type: string
          description: Logical path relative to the Grav root; pass it back to `DELETE /blueprint-upload`.
          example: user/themes/quark/images/logo/logo.png
        size:
          type: integer
          example: 20480
        type:
          type: string
          description: Detected MIME type.
          example: image/png
        url:
          type: string
          nullable: true
          description: Public URL of the file.
          example: /user/themes/quark/images/logo/logo.png
      required: [name, path, size, type]

    # -- Audit -----------------------------------------------------------------
    AuditEvent:
      type: object
      description: One recorded API action.
      properties:
        id:
          type: integer
          example: 42
        ts:
          type: integer
          format: int64
          description: Time of the action in Unix epoch milliseconds.
          example: 1758441600000
        event:
          type: string
          description: Event code in `namespace.action` form.
          example: page.update
        severity:
          type: string
          description: RFC 5424 level. Recorded events use `info`, `notice` or `warning`.
          example: info
        actor_id:
          type: string
          nullable: true
          example: admin
        actor_name:
          type: string
          nullable: true
          example: Admin User
        actor_roles:
          type: array
          items:
            type: string
          example: [admin]
        auth_method:
          type: string
          nullable: true
          description: How the caller authenticated.
          enum: [apikey, jwt, session, null]
          example: jwt
        ip:
          type: string
          nullable: true
          description: Client IP; the last part is masked when `audit.anonymize_ip` is on.
          example: 127.0.0.1
        user_agent:
          type: string
          nullable: true
          example: Mozilla/5.0
        target_type:
          type: string
          nullable: true
          example: page
        target_id:
          type: string
          nullable: true
          example: /blog/my-first-post
        status:
          type: integer
          nullable: true
          description: Reserved; currently always null.
          example: null
        context:
          type: object
          nullable: true
          additionalProperties: true
          description: "Event-specific detail. With `coverage: detailed`, page edits include field-level before/after values."

    # -- Twig in Content -------------------------------------------------------
    TwigContentLeak:
      type: object
      description: A page whose raw Twig markers render verbatim to visitors.
      properties:
        route:
          type: string
          example: /blog/my-first-post
        requested:
          type: boolean
          description: Whether the page asks for Twig processing (`process.twig`).
          example: true
        gate:
          type: boolean
          description: Master gate state when checked.
          example: false
        reason:
          type: string
          enum: [gate_off, page_off]
          description: "`gate_off`: the site-wide gate is off. `page_off`: the gate is on but this page does not request Twig."
          example: gate_off

    TwigContentEvent:
      type: object
      description: A recent blocked or blanked content-Twig token from the diagnostics ring buffer (last 50 kept).
      properties:
        type:
          type: string
          enum: [gate_blocked, sandbox_tag, sandbox_filter, sandbox_function, sandbox_method, sandbox_property, xss_blanked]
          example: sandbox_filter
        route:
          type: string
          example: /blog/my-first-post
        token:
          type: string
          example: markdown
        class:
          type: string
          description: Owning class for method/property blocks; empty otherwise.
          example: ""
        hint:
          type: string
          description: Plain-text remediation hint.
        timestamp:
          type: integer
          description: Unix epoch seconds.
          example: 1758441600
        allowlist:
          type: object
          allOf:
            - $ref: "#/components/schemas/TwigContentAllowlistDescriptor"
          nullable: true

    TwigContentAllowlistDescriptor:
      type: object
      description: What `POST /reports/twig-content/allowlist` needs to allow this token. Null for events with no allowlist remedy.
      properties:
        rule:
          type: string
          enum: [tag, filter, function, method, property]
          example: filter
        key:
          type: string
          enum: [allowed_tags, allowed_filters, allowed_functions, allowed_methods, allowed_properties]
          example: allowed_filters
        kind:
          type: string
          enum: [list, map]
          example: list
        token:
          type: string
          example: markdown
        class:
          type: string
          example: ""

    TwigContentClassMembersRow:
      type: object
      properties:
        class:
          type: string
          example: Grav\Common\Page\Page
        methods:
          type: string
          description: Comma-separated member names.
          example: title, url

    TwigContentTokenRoutes:
      type: object
      description: Map of token name to the routes using it. Arrives as `[]` when empty.
      additionalProperties:
        type: array
        items:
          type: string

    TwigContentSandboxFlatList:
      type: object
      properties:
        defaults:
          type: array
          items:
            type: string
        added:
          type: array
          items:
            type: string
        denied:
          type: array
          items:
            type: string
        effective:
          type: array
          items:
            type: string

    TwigContentSandboxClassList:
      type: object
      description: Each entry maps a class name to its allowed member names. An empty map arrives as `[]`.
      properties:
        defaults:
          type: object
          additionalProperties:
            type: array
            items:
              type: string
        added:
          type: object
          additionalProperties:
            type: array
            items:
              type: string
        denied:
          type: object
          additionalProperties:
            type: array
            items:
              type: string
        effective:
          type: object
          additionalProperties:
            type: array
            items:
              type: string

    # -- Translations editor ---------------------------------------------------
    I18nKeyRow:
      type: object
      description: One translation key with its source value and a cell per language.
      properties:
        key:
          type: string
          example: THEME_QUARK.SEARCH
        namespace:
          type: string
          description: The part of the key before the first dot.
          example: THEME_QUARK
        source_value:
          type: string
          nullable: true
          description: Value in the source language (the site override if one exists).
          example: Search
        providers:
          type: array
          items:
            type: string
          description: Source ids that ship this key.
          example: ["theme:quark"]
        owner:
          type: string
          nullable: true
          description: Source id whose value wins at runtime.
          example: theme:quark
        known:
          type: boolean
          description: False when no source ships the key (only a site override names it).
          example: true
        values:
          type: object
          description: Keyed by language code.
          additionalProperties:
            $ref: "#/components/schemas/I18nCell"
          example:
            fr:
              value: Rechercher
              state: overridden
              shipped: Recherche

    I18nCell:
      type: object
      properties:
        value:
          type: string
          nullable: true
          description: What the site shows for this language; null when missing.
        state:
          type: string
          enum: [shipped, overridden, missing]
          description: "`shipped` comes from a plugin, theme or core; `overridden` is this site's own wording; `missing` means a fallback is being shown."
        shipped:
          type: string
          nullable: true
          description: The value the sources ship, ignoring site overrides.

    # -- Admin Next preferences ------------------------------------------------
    PreferencesValues:
      type: object
      description: |
        User-overridable preference keys (site defaults in `ui.defaults`, per-user overrides in `admin_next.preferences`). In a PATCH body every key is optional; in `PATCH /admin-next/preferences/user` a `null` value removes that override.
      properties:
        colorMode:
          type: string
          nullable: true
          enum: ["", light, dark]
          description: Empty string follows the operating system.
        accentHue:
          type: integer
          nullable: true
          minimum: 0
          maximum: 360
          example: 271
        accentSaturation:
          type: integer
          nullable: true
          minimum: 0
          maximum: 100
          example: 91
        fontFamily:
          type: string
          nullable: true
          enum: [inter, google-sans, public-sans, nunito-sans, jost, albert-sans]
        fontSize:
          type: string
          nullable: true
          enum: [small, normal, large, xlarge, null]
        editorMode:
          type: string
          nullable: true
          enum: [normal, expert]
        editorKeymap:
          type: string
          nullable: true
          enum: [default, vim]
        editorStickyToolbar:
          type: boolean
          nullable: true
        editorFixedHeight:
          type: integer
          nullable: true
          description: 0 lets the editor grow with its content; any other value is clamped to 300-1200 pixels.
          example: 0
        adminLanguage:
          type: string
          nullable: true
          maxLength: 32
          example: en
        pagesPerPage:
          type: integer
          nullable: true
          minimum: 1
          maximum: 200
          example: 20
        pagesViewMode:
          type: string
          nullable: true
          enum: [tree, list, miller]
        usersViewMode:
          type: string
          nullable: true
          enum: [cards, table]
        groupsViewMode:
          type: string
          nullable: true
          enum: [cards, table]
        pluginsViewMode:
          type: string
          nullable: true
          enum: [cards, table]
        themesViewMode:
          type: string
          nullable: true
          enum: [cards, table]
        flexAfterSave:
          type: string
          nullable: true
          enum: ["", create-new, edit, list]
          description: What Flex editors do after saving. Empty string follows the directory blueprint.

    PreferencesSiteSettings:
      type: object
      description: Site-only behavioral settings stored in `ui.settings`. Users cannot override these.
      properties:
        autoSaveEnabled:
          type: boolean
          example: false
        autoSaveToolbarUndo:
          type: boolean
          example: true
        autoSaveBatchWindowMs:
          type: integer
          minimum: 0
          example: 0
        collabEnabled:
          type: boolean
          example: true
        menubarLinks:
          type: array
          description: Custom links shown in the Admin Next menubar. Entries without both `label` and `url` are dropped.
          items:
            $ref: "#/components/schemas/PreferencesMenubarLink"

    PreferencesMenubarLink:
      type: object
      required: [label, url]
      properties:
        label:
          type: string
          maxLength: 64
          example: Docs
        url:
          type: string
          maxLength: 512
          example: https://learn.getgrav.org
        icon:
          type: string
          maxLength: 64
          example: book
        external:
          type: boolean
          example: true

    PreferencesMediaUpload:
      type: object
      description: Client-side image resize constraints, read from the classic admin plugin's `pagemedia` settings (all zeros when the classic admin is not installed). Read-only.
      properties:
        resizeWidth:
          type: integer
        resizeHeight:
          type: integer
        resizeQuality:
          type: number
          format: float
          example: 0.8
        minWidth:
          type: integer
        minHeight:
          type: integer
        maxWidth:
          type: integer
        maxHeight:
          type: integer

    BrandingSettings:
      type: object
      description: Site branding stored under `ui.branding` in `user/config/admin-next.yaml`.
      properties:
        mode:
          type: string
          enum: [default, text, custom]
          description: "`default` shows the Grav logo, `text` shows `text`, `custom` shows the uploaded logos."
        text:
          type: string
          maxLength: 64
          description: Logo text for `text` mode. Empty falls back to `Grav`.
          example: Grav
        logoLight:
          type: string
          description: Basename of the light logo inside `user/media/admin-next/`, or empty.
        logoDark:
          type: string
          description: Basename of the dark logo inside `user/media/admin-next/`, or empty.
        logoHeight:
          type: integer
          description: Custom logo height in CSS pixels, clamped to 16-44. 0 uses the built-in 28px.
          example: 0
        title:
          type: string
          maxLength: 64
          description: Sign-in screen and browser tab title. Empty uses the built-in copy.
        subtitle:
          type: string
          maxLength: 128
          description: Sign-in screen subtitle. Empty uses the built-in copy.
        showPoweredBy:
          type: boolean
          description: Show the "Powered by Grav CMS" line on the login and setup screens.
          example: true
        favicon:
          type: string
          description: Basename of a custom favicon inside `user/media/admin-next/`, or empty for the generated accent-colored favicon.

    BrandingUrls:
      type: object
      description: URLs for the branding files relative to the site root, without the site's base path (Admin Next prefixes its own `serverUrl`), or empty strings when unset. Built from the real location of the user folder, so a custom user path is honored.
      properties:
        light:
          type: string
          example: /user/media/admin-next/logo-light-3f9c2a7e5b.svg
        dark:
          type: string
          example: ""
        favicon:
          type: string
          example: ""

    PreferencesPayload:
      type: object
      description: The full resolved Admin Next preferences payload.
      properties:
        branding:
          $ref: "#/components/schemas/BrandingSettings"
        branding_urls:
          $ref: "#/components/schemas/BrandingUrls"
        site:
          allOf:
            - $ref: "#/components/schemas/PreferencesValues"
          description: Site defaults merged over built-in defaults (every key present).
        site_settings:
          $ref: "#/components/schemas/PreferencesSiteSettings"
        media_upload:
          $ref: "#/components/schemas/PreferencesMediaUpload"
        user:
          type: object
          additionalProperties: true
          description: The caller's raw saved overrides (only keys they have set).
        effective:
          allOf:
            - $ref: "#/components/schemas/PreferencesValues"
            - $ref: "#/components/schemas/PreferencesSiteSettings"
          description: Final values to apply (built-in defaults, then site defaults, then user overrides, then site-only settings).
        can_edit_site:
          type: boolean
          description: Whether the caller is a super-admin and may write site preferences and branding.
      example:
        branding:
          mode: default
          text: Grav
          logoLight: ""
          logoDark: ""
          logoHeight: 0
          title: ""
          subtitle: ""
          showPoweredBy: true
          favicon: ""
        branding_urls:
          light: ""
          dark: ""
          favicon: ""
        site:
          colorMode: ""
          accentHue: 271
          accentSaturation: 91
          fontFamily: google-sans
          fontSize: normal
          editorMode: normal
          editorKeymap: default
          editorStickyToolbar: true
          editorFixedHeight: 0
          adminLanguage: en
          pagesPerPage: 20
          pagesViewMode: miller
          usersViewMode: cards
          groupsViewMode: cards
          pluginsViewMode: cards
          themesViewMode: cards
          flexAfterSave: ""
        site_settings:
          autoSaveEnabled: false
          autoSaveToolbarUndo: true
          autoSaveBatchWindowMs: 0
          collabEnabled: true
          menubarLinks: []
        media_upload:
          resizeWidth: 0
          resizeHeight: 0
          resizeQuality: 0.8
          minWidth: 0
          minHeight: 0
          maxWidth: 0
          maxHeight: 0
        user:
          colorMode: dark
        effective:
          colorMode: dark
          accentHue: 271
          fontFamily: google-sans
          autoSaveEnabled: false
          collabEnabled: true
          menubarLinks: []
        can_edit_site: true

    # -- Dashboard widgets -----------------------------------------------------
    DashboardWidget:
      type: object
      description: A dashboard widget from the core registry or a plugin, annotated with its effective layout. Plugin widgets may carry extra plugin-defined keys.
      properties:
        id:
          type: string
          example: core.stats
        source:
          type: string
          enum: [core, plugin]
        label:
          type: string
          description: Display label or translation key.
          example: ADMIN_NEXT.DASHBOARD.WIDGETS.STATS
        icon:
          type: string
          example: BarChart3
        sizes:
          type: array
          description: Sizes this widget supports. A plugin widget that declares none (or no valid ones) gets `[sm, md, lg]`.
          items:
            type: string
            enum: [xs, sm, md, lg, xl]
        defaultSize:
          type: string
          enum: [xs, sm, md, lg, xl]
          description: Always one of `sizes`. A plugin widget that declares none gets `md`, or its first size when `md` isn't supported.
        priority:
          type: integer
          description: Default ordering weight (higher appears first when no layout sets `order`).
        visible:
          type: boolean
          description: Effective visibility (user layout, else site layout, else true).
        size:
          type: string
          enum: [xs, sm, md, lg, xl]
          description: Effective size, always one of `sizes`.
        order:
          type: integer
          description: Effective sort position (ascending).
      additionalProperties: true

    DashboardWidgetLayoutEntry:
      type: object
      required: [id]
      properties:
        id:
          type: string
          example: core.stats
        visible:
          type: boolean
          default: true
        size:
          type: string
          nullable: true
          enum: [xs, sm, md, lg, xl, null]
        order:
          type: integer
          description: Sort position. Leave it out to keep the widget's priority-based position.

    DashboardWidgetLayout:
      type: object
      description: A stored dashboard layout (per user or site-wide). Saving replaces the whole layout.
      properties:
        preset:
          type: string
          enum: [default, minimal, compact, custom]
          default: custom
        widgets:
          type: array
          items:
            $ref: "#/components/schemas/DashboardWidgetLayoutEntry"

    DashboardWidgetsResult:
      type: object
      properties:
        widgets:
          type: array
          items:
            $ref: "#/components/schemas/DashboardWidget"
        user_layout:
          $ref: "#/components/schemas/DashboardWidgetLayout"
        site_layout:
          $ref: "#/components/schemas/DashboardWidgetLayout"
        can_edit_site:
          type: boolean
          description: Whether the caller may save the site-wide layout.

    DashboardExposureProbe:
      type: object
      properties:
        location:
          type: string
          description: Folder path relative to the web root.
          example: user/data
        extension:
          type: string
          enum: [dat, txt, zip, json]
        url:
          type: string
          format: uri
          description: Absolute URL the browser should fetch.
        token:
          type: string
          description: Expected file contents (32-64 hex characters), or empty when the sentinel could not be created or read.
        available:
          type: boolean
          description: Whether a token is available, meaning the probe can be run.

    DashboardExposureProbeResult:
      type: object
      properties:
        url:
          type: string
          description: Legacy field, the first `user://data` probe URL (empty if none).
        token:
          type: string
          description: Legacy field, the first `user://data` probe token.
        available:
          type: boolean
          description: Legacy field, whether the first `user://data` probe is usable.
        probes:
          type: array
          items:
            $ref: "#/components/schemas/DashboardExposureProbe"

    # -- UI extension registries -----------------------------------------------
    CustomFieldProvider:
      type: object
      properties:
        slug:
          type: string
          description: Plugin or theme slug that ships the field.
          example: seo-magic
        kind:
          type: string
          enum: [plugins, themes]
          description: Package kind, used in the script URL `/gpm/{kind}/{slug}/field/{type}`.

    EditorToolbarButton:
      type: object
      description: A markdown editor toolbar button contributed by a plugin. Plugins may add extra keys, which are passed through.
      properties:
        id:
          type: string
          example: youtube
        plugin:
          type: string
          example: youtube
        label:
          type: string
          description: Tooltip and aria-label.
          example: YouTube Video
        icon:
          type: string
          description: Inline SVG markup or an icon class.
        modal:
          type: object
          description: Open a plugin modal (served by `/gpm/plugins/{plugin}/modal-script/{component}`).
          properties:
            component:
              type: string
              example: youtube-insert
            title:
              type: string
            size:
              type: string
              example: md
        insert:
          type: object
          description: Insert content directly without a modal.
          properties:
            content:
              type: string
            mode:
              type: string
              enum: [insert-at-cursor, append, replace]
      additionalProperties: true

    FloatingWidget:
      type: object
      description: A floating widget contributed by a plugin. Plugins may add extra keys, which are passed through.
      properties:
        id:
          type: string
          example: ai-pro-chat
        plugin:
          type: string
          description: Owning plugin slug; its script is served by `/gpm/plugins/{plugin}/widget-script`.
          example: ai-pro
        label:
          type: string
        icon:
          type: string
          description: Lucide icon name.
        priority:
          type: integer
          description: Sort weight (higher first). Sorting is done by the client.
        autoLoad:
          type: boolean
          description: Load the widget script eagerly instead of on first open.
        routes:
          type: array
          description: Admin Next routes that scope `autoLoad`. Not a permission boundary. Omitted means everywhere.
          items:
            type: string
          example: [/users, /pages]
      additionalProperties: true

    ContextPanel:
      type: object
      description: A page-editor slide-in panel contributed by a plugin. Returned exactly as the plugin built it.
      properties:
        id:
          type: string
          example: revisions-pro
        plugin:
          type: string
          description: Owning plugin slug; its script is served by `/gpm/plugins/{plugin}/panel-script`.
          example: revisions-pro
        label:
          type: string
        icon:
          type: string
          description: Lucide icon name.
        contexts:
          type: array
          description: Where the trigger button appears.
          items:
            type: string
          example: [pages]
        priority:
          type: integer
          description: Sort weight (higher first). Sorting is done by the client.
        width:
          type: integer
          description: Panel width in pixels.
        badgeEndpoint:
          type: string
          description: Optional API path returning a `count` number for a badge on the trigger button.
      additionalProperties: true

    SettingsPanel:
      type: object
      description: A blueprint-driven section for the Admin Next Settings page, contributed by a plugin.
      properties:
        id:
          type: string
          example: login-settings
        plugin:
          type: string
          description: Plugin that owns the blueprint.
          example: api
        label:
          type: string
        description:
          type: string
        icon:
          type: string
        blueprint:
          type: string
          description: Blueprint file name.
        data_endpoint:
          type: string
          description: API path to GET the panel data from.
        save_endpoint:
          type: string
          description: API path to PATCH the panel data to.
        priority:
          type: integer
          description: Sort weight (higher first); the server sorts by it.
      additionalProperties: true

    # -- Invitations -----------------------------------------------------------
    Invitation:
      type: object
      properties:
        token:
          type: string
          description: The invite credential used in `/auth/invite/{token}`.
        email:
          type: string
          format: email
        fullname:
          type: string
        groups:
          type: array
          items:
            type: string
        created:
          type: integer
          description: Unix timestamp.
        created_by:
          type: string
          description: Username of the inviting admin.
        created_by_name:
          type: string
        expires:
          type: integer
          description: Unix timestamp.
        expired:
          type: boolean

    InvitationCreated:
      allOf:
        - $ref: "#/components/schemas/Invitation"
        - type: object
          properties:
            link:
              type: string
              format: uri
              description: Accept link, `{adminBase}/invite?token={token}`.
              example: https://example.com/admin/invite?token=9c1f0e6b2a7d4c8e5f3a1b9d0c7e2f4a
            email_sent:
              type: boolean
            warning:
              type: string
              description: Present when the email was not sent; share `link` manually.

    InvitationCreateRequest:
      type: object
      required: [email]
      properties:
        email:
          type: string
          format: email
          description: Must not belong to an existing account.
        fullname:
          type: string
          description: Prefilled on the accept form.
        access:
          type: object
          additionalProperties: true
          description: Permission map the new account receives. Super flags are removed unless the caller is a super-admin.
        groups:
          type: array
          items:
            type: string
          description: Groups the new account joins. Honored only for super-admin callers.
        expiration:
          type: integer
          description: Lifetime in seconds. Values under 300 fall back to `plugins.api.invitations.expiration` (default 604800, 7 days); values over 2592000 (30 days) are capped at 30 days.
          example: 604800
        message:
          type: string
          description: Optional personal note included in the email.
        admin_base_url:
          type: string
          format: uri
          description: Admin Next base URL used to build the link; accepted only for the site's own origin or a configured CORS origin.
